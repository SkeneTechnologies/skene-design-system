# ui/card

<!-- @design:generated -->

_Generated by `scripts/generate-design-md.mjs` from `machine/context.yaml`, `machine/components.yaml`, `machine/compositions.yaml`._
_Do not edit. Change the source and run `npm run design`; `npm run design:check` fails the build if this file is stale._
_@skene/design-system 0.18.0._

|  |  |
| --- | --- |
| import | `@skene/design-system/ui/card` |
| polarity | `inherits` |
| boundary | server-renderable |
| intent | `group-related-items` |

## Use for

The generic product container: header, content, footer. `variant="surface"` is the flat flavour of the same card — `border-surface-border` over `bg-surface-1`, 24px of its own padding — which appeared inline eight times across skene-site's routes plus six near-copies that differed only by being an `<a>`. SCOPE NARROWED IN 0.9.19: `surface` is no longer the marketing grid cell. `render_marketing_cards_as_feature_row` routes those to `FeatureRow`, whose copy-only path renders a content-height row so a 2-up grid does not gain a 600px floor per cell. On product surfaces this component is unchanged and remains the default container.

## Exports

`Card`, `CardHeader`, `CardFooter`, `CardTitle`, `CardDescription`, `CardContent`

## Props

| export | prop | type / allowed | required | default |
| --- | --- | --- | --- | --- |
| `Card` | `variant` | `CardVariant` |  | `"default"` |
| `Card` | `asChild` | `boolean` |  | `false` |

## Accepts

| export | props passed through |
| --- | --- |
| `Card` | `React.ComponentProps<"div">` |
| `CardHeader` | `React.ComponentProps<"div">` |
| `CardTitle` | `React.ComponentProps<"div">` |
| `CardDescription` | `React.ComponentProps<"div">` |
| `CardContent` | `React.ComponentProps<"div">` |
| `CardFooter` | `React.ComponentProps<"div">` |

## Also for

Every claim cites the prop, default or export that makes it true. A claim that cannot cite one is rejected by a test.

| claim | via |
| --- | --- |
| Any dashboard panel that is not a table and not a drawn artifact. | `export CardContent` |
| A flat marketing panel on a dark band, via `variant="surface"`: a border rather than a shadow, per principles.md 16. | `prop Card.variant` |
| A whole card that is a link, via `asChild` with an `<a>` or a `next/link` inside — the `surface` variant already carries `block` and `no-underline`, so the call site adds neither. | `prop Card.asChild` |

## Not for — reach for this instead

| instead | why |
| --- | --- |
| `AppPanel` | A panel inside a drawn Skene Cloud artifact. That is AppPanel, which is the same object in the artifact register. |

## Watch for

| note | via |
| --- | --- |
| It is themed, not chrome. On a fixed-dark marketing surface it follows the page theme rather than staying dark, which is usually not what a marketing band wants. | `export CardHeader` |
| `surface` bakes in `p-[24px]`, which `default` deliberately does not — `default` expects `CardHeader`/`CardContent`/`CardFooter` to bring their own `p-6`. Composing the sub-parts inside a `surface` card double-pads; pass `p-0` or use `default`. | `prop Card.variant` |

## Reachable from outside

`className`

## Rendered in

Gallery cases with a committed light and dark baseline: `ui-card`, `ui-card-surface`.

## What binds this module

- **Polarity `inherits`.** It puts no theme class on its own root, so it takes the page. Place it on a light fill and the `light` class is yours to add, or its mode-aware tokens resolve to their DARK values against that fill — measured at 1.07:1 on rendered pixels.
- **`chrome.*` is invariant.** This module is in the fixed-dark world, so the distinction bites here: `chrome.*` never inverts and belongs only on a surface that never flips. Anything on a surface that follows the theme uses `text.*` / `surface.*`. The two share their dark values, so the wrong pick looks correct until someone opens light mode.

These are the rules this module can break. The full set, the surface roles and
the contrast floors are in [DESIGN.md](../../DESIGN.md).

---

System-wide tokens, scales and the full module index: [DESIGN.md](../../DESIGN.md).
