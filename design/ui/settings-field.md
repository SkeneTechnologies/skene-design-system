# ui/settings-field

<!-- @design:generated -->

_Generated by `scripts/generate-design-md.mjs` from `machine/context.yaml`, `machine/components.yaml`, `machine/compositions.yaml`._
_Do not edit. Change the source and run `npm run design`; `npm run design:check` fails the build if this file is stale._
_@skene/design-system 0.18.0._

|  |  |
| --- | --- |
| import | `@skene/design-system/ui/settings-field` |
| polarity | `inherits` |
| boundary | `"use client"` — client component |
| intent | `collect-input` |

## Use for

The workspace form row: label, control, description, in one component.

## Exports

`SettingsInput`, `SettingsSelect`, `SettingsSwitch`

## Props

| export | prop | type / allowed | required | default |
| --- | --- | --- | --- | --- |
| `SettingsInput` | `tone` | `SettingsFieldVariantProps["tone"]` |  |  |
| `SettingsSelect` | `size` | `"default" \| "sm"` |  | `"default"` |
| `SettingsSelect` | `tone` | `"default" \| "muted"` |  |  |
| `SettingsSwitch` | `label` | `string` |  |  |
| `SettingsSwitch` | `labelPosition` | `"left" \| "right"` |  | `"left"` |
| `SettingsSwitch` | `labelClassName` | `string` |  |  |
| `SettingsSwitch` | `description` | `string` |  |  |

## Accepts

| export | props passed through |
| --- | --- |
| `SettingsInput` | `React.ComponentProps<typeof Input> & Pick<SettingsFieldVariantProps, "validation" \| "mono">` |
| `SettingsSelect` | `Omit<React.ComponentProps<"select">, "size">` |
| `SettingsSwitch` | `React.ComponentProps<typeof Switch>` |

## Also for

Every claim cites the prop, default or export that makes it true. A claim that cannot cite one is rejected by a test.

| claim | via |
| --- | --- |
| Any dense settings screen — three exports cover input, select and switch. | `export SettingsSwitch` |

## Not for — reach for this instead

| instead | why |
| --- | --- |
| `Input` | A field on a marketing form or a modal, where the density does not apply. That is Input. |

## Watch for

| note | via |
| --- | --- |
| machine/components.yaml records these as preferred over raw Input/Select/Switch on workspace surfaces. Reaching past them is how a settings page drifts out of rhythm. | `export SettingsInput` |

## Reachable from outside

`className`, `use client`

## Rendered in

Gallery cases with a committed light and dark baseline: `ui-settings-field`.

## What binds this module

- **Polarity `inherits`.** It puts no theme class on its own root, so it takes the page. Place it on a light fill and the `light` class is yours to add, or its mode-aware tokens resolve to their DARK values against that fill — measured at 1.07:1 on rendered pixels.

These are the rules this module can break. The full set, the surface roles and
the contrast floors are in [DESIGN.md](../../DESIGN.md).

---

System-wide tokens, scales and the full module index: [DESIGN.md](../../DESIGN.md).
