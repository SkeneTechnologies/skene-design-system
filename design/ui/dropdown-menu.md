# ui/dropdown-menu

<!-- @design:generated -->

_Generated by `scripts/generate-design-md.mjs` from `machine/context.yaml`, `machine/components.yaml`, `machine/compositions.yaml`._
_Do not edit. Change the source and run `npm run design`; `npm run design:check` fails the build if this file is stale._
_@skene/design-system 0.15.0._

|  |  |
| --- | --- |
| import | `@skene/design-system/ui/dropdown-menu` |
| polarity | `inherits` |
| boundary | `"use client"` — client component |
| intent | `navigate`, `reveal-on-demand` |

## Use for

Radix menu hung off a trigger.

## Exports

`DropdownMenu`, `DropdownMenuTrigger`, `DropdownMenuContent`, `DropdownMenuItem`, `DropdownMenuCheckboxItem`, `DropdownMenuRadioItem`, `DropdownMenuLabel`, `DropdownMenuSeparator`, `DropdownMenuShortcut`, `DropdownMenuGroup`, `DropdownMenuPortal`, `DropdownMenuSub`, `DropdownMenuSubContent`, `DropdownMenuSubTrigger`, `DropdownMenuRadioGroup`

## Props

| export | prop | type / allowed | required | default |
| --- | --- | --- | --- | --- |
| `DropdownMenuSubTrigger` | `inset` | `boolean` |  |  |
| `DropdownMenuItem` | `inset` | `boolean` |  |  |
| `DropdownMenuLabel` | `inset` | `boolean` |  |  |

## Accepts

| export | props passed through |
| --- | --- |
| `DropdownMenuSubTrigger` | `React.ComponentProps<typeof DropdownMenuPrimitive.SubTrigger>` |
| `DropdownMenuSubContent` | `React.ComponentProps<typeof DropdownMenuPrimitive.SubContent>` |
| `DropdownMenuContent` | `React.ComponentProps<typeof DropdownMenuPrimitive.Content>` |
| `DropdownMenuItem` | `React.ComponentProps<typeof DropdownMenuPrimitive.Item>` |
| `DropdownMenuCheckboxItem` | `React.ComponentProps<typeof DropdownMenuPrimitive.CheckboxItem>` |
| `DropdownMenuRadioItem` | `React.ComponentProps<typeof DropdownMenuPrimitive.RadioItem>` |
| `DropdownMenuLabel` | `React.ComponentProps<typeof DropdownMenuPrimitive.Label>` |
| `DropdownMenuSeparator` | `React.ComponentProps<typeof DropdownMenuPrimitive.Separator>` |
| `DropdownMenuShortcut` | `React.ComponentProps<"span">` |
| `DropdownMenu` | `DropdownMenuPrimitive.DropdownMenuProps` |
| `DropdownMenuTrigger` | `DropdownMenuPrimitive.DropdownMenuTriggerProps` |
| `DropdownMenuGroup` | `DropdownMenuPrimitive.DropdownMenuGroupProps` |
| `DropdownMenuPortal` | `DropdownMenuPrimitive.DropdownMenuPortalProps` |
| `DropdownMenuSub` | `DropdownMenuPrimitive.DropdownMenuSubProps` |
| `DropdownMenuRadioGroup` | `DropdownMenuPrimitive.DropdownMenuRadioGroupProps` |

## Also for

Every claim cites the prop, default or export that makes it true. A claim that cannot cite one is rejected by a test.

| claim | via |
| --- | --- |
| Row actions, a user menu, a filter with checkable items. | `export DropdownMenuCheckboxItem` |

## Not for — reach for this instead

| instead | why |
| --- | --- |
| `NavigationMenu` | Top-level site navigation with rich panels. That is NavigationMenu. |

## Watch for

| note | via |
| --- | --- |
| Also portalled — same theming caveat as Dialog. | `export DropdownMenuContent` |

## Reachable from outside

`className`, `use client`

## Custom properties

`--radix-dropdown-menu-content-transform-origin`, `--radix-dropdown-menu-content-available-height`

## Rendered in

Gallery cases with a committed light and dark baseline: `ui-dropdown-menu`.

## Rules that are not negotiable

1. **`chrome.*` is invariant and cannot invert.** `color.chrome.surface.*` and
   `color.chrome.text.*` are always dark. Use them only on a surface that never
   flips — a terminal, a log panel, a code frame. Anything on a surface that
   flips uses the theme-aware `color.text.*` / `color.surface.*`. The two share
   their dark values and diverge only in light, so the wrong pick looks correct
   until someone opens light mode.
2. **A light surface on a dark page needs the `light` class on its own root.**
   Without it the mode-aware tokens resolve to their dark values against a light
   fill. That has shipped text at 1.08:1. Check the module's `polarity` first —
   `applies-light` means the module already does this for you.
3. **Content is props.** No section hardcodes copy.

Theming is class-based and the package ships base mode `dark`. Contrast floors: body text 4.5:1, large text 3:1 (WCAG 2.2).

---

System-wide tokens, scales and the full module index: [DESIGN.md](../../DESIGN.md).
