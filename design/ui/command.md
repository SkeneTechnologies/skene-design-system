# ui/command

<!-- @design:generated -->

_Generated by `scripts/generate-design-md.mjs` from `machine/context.yaml`, `machine/components.yaml`, `machine/compositions.yaml`._
_Do not edit. Change the source and run `npm run design`; `npm run design:check` fails the build if this file is stale._
_@skene/design-system 0.26.1._

|  |  |
| --- | --- |
| import | `@skene/design-system/ui/command` |
| polarity | `inherits` |
| boundary | `"use client"` — client component |
| intent | `navigate`, `reveal-on-demand` |

## Use for

cmdk palette: a searchable list with groups and shortcuts.

## Exports

`Command`, `CommandDialog`, `CommandInput`, `CommandList`, `CommandEmpty`, `CommandGroup`, `CommandItem`, `CommandShortcut`, `CommandSeparator`

## Accepts

| export | props passed through |
| --- | --- |
| `Command` | `React.ComponentProps<typeof CommandPrimitive>` |
| `CommandInput` | `React.ComponentProps<typeof CommandPrimitive.Input>` |
| `CommandList` | `React.ComponentProps<typeof CommandPrimitive.List>` |
| `CommandEmpty` | `React.ComponentProps<typeof CommandPrimitive.Empty>` |
| `CommandGroup` | `React.ComponentProps<typeof CommandPrimitive.Group>` |
| `CommandSeparator` | `React.ComponentProps<typeof CommandPrimitive.Separator>` |
| `CommandItem` | `React.ComponentProps<typeof CommandPrimitive.Item>` |
| `CommandShortcut` | `React.ComponentProps<"span">` |
| `CommandDialog` | `DialogProps` |

## Also for

Every claim cites the prop, default or export that makes it true. A claim that cannot cite one is rejected by a test.

| claim | via |
| --- | --- |
| A workspace switcher or a jump-to-resource palette. | `export CommandItem` |

## Not for — reach for this instead

| instead | why |
| --- | --- |
| `Select` | Choosing one value from a short known list. That is Select. |

## Watch for

| note | via |
| --- | --- |
| CommandDialog is the modal wrapper; Command alone renders inline. Picking the wrong one gives a palette that cannot be dismissed or one that never opens. | `export CommandDialog` |

## Reachable from outside

`className`, `use client`

## Rendered in

Gallery cases with a committed light and dark baseline: `ui-command`.

## What binds this module

- **Polarity `inherits`.** It puts no theme class on its own root, so it takes the page. Place it on a light fill and the `light` class is yours to add, or its mode-aware tokens resolve to their DARK values against that fill — measured at 1.07:1 on rendered pixels.

These are the rules this module can break. The full set, the surface roles and
the contrast floors are in [DESIGN.md](../../DESIGN.md).

---

System-wide tokens, scales and the full module index: [DESIGN.md](../../DESIGN.md).
