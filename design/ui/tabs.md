# ui/tabs

<!-- @design:generated -->

_Generated by `scripts/generate-design-md.mjs` from `machine/context.yaml`, `machine/components.yaml`, `machine/compositions.yaml`._
_Do not edit. Change the source and run `npm run design`; `npm run design:check` fails the build if this file is stale._
_@skene/design-system 0.24.0._

|  |  |
| --- | --- |
| import | `@skene/design-system/ui/tabs` |
| polarity | `inherits` |
| boundary | `"use client"` — client component |
| intent | `navigate`, `reveal-on-demand` |

## Use for

Mutually exclusive views, as a segmented control.

## Exports

`Tabs`, `TabsList`, `TabsTrigger`, `TabsContent`

## Accepts

| export | props passed through |
| --- | --- |
| `TabsList` | `React.ComponentProps<typeof TabsPrimitive.List>` |
| `TabsTrigger` | `React.ComponentProps<typeof TabsPrimitive.Trigger>` |
| `TabsContent` | `React.ComponentProps<typeof TabsPrimitive.Content>` |
| `Tabs` | `TabsPrimitive.TabsProps` |

## Also for

Every claim cites the prop, default or export that makes it true. A claim that cannot cite one is rejected by a test.

| claim | via |
| --- | --- |
| Canvas/list, tokens/gallery — any either-or on one screen. | `export TabsTrigger` |

## Not for — reach for this instead

| instead | why |
| --- | --- |
| `SurfaceTile` | An editorial row of surfaces where one is picked out. That is SurfaceTiles, which has no interaction. |

## Watch for

| note | via |
| --- | --- |
| machine/components.yaml is explicit: do not hand-roll a segmented control from buttons and active classes. Controlled mode is needed when the panel renders outside TabsContent. | `export TabsContent` |

## Reachable from outside

`className`, `use client`

## Constraints

From `machine/components.yaml` (`primitives.Tabs`).

- Use for mutually exclusive sections or view modes (canvas/list, tokens/gallery)
- Do not hand-roll segmented controls with raw buttons and manual active classes
- Controlled Tabs (value + onValueChange) when panel content renders outside TabsContent
- Active trigger uses data-[state=active]:bg-card + border-border on bg-muted track

## Rendered in

Gallery cases with a committed light and dark baseline: `ui-tabs`.

## What binds this module

- **Polarity `inherits`.** It puts no theme class on its own root, so it takes the page. Place it on a light fill and the `light` class is yours to add, or its mode-aware tokens resolve to their DARK values against that fill — measured at 1.07:1 on rendered pixels.

These are the rules this module can break. The full set, the surface roles and
the contrast floors are in [DESIGN.md](../../DESIGN.md).

---

System-wide tokens, scales and the full module index: [DESIGN.md](../../DESIGN.md).
