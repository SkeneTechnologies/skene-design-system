# ui/button

<!-- @design:generated -->

_Generated by `scripts/generate-design-md.mjs` from `machine/context.yaml`, `machine/components.yaml`, `machine/compositions.yaml`._
_Do not edit. Change the source and run `npm run design`; `npm run design:check` fails the build if this file is stale._
_@skene/design-system 0.22.0._

|  |  |
| --- | --- |
| import | `@skene/design-system/ui/button` |
| polarity | `inherits` |
| boundary | server-renderable |
| intent | `drive-an-action` |

## Use for

The action primitive. Six variants, four sizes.

## Exports

`Button`

## Props

| export | prop | type / allowed | required | default |
| --- | --- | --- | --- | --- |
| `Button` | `asChild` | `boolean` |  | `false` |
| `Button` | `variant` | `"default" \| "destructive" \| "outline" \| "secondary" \| "ghost" \| "link" \| "glass" \| "glass-dark"` |  |  |
| `Button` | `size` | `"default" \| "sm" \| "lg" \| "icon"` |  |  |

## Accepts

| export | props passed through |
| --- | --- |
| `Button` | `React.ButtonHTMLAttributes<HTMLButtonElement>` |

## Also for

Every claim cites the prop, default or export that makes it true. A claim that cannot cite one is rejected by a test.

| claim | via |
| --- | --- |
| Any action anywhere in either app — the marketing CTA and the workspace toolbar are the same component. | `export Button` |

## Not for — reach for this instead

| instead | why |
| --- | --- |
| `PillNavLink` | A nav item in the marketing pill bar. That is PillNavLink. |

## Watch for

| note | via |
| --- | --- |
| The primary variant carries a fixed brand-peach fill rather than the theme-split primary token. Inside a cream surface peach resolves to its light value, so pass the variant you actually want rather than assuming the dark-page one. | `export Button` |

## Reachable from outside

`className`

## Custom properties

`--blur-glass`

## Constraints

From `machine/components.yaml` (`primitives.Button`).

**Variants** — `default`, `destructive`, `outline`, `secondary`, `ghost`, `link`

**Sizes** — `default`, `sm`, `lg`, `icon`

- Primary CTA uses default variant with fixed bg-brand-peach (#fec089), not theme-split --primary
- Do not add bg-[#...] overrides

## Rendered in

Gallery cases with a committed light and dark baseline: `ui-button`.

## What binds this module

- **Polarity `inherits`.** It puts no theme class on its own root, so it takes the page. Place it on a light fill and the `light` class is yours to add, or its mode-aware tokens resolve to their DARK values against that fill — measured at 1.07:1 on rendered pixels.

These are the rules this module can break. The full set, the surface roles and
the contrast floors are in [DESIGN.md](../../DESIGN.md).

---

System-wide tokens, scales and the full module index: [DESIGN.md](../../DESIGN.md).
