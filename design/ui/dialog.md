# ui/dialog

<!-- @design:generated -->

_Generated by `scripts/generate-design-md.mjs` from `machine/context.yaml`, `machine/components.yaml`, `machine/compositions.yaml`._
_Do not edit. Change the source and run `npm run design`; `npm run design:check` fails the build if this file is stale._
_@skene/design-system 0.25.0._

|  |  |
| --- | --- |
| import | `@skene/design-system/ui/dialog` |
| polarity | `inherits` |
| boundary | `"use client"` — client component |
| intent | `interrupt-the-reader` |

## Use for

Radix modal for a task the reader may abandon.

## Exports

`Dialog`, `DialogPortal`, `DialogOverlay`, `DialogTrigger`, `DialogClose`, `DialogContent`, `DialogHeader`, `DialogFooter`, `DialogTitle`, `DialogDescription`

## Accepts

| export | props passed through |
| --- | --- |
| `DialogOverlay` | `React.ComponentProps<typeof DialogPrimitive.Overlay>` |
| `DialogContent` | `React.ComponentProps<typeof DialogPrimitive.Content>` |
| `DialogHeader` | `React.ComponentProps<"div">` |
| `DialogFooter` | `React.ComponentProps<"div">` |
| `DialogTitle` | `React.ComponentProps<typeof DialogPrimitive.Title>` |
| `DialogDescription` | `React.ComponentProps<typeof DialogPrimitive.Description>` |
| `Dialog` | `DialogPrimitive.DialogProps` |
| `DialogTrigger` | `DialogPrimitive.DialogTriggerProps` |
| `DialogPortal` | `DialogPrimitive.DialogPortalProps` |
| `DialogClose` | `DialogPrimitive.DialogCloseProps` |

## Also for

Every claim cites the prop, default or export that makes it true. A claim that cannot cite one is rejected by a test.

| claim | via |
| --- | --- |
| A create/edit form, a detail view, a connect flow. | `export DialogContent` |

## Not for — reach for this instead

| instead | why |
| --- | --- |
| `AlertDialogAction` | A destructive confirmation. That is AlertDialog, which blocks until answered. |

## Watch for

| note | via |
| --- | --- |
| Content portals to document.body, so a light or dark class on an ancestor does not reach it. The theme has to be on the html element or on the content itself. | `export DialogPortal` |

## Reachable from outside

`className`, `use client`

## Rendered in

Gallery cases with a committed light and dark baseline: `ui-dialog`.

## What binds this module

- **Polarity `inherits`.** It puts no theme class on its own root, so it takes the page. Place it on a light fill and the `light` class is yours to add, or its mode-aware tokens resolve to their DARK values against that fill — measured at 1.07:1 on rendered pixels.

These are the rules this module can break. The full set, the surface roles and
the contrast floors are in [DESIGN.md](../../DESIGN.md).

---

System-wide tokens, scales and the full module index: [DESIGN.md](../../DESIGN.md).
