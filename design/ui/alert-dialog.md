# ui/alert-dialog

<!-- @design:generated -->

_Generated by `scripts/generate-design-md.mjs` from `machine/context.yaml`, `machine/components.yaml`, `machine/compositions.yaml`._
_Do not edit. Change the source and run `npm run design`; `npm run design:check` fails the build if this file is stale._
_@skene/design-system 0.20.0._

|  |  |
| --- | --- |
| import | `@skene/design-system/ui/alert-dialog` |
| polarity | `inherits` |
| boundary | `"use client"` — client component |
| intent | `interrupt-the-reader` |

## Use for

Radix modal that demands an answer before anything continues.

## Exports

`AlertDialog`, `AlertDialogPortal`, `AlertDialogOverlay`, `AlertDialogTrigger`, `AlertDialogContent`, `AlertDialogHeader`, `AlertDialogFooter`, `AlertDialogTitle`, `AlertDialogDescription`, `AlertDialogAction`, `AlertDialogCancel`

## Accepts

| export | props passed through |
| --- | --- |
| `AlertDialogOverlay` | `React.ComponentProps<typeof AlertDialogPrimitive.Overlay>` |
| `AlertDialogContent` | `React.ComponentProps<typeof AlertDialogPrimitive.Content>` |
| `AlertDialogHeader` | `React.ComponentProps<"div">` |
| `AlertDialogFooter` | `React.ComponentProps<"div">` |
| `AlertDialogTitle` | `React.ComponentProps<typeof AlertDialogPrimitive.Title>` |
| `AlertDialogDescription` | `React.ComponentProps<typeof AlertDialogPrimitive.Description>` |
| `AlertDialogAction` | `React.ComponentProps<typeof AlertDialogPrimitive.Action>` |
| `AlertDialogCancel` | `React.ComponentProps<typeof AlertDialogPrimitive.Cancel>` |
| `AlertDialog` | `AlertDialogPrimitive.AlertDialogProps` |
| `AlertDialogTrigger` | `AlertDialogPrimitive.AlertDialogTriggerProps` |
| `AlertDialogPortal` | `AlertDialogPrimitive.AlertDialogPortalProps` |

## Also for

Every claim cites the prop, default or export that makes it true. A claim that cannot cite one is rejected by a test.

| claim | via |
| --- | --- |
| Any destructive confirmation — delete, disconnect, revoke. | `export AlertDialogAction` |

## Not for — reach for this instead

| instead | why |
| --- | --- |
| `Dialog` | A form or a detail panel the reader may abandon. That is Dialog. |

## Watch for

| note | via |
| --- | --- |
| It traps focus and cannot be dismissed by clicking away, unlike Dialog. That is the whole difference; using it for a non-blocking message makes the product feel hostile. | `export AlertDialogCancel` |

## Reachable from outside

`className`, `use client`

## Rendered in

Gallery cases with a committed light and dark baseline: `ui-alert-dialog`.

## What binds this module

- **Polarity `inherits`.** It puts no theme class on its own root, so it takes the page. Place it on a light fill and the `light` class is yours to add, or its mode-aware tokens resolve to their DARK values against that fill — measured at 1.07:1 on rendered pixels.

These are the rules this module can break. The full set, the surface roles and
the contrast floors are in [DESIGN.md](../../DESIGN.md).

---

System-wide tokens, scales and the full module index: [DESIGN.md](../../DESIGN.md).
