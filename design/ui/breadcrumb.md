# ui/breadcrumb

<!-- @design:generated -->

_Generated by `scripts/generate-design-md.mjs` from `machine/context.yaml`, `machine/components.yaml`, `machine/compositions.yaml`._
_Do not edit. Change the source and run `npm run design`; `npm run design:check` fails the build if this file is stale._
_@skene/design-system 0.17.0._

|  |  |
| --- | --- |
| import | `@skene/design-system/ui/breadcrumb` |
| polarity | `inherits` |
| boundary | server-renderable |
| intent | `navigate` |

## Use for

The path back up a product hierarchy.

## Exports

`Breadcrumb`, `BreadcrumbList`, `BreadcrumbItem`, `BreadcrumbLink`, `BreadcrumbPage`, `BreadcrumbSeparator`, `BreadcrumbEllipsis`

## Props

| export | prop | type / allowed | required | default |
| --- | --- | --- | --- | --- |
| `BreadcrumbLink` | `asChild` | `boolean` |  |  |

## Accepts

| export | props passed through |
| --- | --- |
| `Breadcrumb` | `React.ComponentProps<"nav">` |
| `BreadcrumbList` | `React.ComponentProps<"ol">` |
| `BreadcrumbItem` | `React.ComponentProps<"li">` |
| `BreadcrumbLink` | `React.ComponentProps<"a">` |
| `BreadcrumbPage` | `React.ComponentProps<"span">` |
| `BreadcrumbSeparator` | `React.ComponentProps<"li">` |
| `BreadcrumbEllipsis` | `React.ComponentProps<"span">` |

## Also for

Every claim cites the prop, default or export that makes it true. A claim that cannot cite one is rejected by a test.

| claim | via |
| --- | --- |
| A workspace trail — workspace, project, resource. | `export BreadcrumbPage` |

## Not for — reach for this instead

| instead | why |
| --- | --- |
| `ArtTitle` | The crumb line inside a drawn artifact. AppWindow takes it as a prop and ArtTitle styles it. |

## Watch for

| note | via |
| --- | --- |
| BreadcrumbPage is the current leaf and renders unlinked. Wrapping it in a link is the common mistake and reads as a no-op to the reader. | `export BreadcrumbLink` |

## Reachable from outside

`className`

## Rendered in

Gallery cases with a committed light and dark baseline: `ui-breadcrumb`.

## What binds this module

- **Polarity `inherits`.** It puts no theme class on its own root, so it takes the page. Place it on a light fill and the `light` class is yours to add, or its mode-aware tokens resolve to their DARK values against that fill — measured at 1.07:1 on rendered pixels.

These are the rules this module can break. The full set, the surface roles and
the contrast floors are in [DESIGN.md](../../DESIGN.md).

---

System-wide tokens, scales and the full module index: [DESIGN.md](../../DESIGN.md).
