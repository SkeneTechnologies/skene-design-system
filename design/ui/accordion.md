# ui/accordion

<!-- @design:generated -->

_Generated by `scripts/generate-design-md.mjs` from `machine/context.yaml`, `machine/components.yaml`, `machine/compositions.yaml`._
_Do not edit. Change the source and run `npm run design`; `npm run design:check` fails the build if this file is stale._
_@skene/design-system 0.19.0._

|  |  |
| --- | --- |
| import | `@skene/design-system/ui/accordion` |
| polarity | `inherits` |
| boundary | `"use client"` — client component |
| intent | `reveal-on-demand` |

## Use for

Radix disclosure list. One trigger, one panel, animated open and close.

## Exports

`Accordion`, `AccordionItem`, `AccordionTrigger`, `AccordionContent`

## Accepts

| export | props passed through |
| --- | --- |
| `AccordionItem` | `React.ComponentProps<typeof AccordionPrimitive.Item>` |
| `AccordionTrigger` | `React.ComponentProps<typeof AccordionPrimitive.Trigger>` |
| `AccordionContent` | `React.ComponentProps<typeof AccordionPrimitive.Content>` |
| `Accordion` | `(AccordionPrimitive.AccordionSingleProps \| AccordionPrimitive.AccordionMultipleProps)` |

## Also for

Every claim cites the prop, default or export that makes it true. A claim that cannot cite one is rejected by a test.

| claim | via |
| --- | --- |
| A settings or filter group that collapses on a dense product screen. | `export AccordionItem` |

## Not for — reach for this instead

| instead | why |
| --- | --- |
| `FaqBand` | A cream FAQ band on a marketing page. FaqBand owns the ground, the two-column split, the hairline per row and the round toggle; this is the level below it. |

## Watch for

| note | via |
| --- | --- |
| It is the primitive, not the marketing band. FaqBand composes Radix directly because this trigger bakes in a chevron. | `export AccordionTrigger` |

## Reachable from outside

`className`, `use client`

## Rendered in

Gallery cases with a committed light and dark baseline: `ui-accordion`.

## What binds this module

- **Polarity `inherits`.** It puts no theme class on its own root, so it takes the page. Place it on a light fill and the `light` class is yours to add, or its mode-aware tokens resolve to their DARK values against that fill — measured at 1.07:1 on rendered pixels.

These are the rules this module can break. The full set, the surface roles and
the contrast floors are in [DESIGN.md](../../DESIGN.md).

---

System-wide tokens, scales and the full module index: [DESIGN.md](../../DESIGN.md).
