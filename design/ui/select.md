# ui/select

<!-- @design:generated -->

_Generated by `scripts/generate-design-md.mjs` from `machine/context.yaml`, `machine/components.yaml`, `machine/compositions.yaml`._
_Do not edit. Change the source and run `npm run design`; `npm run design:check` fails the build if this file is stale._
_@skene/design-system 0.15.0._

|  |  |
| --- | --- |
| import | `@skene/design-system/ui/select` |
| polarity | `inherits` |
| boundary | `"use client"` — client component |
| intent | `collect-input` |

## Use for

Radix select with the package's two sizes.

## Exports

`Select`, `SelectContent`, `SelectGroup`, `SelectItem`, `SelectLabel`, `SelectScrollDownButton`, `SelectScrollUpButton`, `SelectSeparator`, `SelectTrigger`, `SelectValue`

## Props

| export | prop | type | required | default |
| --- | --- | --- | --- | --- |
| `SelectTrigger` | `size` | `"sm" \| "default"` |  | `"default"` |

## Accepts

| export | props passed through |
| --- | --- |
| `Select` | `React.ComponentProps<typeof SelectPrimitive.Root>` |
| `SelectGroup` | `React.ComponentProps<typeof SelectPrimitive.Group>` |
| `SelectValue` | `React.ComponentProps<typeof SelectPrimitive.Value>` |
| `SelectTrigger` | `React.ComponentProps<typeof SelectPrimitive.Trigger>` |
| `SelectContent` | `React.ComponentProps<typeof SelectPrimitive.Content>` |
| `SelectLabel` | `React.ComponentProps<typeof SelectPrimitive.Label>` |
| `SelectItem` | `React.ComponentProps<typeof SelectPrimitive.Item>` |
| `SelectSeparator` | `React.ComponentProps<typeof SelectPrimitive.Separator>` |
| `SelectScrollUpButton` | `React.ComponentProps<typeof SelectPrimitive.ScrollUpButton>` |
| `SelectScrollDownButton` | `React.ComponentProps<typeof SelectPrimitive.ScrollDownButton>` |

## Also for

Every claim cites the prop, default or export that makes it true. A claim that cannot cite one is rejected by a test.

| claim | via |
| --- | --- |
| Any choice from a short known list, on either surface. | `export SelectItem` |

## Not for — reach for this instead

| instead | why |
| --- | --- |
| `SettingsSelect` | A labelled select on a workspace settings screen. That is SettingsSelect. |

## Watch for

| note | via |
| --- | --- |
| Content is portalled; the sm size exists for dense workspace rows. | `export SelectTrigger` |

## Reachable from outside

`className`, `use client`

## Custom properties

`--radix-select-content-available-height`, `--radix-select-content-transform-origin`, `--radix-select-trigger-width`

## Constraints

From `machine/components.yaml` (`primitives.Select`).

**Sizes** — `sm`, `default`

## Rendered in

Gallery cases with a committed light and dark baseline: `ui-select`.

## Rules that are not negotiable

1. **`chrome.*` is invariant and cannot invert.** `color.chrome.surface.*` and
   `color.chrome.text.*` are always dark. Use them only on a surface that never
   flips — a terminal, a log panel, a code frame. Anything on a surface that
   flips uses the theme-aware `color.text.*` / `color.surface.*`. The two share
   their dark values and diverge only in light, so the wrong pick looks correct
   until someone opens light mode.
2. **A light surface on a dark page needs the `light` class on its own root.**
   Without it the mode-aware tokens resolve to their dark values against a light
   fill. That has shipped text at 1.08:1. Check the module's `polarity` first —
   `applies-light` means the module already does this for you.
3. **Content is props.** No section hardcodes copy.

Theming is class-based and the package ships base mode `dark`. Contrast floors: body text 4.5:1, large text 3:1 (WCAG 2.2).

---

System-wide tokens, scales and the full module index: [DESIGN.md](../../DESIGN.md).
