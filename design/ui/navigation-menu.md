# ui/navigation-menu

<!-- @design:generated -->

_Generated by `scripts/generate-design-md.mjs` from `machine/context.yaml`, `machine/components.yaml`, `machine/compositions.yaml`._
_Do not edit. Change the source and run `npm run design`; `npm run design:check` fails the build if this file is stale._
_@skene/design-system 0.23.0._

|  |  |
| --- | --- |
| import | `@skene/design-system/ui/navigation-menu` |
| polarity | `inherits` |
| boundary | `"use client"` — client component |
| intent | `navigate`, `reveal-on-demand` |

## Use for

Radix top navigation with dropdown panels.

## Exports

`NavigationMenu`, `NavigationMenuList`, `NavigationMenuItem`, `NavigationMenuContent`, `NavigationMenuTrigger`, `NavigationMenuLink`, `NavigationMenuIndicator`, `NavigationMenuViewport`

## Accepts

| export | props passed through |
| --- | --- |
| `NavigationMenu` | `React.ComponentProps<typeof NavigationMenuPrimitive.Root>` |
| `NavigationMenuList` | `React.ComponentProps<typeof NavigationMenuPrimitive.List>` |
| `NavigationMenuTrigger` | `React.ComponentProps<typeof NavigationMenuPrimitive.Trigger>` |
| `NavigationMenuContent` | `React.ComponentProps<typeof NavigationMenuPrimitive.Content>` |
| `NavigationMenuViewport` | `React.ComponentProps<typeof NavigationMenuPrimitive.Viewport>` |
| `NavigationMenuIndicator` | `React.ComponentProps<typeof NavigationMenuPrimitive.Indicator>` |
| `NavigationMenuItem` | `NavigationMenuPrimitive.NavigationMenuItemProps` |
| `NavigationMenuLink` | `NavigationMenuPrimitive.NavigationMenuLinkProps` |

## Also for

Every claim cites the prop, default or export that makes it true. A claim that cannot cite one is rejected by a test.

| claim | via |
| --- | --- |
| A product or resources menu on the marketing site. | `export NavigationMenuContent` |

## Not for — reach for this instead

| instead | why |
| --- | --- |
| `PillNav` | The marketing site's own nav bar. That is PillNav. |

## Watch for

| note | via |
| --- | --- |
| PillNav is the Skene marketing nav shell and does not use this — it is a translucent bar over the hero. These two solve different halves of the same page. | `export NavigationMenuTrigger` |

## Reachable from outside

`className`, `use client`

## Custom properties

`--radix-navigation-menu-viewport-height`, `--radix-navigation-menu-viewport-width`

## Rendered in

Gallery cases with a committed light and dark baseline: `ui-navigation-menu`.

## What binds this module

- **Polarity `inherits`.** It puts no theme class on its own root, so it takes the page. Place it on a light fill and the `light` class is yours to add, or its mode-aware tokens resolve to their DARK values against that fill — measured at 1.07:1 on rendered pixels.

These are the rules this module can break. The full set, the surface roles and
the contrast floors are in [DESIGN.md](../../DESIGN.md).

---

System-wide tokens, scales and the full module index: [DESIGN.md](../../DESIGN.md).
