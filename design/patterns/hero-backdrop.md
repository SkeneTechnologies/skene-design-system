# patterns/hero-backdrop

<!-- @design:generated -->

_Generated by `scripts/generate-design-md.mjs` from `machine/context.yaml`, `machine/components.yaml`, `machine/compositions.yaml`._
_Do not edit. Change the source and run `npm run design`; `npm run design:check` fails the build if this file is stale._
_@skene/design-system 0.26.1._

|  |  |
| --- | --- |
| import | `@skene/design-system/patterns/hero-backdrop` |
| polarity | `inherits` |
| boundary | server-renderable |
| intent | `open-a-page`, `texture-a-surface` |

## Use for

A dark hero strip: a background image at low opacity, a gradient fading to the page ground, and content above it.

## Exports

`HeroBackdrop`

## Props

| export | prop | type / allowed | required | default |
| --- | --- | --- | --- | --- |
| `HeroBackdrop` | `image` | `string` |  |  |
| `HeroBackdrop` | `imageOpacity` | `number` |  | `0.9` |
| `HeroBackdrop` | `fadeTo` | `string` |  | `'var(--color-chrome-surface-darker)'` |
| `HeroBackdrop` | `children` | `React.ReactNode` | **yes** |  |

## Also for

Every claim cites the prop, default or export that makes it true. A claim that cannot cite one is rejected by a test.

| claim | via |
| --- | --- |
| Any short banner that needs to sit on the page rather than end abruptly — a subpage header, a section lead-in. | `prop HeroBackdrop.children` |
| A plain dark strip with no image at all: `image` is optional and the fade still renders. | `prop HeroBackdrop.image` |
| The textured split header — eyebrow, h1 and lede in a narrow track with an artifact beside them, the dither strip behind both. A recipe, not an export: wrap a two-column grid in `children` with `image` set to the dithered asset; the header comment carries the markup and skene-marketing-website's `page-shell.tsx` (`flat-split`) is the reference implementation. The grid's ratios and wrap width are page decisions, which is why the package ships the ground and not the grid. | `prop HeroBackdrop.children` |

## Not for — reach for this instead

| instead | why |
| --- | --- |
| `DitheredMedia` | A full hero with video, dither and a measured scrim. That is DitheredMedia; this is its subset and the two are scoped by size, not by look. |

## Easy to confuse with

`patterns/dither#DitheredMedia`

These edges are symmetric — the same pairing is recorded from the other side.

## Watch for

| note | via |
| --- | --- |
| `fadeTo` must match the ground of whatever follows, or the seam shows as a band edge. | `prop HeroBackdrop.fadeTo` |

## Reachable from outside

`style`, `className`

`style` means this module writes an inline style that beats any class you pass.

## Rendered in

Gallery cases with a committed light and dark baseline: `pattern-hero-backdrop`.

## What binds this module

- **Polarity `inherits`.** It puts no theme class on its own root, so it takes the page. Place it on a light fill and the `light` class is yours to add, or its mode-aware tokens resolve to their DARK values against that fill — measured at 1.07:1 on rendered pixels.
- **Content is props.** This pattern hardcodes no copy; the page supplies it.

These are the rules this module can break. The full set, the surface roles and
the contrast floors are in [DESIGN.md](../../DESIGN.md).

---

System-wide tokens, scales and the full module index: [DESIGN.md](../../DESIGN.md).
