# patterns/skene-mark

<!-- @design:generated -->

_Generated by `scripts/generate-design-md.mjs` from `machine/context.yaml`, `machine/components.yaml`, `machine/compositions.yaml`._
_Do not edit. Change the source and run `npm run design`; `npm run design:check` fails the build if this file is stale._
_@skene/design-system 0.17.0._

|  |  |
| --- | --- |
| import | `@skene/design-system/patterns/skene-mark` |
| polarity | `inherits` |
| boundary | server-renderable |
| intent | `brand-the-surface` |

## Use for

The brand artwork as elements: the square symbol, and the horizontal lockup with the wordmark.

## Exports

`SkeneMark`, `SkeneLockup`

## Props

| export | prop | type / allowed | required | default |
| --- | --- | --- | --- | --- |
| `SkeneMark` | `tone` | `SkeneMarkTone` |  | `'block'` |
| `SkeneMark` | `size` | `number` |  | `28` |
| `SkeneMark` | `radius` | `number` |  | `8` |
| `SkeneMark` | `alt` | `string` |  |  |
| `SkeneLockup` | `tone` | `SkeneLockupTone` |  | `'onDark'` |
| `SkeneLockup` | `height` | `number` |  | `26` |
| `SkeneLockup` | `alt` | `string` |  | `'Skene'` |

## Also for

Every claim cites the prop, default or export that makes it true. A claim that cannot cite one is rejected by a test.

| claim | via |
| --- | --- |
| Anywhere the product speaks for itself — an avatar, a card that says SKENE, a nav lockup. AgentCallout already defaults its avatar to it. | `export SkeneMark` |
| A favicon-shaped tile at any size: `size` moves the whole thing and `radius` only applies to the block tone, which is the one that brings its own ground. | `prop SkeneMark.size` |

## Not for — reach for this instead

| instead | why |
| --- | --- |
| `SkeneLockup` | A nav bar with no word beside the mark. Use SkeneLockup, which defaults its alt to Skene because there it IS the accessible name. |

## Watch for

| note | via |
| --- | --- |
| `tone` names the GROUND, not the ink — onLight is the BLACK glyph. Naming it after the ink is what makes a caller pick the invisible variant. | `prop SkeneMark.tone` |
| SkeneLockup is sized by `height`, not `size`: the artwork is 1016×260, and one prop name meaning width in one component and height in the other ships at the wrong scale. | `prop SkeneLockup.height` |

## Reachable from outside

`style`, `className`

`style` means this module writes an inline style that beats any class you pass.

## Constraints

From `machine/components.yaml` (`patterns.skeneMark`).

| prop | allowed |
| --- | --- |
| `tone` | `block`, `onDark`, `onLight` |
| `size` | px, square |
| `radius` | px, applied only to `block` — the other two have no fill to round |

- tone names the GROUND, not the ink: onLight is the BLACK glyph. Naming it after the ink is what makes a caller pick the invisible variant.
- block — the peach glyph on its own black tile — is the default because it brings its own ground and is safe anywhere. There is no peach-on-transparent file; do not fake one with a mask recolour, the counters are part of the artwork.
- Anywhere the product speaks for itself — the featured Bridge card, the AgentCallout avatar — this is the mark. Do not draw a ring glyph or a letter "S" as a stand-in.
- SkeneLockup is the symbol WITH the wordmark, and it is sized by `height` rather than by `size`: the artwork is 1016x260 and one number meaning width in one component and height in the other ships at the wrong scale. Its `accent` tone is the only one not named after a ground — peach symbol, white wordmark, dark grounds only.
- SkeneLockup defaults alt="Skene". A nav bar has no word beside it, so the lockup usually IS the accessible name; the mark defaults the other way because it sits next to type that already says it.

## Rendered in

Gallery cases with a committed light and dark baseline: `pattern-skene-mark`.

## What binds this module

- **Polarity `inherits`.** It puts no theme class on its own root, so it takes the page. Place it on a light fill and the `light` class is yours to add, or its mode-aware tokens resolve to their DARK values against that fill — measured at 1.07:1 on rendered pixels.
- **Content is props.** This pattern hardcodes no copy; the page supplies it.

These are the rules this module can break. The full set, the surface roles and
the contrast floors are in [DESIGN.md](../../DESIGN.md).

---

System-wide tokens, scales and the full module index: [DESIGN.md](../../DESIGN.md).
