# patterns/dither

<!-- @design:generated -->

_Generated by `scripts/generate-design-md.mjs` from `machine/context.yaml`, `machine/components.yaml`, `machine/compositions.yaml`._
_Do not edit. Change the source and run `npm run design`; `npm run design:check` fails the build if this file is stale._
_@skene/design-system 0.15.0._

|  |  |
| --- | --- |
| import | `@skene/design-system/patterns/dither` |
| polarity | `inherits` |
| boundary | server-renderable |
| intent | `open-a-page`, `texture-a-surface` |

## Use for

The Skene surface treatment: a halftone texture blended over photographic or video media, and the full hero composition built from it. DitherOverlay is the raw layer; DitheredMedia is media, dither, scrim, gradient and content in one.

## Exports

`DitherOverlay`, `DitheredMedia`

## Props

| export | prop | type | required | default |
| --- | --- | --- | --- | --- |
| `DitherOverlay` | `src` | `string` | yes |  |
| `DitherOverlay` | `opacity` | `number` |  | `0.8` |
| `DitherOverlay` | `blend` | `'soft-light' \| 'overlay' \| 'multiply' \| 'screen'` |  | `'soft-light'` |
| `DitheredMedia` | `video` | `string` |  |  |
| `DitheredMedia` | `image` | `string` |  |  |
| `DitheredMedia` | `dither` | `string` |  |  |
| `DitheredMedia` | `fadeTo` | `string` |  | `'var(--color-chrome-surface-darker)'` |
| `DitheredMedia` | `scrim` | `number` |  | `0.56` |
| `DitheredMedia` | `poster` | `string` |  |  |
| `DitheredMedia` | `children` | `React.ReactNode` |  |  |

## Also for

Every claim cites the prop, default or export that makes it true. A claim that cannot cite one is rejected by a test.

| claim | via |
| --- | --- |
| Any full-bleed media band, not only a hero — the `video` and `image` props are both optional and the composition renders with either. | `prop DitheredMedia.video` |
| Texture over your own stack: DitherOverlay takes a `src` and a `blend`, so it composes onto anything with a position context. | `prop DitherOverlay.src` |
| Joining a band to whatever follows it, by setting `fadeTo` to the next section's ground. | `prop DitheredMedia.fadeTo` |

## Not for — reach for this instead

| instead | why |
| --- | --- |
| `HeroBackdrop` | A dark strip that fades into the page with no media over it. That is HeroBackdrop, which is this composition's subset. |
| `SectionBackdrop` | A halftone field behind a mock inside a feature row. That is SectionBackdrop, which ships the three site textures. |

## Easy to confuse with

`patterns/hero-backdrop#HeroBackdrop`

These edges are symmetric — the same pairing is recorded from the other side.

## Watch for

| note | via |
| --- | --- |
| `scrim` is measured, not decorative. Video is not a background you can check once: the homepage hero measured 1.04:1 for peach display type over a bright frame. The default is 0.56 and setting 0 means you have measured the footage yourself. | `prop DitheredMedia.scrim` |
| `blend` defaults to soft-light because that is what reads as a print halftone. overlay is harsher and multiply just dims — both stop looking like the brand. | `prop DitherOverlay.blend` |

## Reachable from outside

`style`, `className`

`style` means this module writes an inline style that beats any class you pass.

## Constraints

From `machine/components.yaml` (`patterns.dither`).

- Four things in this package put texture behind content and they are scoped differently. DitherOverlay is the raw layer; DitheredMedia is a full hero composition; HeroBackdrop is a hero strip; SectionBackdrop is the field behind a mock inside a feature row. Pick by scope, see docs/sections.md.
- soft-light is what makes the overlay read as a print halftone rather than a sticker. overlay is harsher, multiply just dims. Change deliberately.

## Rendered in

Gallery cases with a committed light and dark baseline: `pattern-dithered-media`.

## Page templates that carry it

| template | role |
| --- | --- |
| [home-page](../pages/home-page.md) | observed on 1 route |

## Rules that are not negotiable

1. **`chrome.*` is invariant and cannot invert.** `color.chrome.surface.*` and
   `color.chrome.text.*` are always dark. Use them only on a surface that never
   flips — a terminal, a log panel, a code frame. Anything on a surface that
   flips uses the theme-aware `color.text.*` / `color.surface.*`. The two share
   their dark values and diverge only in light, so the wrong pick looks correct
   until someone opens light mode.
2. **A light surface on a dark page needs the `light` class on its own root.**
   Without it the mode-aware tokens resolve to their dark values against a light
   fill. That has shipped text at 1.08:1. Check the module's `polarity` first —
   `applies-light` means the module already does this for you.
3. **Content is props.** No section hardcodes copy.

Theming is class-based and the package ships base mode `dark`. Contrast floors: body text 4.5:1, large text 3:1 (WCAG 2.2).

---

System-wide tokens, scales and the full module index: [DESIGN.md](../../DESIGN.md).
