# patterns/pill-nav-mobile-menu

<!-- @design:generated -->

_Generated by `scripts/generate-design-md.mjs` from `machine/context.yaml`, `machine/components.yaml`, `machine/compositions.yaml`._
_Do not edit. Change the source and run `npm run design`; `npm run design:check` fails the build if this file is stale._
_@skene/design-system 0.19.0._

|  |  |
| --- | --- |
| import | `@skene/design-system/patterns/pill-nav-mobile-menu` |
| polarity | `inherits` |
| boundary | `"use client"` — client component |
| intent | `navigate`, `reveal-on-demand` |

## Use for

The drawer layers behind PillNav's mobile navigation: the Menu and Close toggle, the backdrop, and the bordered link list with the actions repeated in its footer.

## Exports

`PillNavMobileMenuToggle`, `PillNavMobileMenuLayers`

## Props

| export | prop | type / allowed | required | default |
| --- | --- | --- | --- | --- |
| `PillNavMobileMenuToggle` | `isOpen` | `boolean` | **yes** |  |
| `PillNavMobileMenuToggle` | `onOpenChange` | `(open: boolean) => void` | **yes** |  |
| `PillNavMobileMenuToggle` | `panelId` | `string` | **yes** |  |
| `PillNavMobileMenuLayers` | `links` | `PillNavMobileLink[]` | **yes** |  |
| `PillNavMobileMenuLayers` | `actions` | `React.ReactNode` |  |  |
| `PillNavMobileMenuLayers` | `isOpen` | `boolean` | **yes** |  |
| `PillNavMobileMenuLayers` | `onOpenChange` | `(open: boolean) => void` | **yes** |  |
| `PillNavMobileMenuLayers` | `panelId` | `string` | **yes** |  |

## Types — not components

These are TypeScript types, not exports you can render. `<PillNavMobileLink />` is not a component; the type describes the shape of a value you pass to one of the exports above. The name usually reads like a component, which is why this heading says so.

| type | shape |
| --- | --- |
| `PillNavMobileLink` | `href`, `label`, `active` |

## Also for

Every claim cites the prop, default or export that makes it true. A claim that cannot cite one is rejected by a test.

| claim | via |
| --- | --- |
| A drawer for a nav that is not PillNav: the layers take their links and open state as props, so a different bar can reuse the same panel, backdrop and toggle. | `export PillNavMobileMenuLayers` |
| The toggle on its own, for a bar that wants the Menu and Close affordance and its aria wiring without the package's panel. | `export PillNavMobileMenuToggle` |

## Not for — reach for this instead

| instead | why |
| --- | --- |
| `PillNav` | The assembled nav. PillNav collects its links from PillNavLink children and owns the open state, so reach past it only when the bar itself is different. |
| `Sheet` | A general side panel for application UI. This drawer is marketing chrome and is sized and styled for a nav. |

## Reachable from outside

`className`, `use client`

## Composes

`same-layer/pill-nav-frosted`

## Rendered in

Gallery cases with a committed light and dark baseline: `pattern-pill-nav-mobile-menu`.

## What binds this module

- **Polarity `inherits`.** It puts no theme class on its own root, so it takes the page. Place it on a light fill and the `light` class is yours to add, or its mode-aware tokens resolve to their DARK values against that fill — measured at 1.07:1 on rendered pixels.
- **Content is props.** This pattern hardcodes no copy; the page supplies it.

These are the rules this module can break. The full set, the surface roles and
the contrast floors are in [DESIGN.md](../../DESIGN.md).

---

System-wide tokens, scales and the full module index: [DESIGN.md](../../DESIGN.md).
