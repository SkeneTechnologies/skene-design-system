# Module index

<!-- @design:generated -->

_Generated by `scripts/generate-design-md.mjs` from `machine/context.yaml`._
_Do not edit. Change the source and run `npm run design`; `npm run design:check` fails the build if this file is stale._
_@skene/design-system 0.15.0._

All 89 modules, two ways. Find the row, open that one file, stop — each module page is self-sufficient and costs about a tenth of this one.

## By intent — read this backwards

You know what you are trying to DO. The tag takes you to the candidates without reading 89 entries. The vocabulary is closed and declared at the head of `machine/context.yaml`.

| intent | means | modules |
| --- | --- | --- |
| `answer-a-question` | Copy shaped as a question and its answer, whether it is asked of the reader or by them. | [ask-widget](sections/ask-widget.md), [faq-band](sections/faq-band.md), [question-grid](sections/question-grid.md) |
| `brand-the-surface` | Putting Skene itself on the page: the mark, the wordmark, the lockup that says whose surface this is. | [pill-nav](patterns/pill-nav.md), [skene-mark](patterns/skene-mark.md), [footer](sections/footer.md) |
| `collect-input` | Taking something from the reader — a field, a control, a toggle, a form row, a prompt box. | [checkbox](ui/checkbox.md), [input-group](ui/input-group.md), [input](ui/input.md), [label](ui/label.md), [select](ui/select.md), [settings-field](ui/settings-field.md), [slider](ui/slider.md), [switch](ui/switch.md), [textarea](ui/textarea.md), [ask-widget](sections/ask-widget.md), [billing-toggle](sections/billing-toggle.md) |
| `compare-options` | Setting two or more choices against each other so one of them wins — tiers, approaches, before/after, this-not-that. | [billing-toggle](sections/billing-toggle.md), [bridge](sections/bridge.md), [comparison-table](sections/comparison-table.md), [plan-card](sections/plan-card.md), [side-by-side-diff](sections/side-by-side-diff.md), [surface-tiles](sections/surface-tiles.md), [value-cards](sections/value-cards.md) |
| `display-code` | Literal machine text the reader is meant to read or copy — shell lines, config, diffs, an identifier inside a sentence. | [terminal](patterns/terminal.md), [code](sections/code.md), [mcp-block](sections/mcp-block.md), [pr-review](sections/pr-review.md), [side-by-side-diff](sections/side-by-side-diff.md), [terminal-block](sections/terminal-block.md) |
| `drive-an-action` | The thing the reader is supposed to do next — a button, a closing CTA band, a copyable install line, a proposal. | [button](ui/button.md), [final-cta](sections/final-cta.md), [recommendation-card](sections/recommendation-card.md), [terminal-block](sections/terminal-block.md) |
| `explain-a-process` | Ordered stages and what happens between them — steps, funnels, lifecycles, a journey with a shape. | [marketing](patterns/marketing.md), [annotated-curve](sections/annotated-curve.md), [flow-diagram](sections/flow-diagram.md), [journey-signal-scene](sections/journey-signal-scene.md), [journey-track](sections/journey-track.md), [lifecycle-canvas](sections/lifecycle-canvas.md), [pipeline-stepper](sections/pipeline-stepper.md) |
| `frame-a-mock` | The chrome around a product artifact so it reads as a tool: window borders, title bars, the field it floats on. | [terminal](patterns/terminal.md), [artifact-shell](sections/artifact-shell.md), [product-window](sections/product-window.md), [section-backdrop](sections/section-backdrop.md), [traffic-lights](sections/traffic-lights.md) |
| `group-related-items` | A container or grid holding N peers of the same kind, where the set is the point. | [card](ui/card.md), [card-animation-integrations](sections/card-animation-integrations.md), [check-list](sections/check-list.md), [integrations-highlight](sections/integrations-highlight.md), [lifecycle-canvas](sections/lifecycle-canvas.md), [logo-row](sections/logo-row.md), [mcp-block](sections/mcp-block.md), [overview-tiles](sections/overview-tiles.md), [plan-card](sections/plan-card.md), [question-grid](sections/question-grid.md), [surface-cards](sections/surface-cards.md), [team-card](sections/team-card.md) |
| `interrupt-the-reader` | Something that takes over or demands an answer before the reader continues — modal, confirm, toast. | [alert-dialog](ui/alert-dialog.md), [dialog](ui/dialog.md), [sonner](ui/sonner.md) |
| `mark-a-label` | A small piece of type that classifies what it sits beside: chip, badge, eyebrow, glyph disc, inline code. | [badge](ui/badge.md), [label](ui/label.md), [marketing](patterns/marketing.md), [chip](sections/chip.md), [code](sections/code.md), [glyph-badge](sections/glyph-badge.md), [stat-chip](sections/stat-chip.md) |
| `navigate` | Moving the reader somewhere else — top nav, footer links, breadcrumbs, menus, tabbed views, a command palette. | [breadcrumb](ui/breadcrumb.md), [command](ui/command.md), [dropdown-menu](ui/dropdown-menu.md), [navigation-menu](ui/navigation-menu.md), [sheet](ui/sheet.md), [tabs](ui/tabs.md), [pill-nav-frosted](patterns/pill-nav-frosted.md), [pill-nav-mobile-menu](patterns/pill-nav-mobile-menu.md), [pill-nav](patterns/pill-nav.md), [footer](sections/footer.md) |
| `open-a-page` | The band that starts a page: hero strip, full-bleed media composition, the display heading over the fold. | [dither](patterns/dither.md), [hero-backdrop](patterns/hero-backdrop.md), [marketing](patterns/marketing.md) |
| `pitch-a-capability` | A whole marketing band arguing that Skene does something: promise, proof and a visual beside it. | [card-animation-integrations](sections/card-animation-integrations.md), [feature-row](sections/feature-row.md), [integrations-highlight](sections/integrations-highlight.md), [journey-signal-scene](sections/journey-signal-scene.md), [light-section-card](sections/light-section-card.md), [surface-cards](sections/surface-cards.md), [surface-tiles](sections/surface-tiles.md), [value-cards](sections/value-cards.md) |
| `prove-a-claim` | Evidence beside an assertion — checkable facts, logos, a ruled proof list, a verdict with its reasoning. | [agent-callout](sections/agent-callout.md), [annotated-curve](sections/annotated-curve.md), [check-list](sections/check-list.md), [feature-row](sections/feature-row.md), [light-section-card](sections/light-section-card.md), [logo-row](sections/logo-row.md), [recommendation-card](sections/recommendation-card.md), [stat-chip](sections/stat-chip.md), [team-card](sections/team-card.md), [trust-panel](sections/trust-panel.md) |
| `reveal-on-demand` | Content that is present but hidden until the reader asks: disclosure, popover, drawer, tooltip, tabbed panel. | [accordion](ui/accordion.md), [collapsible](ui/collapsible.md), [command](ui/command.md), [dropdown-menu](ui/dropdown-menu.md), [hover-card](ui/hover-card.md), [navigation-menu](ui/navigation-menu.md), [popover](ui/popover.md), [sheet](ui/sheet.md), [tabs](ui/tabs.md), [tooltip](ui/tooltip.md), [pill-nav-mobile-menu](patterns/pill-nav-mobile-menu.md), [faq-band](sections/faq-band.md) |
| `show-a-metric` | A number and what it is made of — a headline figure, a score, a rate, a delta, a bar. | [progress](ui/progress.md), [evaluator-check](sections/evaluator-check.md), [finding-card](sections/finding-card.md), [funnel](sections/funnel.md), [overview-tiles](sections/overview-tiles.md), [score-ring](sections/score-ring.md) |
| `show-status` | The state a thing is in right now: ok/warn/broken, live/pending, running/done, loading. | [alert](ui/alert.md), [badge](ui/badge.md), [progress](ui/progress.md), [skeleton](ui/skeleton.md), [sonner](ui/sonner.md), [chip](sections/chip.md), [discovery-table](sections/discovery-table.md), [evaluator-list](sections/evaluator-list.md), [evaluator-panel](sections/evaluator-panel.md), [evaluator-verify](sections/evaluator-verify.md), [finding-card](sections/finding-card.md), [funnel](sections/funnel.md), [integration-rows](sections/integration-rows.md), [journey-track](sections/journey-track.md), [pipeline-stepper](sections/pipeline-stepper.md), [pr-review](sections/pr-review.md) |
| `tabulate-data` | Rows and columns of real data — keys and values, findings, records, a comparison matrix. | [table](ui/table.md), [comparison-table](sections/comparison-table.md), [discovery-table](sections/discovery-table.md), [evaluator-check](sections/evaluator-check.md), [evaluator-list](sections/evaluator-list.md), [evaluator-panel](sections/evaluator-panel.md), [evaluator-verify](sections/evaluator-verify.md), [integration-rows](sections/integration-rows.md), [key-value-table](sections/key-value-table.md) |
| `texture-a-surface` | Painting the Skene ground under other content — halftone, dither, the site textures, a frosted wash. | [dither](patterns/dither.md), [hero-backdrop](patterns/hero-backdrop.md), [pill-nav-frosted](patterns/pill-nav-frosted.md), [final-cta](sections/final-cta.md), [section-backdrop](sections/section-backdrop.md) |

## By namespace

### ui/ — 30 modules

| module | use for |
| --- | --- |
| [ui/accordion](ui/accordion.md) | Radix disclosure list. One trigger, one panel, animated open and close. |
| [ui/alert-dialog](ui/alert-dialog.md) | Radix modal that demands an answer before anything continues. |
| [ui/alert](ui/alert.md) | A static inline notice inside a page — not a toast, not a modal. |
| [ui/badge](ui/badge.md) | The product surface's status marker: a small rounded label with four variants. |
| [ui/breadcrumb](ui/breadcrumb.md) | The path back up a product hierarchy. |
| [ui/button](ui/button.md) | The action primitive. Six variants, four sizes. |
| [ui/card](ui/card.md) | The generic product container: header, content, footer. `variant="surface"` is the flat flavour of the same card — `border-surface-border` over `bg-surface-1`, 24px of its own padding — which appeared inline eight times across skene-site's routes plus six near-copies that differed only by being an `<a>`. SCOPE NARROWED IN 0.9.19: `surface` is no longer the marketing grid cell. `render_marketing_cards_as_feature_row` routes those to `FeatureRow`, whose copy-only path renders a content-height row so a 2-up grid does not gain a 600px floor per cell. On product surfaces this component is unchanged and remains the default container. |
| [ui/checkbox](ui/checkbox.md) | Radix checkbox with the package's focus ring. |
| [ui/collapsible](ui/collapsible.md) | Radix show/hide for one region. |
| [ui/command](ui/command.md) | cmdk palette: a searchable list with groups and shortcuts. |
| [ui/dialog](ui/dialog.md) | Radix modal for a task the reader may abandon. |
| [ui/dropdown-menu](ui/dropdown-menu.md) | Radix menu hung off a trigger. |
| [ui/hover-card](ui/hover-card.md) | Preview on hover, no click. |
| [ui/input-group](ui/input-group.md) | An input with addons welded to it — prefix, suffix, inline button. |
| [ui/input](ui/input.md) | The single-line text field. |
| [ui/label](ui/label.md) | The form label, wired to Radix's accessible name. |
| [ui/navigation-menu](ui/navigation-menu.md) | Radix top navigation with dropdown panels. |
| [ui/popover](ui/popover.md) | Radix floating panel anchored to a trigger. |
| [ui/progress](ui/progress.md) | A determinate bar with the brand gradient indicator. |
| [ui/select](ui/select.md) | Radix select with the package's two sizes. |
| [ui/settings-field](ui/settings-field.md) | The workspace form row: label, control, description, in one component. |
| [ui/sheet](ui/sheet.md) | A side panel that slides in from an edge. |
| [ui/skeleton](ui/skeleton.md) | A loading placeholder block. |
| [ui/slider](ui/slider.md) | Radix range control. |
| [ui/sonner](ui/sonner.md) | The toast host. Mount once, near the root. |
| [ui/switch](ui/switch.md) | The instant on/off control. |
| [ui/table](ui/table.md) | The semantic product table. |
| [ui/tabs](ui/tabs.md) | Mutually exclusive views, as a segmented control. |
| [ui/textarea](ui/textarea.md) | The multi-line field. |
| [ui/tooltip](ui/tooltip.md) | A one-line label on hover or focus. |

### patterns/ — 8 modules

| module | use for |
| --- | --- |
| [patterns/dither](patterns/dither.md) | The Skene surface treatment: a halftone texture blended over photographic or video media, and the full hero composition built from it. DitherOverlay is the raw layer; DitheredMedia is media, dither, scrim, gradient and content in one. |
| [patterns/hero-backdrop](patterns/hero-backdrop.md) | A dark hero strip: a background image at low opacity, a gradient fading to the page ground, and content above it. |
| [patterns/marketing](patterns/marketing.md) | The page furniture the marketing surface repeats: the nav bar over the hero, the display heading and its peach accent, the eyebrow kicker, the numbered step, and the split auth layout. |
| [patterns/pill-nav-frosted](patterns/pill-nav-frosted.md) | The two constants PillNav and its drawer share: the frosted wash that keeps links legible over arbitrary photography, and the absolute and sticky position classes. |
| [patterns/pill-nav-mobile-menu](patterns/pill-nav-mobile-menu.md) | The drawer layers behind PillNav's mobile navigation: the Menu and Close toggle, the backdrop, and the bordered link list with the actions repeated in its footer. |
| [patterns/pill-nav](patterns/pill-nav.md) | The marketing site's top navigation: a frosted pill floating over hero media, with a full-screen drawer below md so a phone gets the links rather than the CTA alone. |
| [patterns/skene-mark](patterns/skene-mark.md) | The brand artwork as elements: the square symbol, and the horizontal lockup with the wordmark. |
| [patterns/terminal](patterns/terminal.md) | A terminal frame: traffic lights, a title, and monospace lines with an optional prompt. |

### sections/ — 51 modules

| module | use for |
| --- | --- |
| [sections/agent-callout](sections/agent-callout.md) | The verdict block: an avatar, a claim in bold, and the evidence under it, in a peach-bordered frame on its own tinted ground. It is the moment Skene speaks on the page. |
| [sections/annotated-curve](sections/annotated-curve.md) | A narrative plotting frame: a Catmull-Rom spline drawn through hand-authored 0-100 percentage points, with HTML callout boxes pinned to the points that carry labels. The split is the whole design — the SVG is `aria-hidden` decoration and the callouts are an ordered list of real HTML, so the copy wraps, selects, finds, and is read in array order. It is for making an argument that has a *shape* (a rise, a dip, a recovery) where the copy is the payload and the curve is the frame the copy hangs on, not for showing measured values. |
| [sections/artifact-shell](sections/artifact-shell.md) | The shell every drawn Skene Cloud artifact is built from: the textured field, the light app window, its panels, the status pill and the data table. |
| [sections/ask-widget](sections/ask-widget.md) | The "ask us anything" prompt block: an attributed question rendered as the loud heading, a quiet borderless textarea that the heading labels via `aria-labelledby`, an AI-disclosure badge, and a small submit sized to its label in the bottom-right. It is the package's one genuinely interactive section — a controlled textarea plus a submit handler — and it lives in its own module specifically so `'use client'` does not spread to server-renderable siblings. The restraint is the design: it is a low-commitment probe, so a full-bleed primary button and a boxed input would make it read as a form rather than as a question. |
| [sections/billing-toggle](sections/billing-toggle.md) | The monthly/yearly switch, and the only `'use client'` file in the pricing section. It exists as its own module for a structural reason rather than a visual one: putting the directive in `plan-card.tsx` would drag the whole pure-markup card across the client boundary, so the interactivity was quarantined here (sections.md rule 4). It is fully controlled — `yearly` in, `onChange` out — and makes both word labels clickable, not just the 40px track, because the words are the obvious target. |
| [sections/bridge](sections/bridge.md) | A whole cream band (a `<section>`, not a card) that lays N `BridgeNode` cards in a row, interleaves arrows between them, and inverts one of them to dark and lifts it. It exists to argue POSITION — that the featured thing sits between the others — and the tone difference is the argument, not styling: three cards in one tone read as three peers and the band says nothing. Structurally it is also the package's one doubly-nested inversion (`light` band, `dark` card inside it), so it doubles as the worked example of how the two subtree switches nest. |
| [sections/card-animation-integrations](sections/card-animation-integrations.md) | Four integration cards on a textured field, cycling detail copy with GSAP: MCP server, GitHub App, Cloud API, and repo audit. Ported from skene-marketing-website's CardAnimationIntegrations. |
| [sections/check-list](sections/check-list.md) | The rule-separated feature list with a peach check — the single highest-frequency element on a Skene marketing page, previously hand-written at every call site. `CheckList` is a bare `<ul>` that sets one CSS variable (`--check-rule`); `CheckItem` is an `<li>` whose separator is a *top border on each row* rather than a divider element, so a list of one still reads as part of its card instead of floating. The check is a real `aria-hidden` `<span>`, not a `::before`, precisely so a screen reader does not announce "tick" before every line. |
| [sections/chip](sections/chip.md) | The 10px monospace uppercase rectangle, extracted from two copies that were written independently — `PlanCard`'s inline tier span and `ProductWindow`'s `WindowStatus` — which had already drifted apart on tracking before anyone noticed. Its job is to hold the geometry still: `tone` carries colour and nothing else, with tracking as the single declared exception. It exists as much to stop a third inline copy being written as to render the two that exist. |
| [sections/code](sections/code.md) | The inline identifier chip inside a sentence: an event name, a column, a flag, a path. Extracted from SEVEN copies — six byte-identical component declarations across skene-site route files, verified by comparing the emitted class string, plus a seventh spelling as `PROSE_CODE`, the same recipe as a descendant selector for prose the author cannot reach element by element. The oldest open entry on that repository's gap list, on a site where an event name appears in nearly every paragraph of body copy. |
| [sections/comparison-table](sections/comparison-table.md) | A real semantic `<table>` for capability comparison — one row per claim, one column per approach, with your column picked out. The featured column is washed in the head *and* every body cell so it reads as one continuous object top to bottom, and the header additionally turns peach so its identity is stated once in words rather than only in colour. The whole component is really the pairing of `min-width: 820px` on the table with `overflow-x: auto` + `tabIndex={0}` on the wrapper: a comparison that reflows into stacked cards stops being a comparison, so on narrow screens it keeps its shape and scrolls sideways instead. |
| [sections/discovery-table](sections/discovery-table.md) | The discovery artifact: the events Skene found, where it found them, and whether each one verified. |
| [sections/evaluator-check](sections/evaluator-check.md) | The evaluator's Check tab: a metric, the formula under it, one row per operand, and the results it produced. |
| [sections/evaluator-list](sections/evaluator-list.md) | The evaluator index: every evaluation in a workspace, and whether the signals it depends on were found. |
| [sections/evaluator-panel](sections/evaluator-panel.md) | The two-pane evaluator: the index on one side, one evaluation's requirements on the other. |
| [sections/evaluator-verify](sections/evaluator-verify.md) | The requirements an evaluation depends on: one row per event, its verdict, and the fields it must carry. |
| [sections/faq-band](sections/faq-band.md) | The FAQ band: a heading column that stays put, and the questions beside it on a cream ground with a hairline per row and a round toggle. |
| [sections/feature-row](sections/feature-row.md) | A whole dark band of a page: a copy column (corner marker, ringed icon, title, italic lede, proof list, pinned actions) beside a visual panel, mirrored per row by `reverse`. The shape is an argument, not a layout — `title` + `lede` are the promise and `children` (usually a `CheckList`) is the proof, which is why the lede is its own slot rather than the first paragraph of the body. Ships with `FeatureStack` (the `grid gap-6` rhythm wrapper) and `FeatureIcon` (a ringed accent glyph in peach/violet/blue) so a three-row feature section is three prop sets rather than a re-derived grid. |
| [sections/final-cta](sections/final-cta.md) | The closing band: full-bleed, centred, one large heading, an optional lede and an actions row, over a textured dark backdrop. It appears twice on the live homepage and once per subpage, and every instance had previously been rebuilt by hand. The default backdrop (`pixel-bg.webp`, 143 KB) ships with the package and is resolved from the module itself, on the principle that the default should produce the section the site actually has rather than a stripped-down gradient the caller has to re-dress. |
| [sections/finding-card](sections/finding-card.md) | Three small pieces that together fill a product window with Skene's actual argument: `Finding` (a status-tagged row — tag, title, consequence), `MetricCard` (the headline number with a coloured delta), and `Sparkline` (an authored bar shape). The point of the set is the contradiction — a metric that looks fine above a list of steps that are unmeasured or renamed — so the status vocabulary is fixed at `good \| warn \| danger` bound to `semantic.matcha / warningAmber / errorRed`, the same three the dashboard uses. All content is props; nothing here knows what a Skene finding says. |
| [sections/flow-diagram](sections/flow-diagram.md) | Nodes in a row joined by arrows, with a value and a note on each edge. |
| [sections/footer](sections/footer.md) | The site footer as a set of composable parts — `SiteFooter` (brand column, link columns, bottom bar), `FooterColumn`, `FooterLink`, `SocialLinks`, `SocialLink`. The part actually worth reusing is the oversized wordmark: 29vw at 1.7% opacity, clipped by the footer's own `overflow-hidden`, so it reads as texture at a glance and only resolves into a word if you look. Rendering it at a "sensible" size or opacity is the obvious change and it is wrong every time. |
| [sections/funnel](sections/funnel.md) | The activation funnel as the reader's own dashboard would draw it: one bar per step, and three states because a broken number is not a low number. |
| [sections/glyph-badge](sections/glyph-badge.md) | The round disc a glyph sits in at the head of a row: a circle, a hairline, a peach mark centred in it, `aria-hidden` always. It is the disc `TrustFact` draws, lifted out so it is reachable without the `<li>`, the separating rule and the qualifier slot that come with a fact row. `tone` picks the ground — `tint` is the cream-panel pair (`chrome.line.onLight` over a 12% peach wash), `muted` is the themed pair that follows the page — and `size` is one number, written to `style` so the glyph inside keeps whatever type size it inherits. |
| [sections/integration-rows](sections/integration-rows.md) | The integrations artifact: what Skene is attached to, and whether each connection is live. |
| [sections/integrations-highlight](sections/integrations-highlight.md) | The homepage integrations band: cream copy column plus the four-card GSAP animation. Ported from skene-marketing-website's IntegrationsHighlight. |
| [sections/journey-signal-scene](sections/journey-signal-scene.md) | Evidence, a traced journey step, and the PR review that catches it breaking, in one animated composition with a GTM/Engineering toggle. No props — content lives in named consts near the top of the source file, edited directly rather than passed in. Measures its own container width and switches between three hand-placed layouts (a three-panel row, a hero-column-width layout, a stacked phone layout) rather than using CSS breakpoints, because the same panel widths read differently depending on which parent gives it space. |
| [sections/journey-track](sections/journey-track.md) | The numbered steps of a funnel on one line, where the connector between two steps is a gradient running from one step's status colour to the next's — the seam carries the state, not just the nodes. That is the section's argument, not a flourish: breakage happens *in the link* (an event that stops firing between checkout and confirmation), and the eye should find it before reading a label. The module also exports `MiniFunnel`, the small authored label/value/bar readout that usually sits beside the track. |
| [sections/key-value-table](sections/key-value-table.md) | The reference table: keys and values at two densities, with a masked value, a note and the 11px tag chip. |
| [sections/lifecycle-canvas](sections/lifecycle-canvas.md) | The lifecycle artifact: stages across the canvas, each holding milestones and the signals bound to them. |
| [sections/light-section-card](sections/light-section-card.md) | The inverted band: one cream card carrying a whole section's worth of copy — heading, italic promise, a ruled proof block, actions — with an optional visual panel opposite. It is `FeatureRow` at the other polarity, and its `light` root is a load-bearing accessibility mechanism, which is why the two were deliberately NOT collapsed behind a `tone` flag. SCOPE NARROWED IN 0.9.19: this is no longer a marketing section band. `render_marketing_cards_as_feature_row` gives that role to `FeatureRow` alone, because a page that alternates one repeating object cannot also alternate polarity without the cream band reading as a second system. What remains is every non-marketing use — product surfaces, gallery cases, and any dark-document context needing a correctly-inverted cream subtree, which is the part no other component does. |
| [sections/logo-row](sections/logo-row.md) | The proof strip: a centred heading, a stat line, a row of outlined logo slots, and the caption that explains them. The slots are EMPTY by default and that is the component's argument, not a missing feature — the strip asserts that named proof only appears with consent, so a `LogoSlot` renders a child only when given one and no fabricated customer mark ever goes in a slot. Every colour is a mode-aware role, so the strip survives both the dark page and a cream `light` ancestor with no `onLight` prop. |
| [sections/mcp-block](sections/mcp-block.md) | A panel inside the MCP artifact: a config snippet to paste, or the list of tools the server answers with. |
| [sections/overview-tiles](sections/overview-tiles.md) | The workspace overview: a row of small tiles, each a caption, one number and what the number is made of. |
| [sections/pipeline-stepper](sections/pipeline-stepper.md) | A labelled progress track: N steps with state rings, joined by connectors the STEPPER owns and fills from the state of the step on their left, above an optional title/subtitle. It was built for a long job running inside a light `ProductWindow`, and two decisions carry that job — connectors are derived rather than drawn per-step, so N dots read as one track filling left to right; and emphasis runs backwards from instinct, with the ACTIVE step taking the label weight and a peach halo while done steps recede to matcha, because a running job's question is "what is it doing now", not "what has it finished". |
| [sections/plan-card](sections/plan-card.md) | A pricing tier card plus the grid that holds a row of them (`PlanGrid` + `PlanCard`). It is pure markup — no state, no client directive — because the only interactive part of a pricing section, the billing switch, was deliberately split into `billing-toggle.tsx` so this file stays server-renderable. The shape's real argument is comparison-by-inversion: `featured` flips one card to cream-on-dark, lifts it `md:-translate-y-3`, and drops a `--shadow-modal` on it, so a row of peers stops reading as peers and one of them becomes the recommendation. |
| [sections/pr-review](sections/pr-review.md) | The pull-request artifact: Skene's review on a PR, with findings by severity, a suggested fix and the file it lands in. |
| [sections/product-window](sections/product-window.md) | The chrome that frames a product mock inside a marketing section: rounded border, modal shadow, and an optional title bar with a left title and a right `status` slot. Its default `tone="light"` is the argument rather than a style choice — the page around it is Skene's dark chrome, so the thing inside the frame reads as the customer's own tool; a dark window on a dark page reads as a screenshot of Skene itself. Ships alongside `WindowStatus` (the title bar's chip), `WindowToolbar` (a filter/breadcrumb strip) and `WindowChip`. |
| [sections/question-grid](sections/question-grid.md) | A 1–4 column grid of near-empty cards, each one a mono category `tag` sitting at the top of the card with a question (or claim) below it and an optional answer. The card is deliberately a border, a 14px radius and a peach corner glow with no opaque fill — that is what lets it sit on a dark band or a cream one without needing a polarity class or a token audit. It exists for the "here is what you'll actually be able to answer" band, where the copy does all the work and the container must not compete with it. |
| [sections/recommendation-card](sections/recommendation-card.md) | The proposal: what to do next, and what makes it worth doing. Deliberately without a status colour. |
| [sections/score-ring](sections/score-ring.md) | A score against a scale: a partial arc on a track with the value inside it. |
| [sections/section-backdrop](sections/section-backdrop.md) | The halftone field a panel floats on: a container that paints one of the three real site textures (`card1/2/3_bg.webp`) and insets its child by a percentage of the track width. The percentage is the whole design — the live panel is 84–92% of its container, and only a percentage holds that proportion at every width; a fixed padding turns the field into a coloured border on a short wide card. It is the narrowest of the package's four texture tools: field behind a mock inside a feature row, as opposed to `HeroBackdrop` (hero strip), `DitheredMedia` (whole hero composition) and `DitherOverlay` (the raw layer). |
| [sections/side-by-side-diff](sections/side-by-side-diff.md) | Two columns of code with added and removed lines marked — what was written beside what was expected. |
| [sections/stat-chip](sections/stat-chip.md) | Two small pills that sit beside headings: `StatChip` asserts something true right now ("121 stars"), `MetaChip` marks something not shipped yet (prose, a hairline divider, then a peach status word). They are two components rather than one with a `variant` precisely so a forgotten prop can never turn a roadmap item into a live claim about the product — two names cannot be defaulted into each other. Both derive their border and fill from `currentColor` via `color-mix`, which is what lets one chip survive both the dark page and the cream `LightSectionCard` with no `onLight` prop. |
| [sections/surface-cards](sections/surface-cards.md) | A compact grid of peer cards on a textured field, one of them drawn cream, where every card carries its own detail: the “ways in” band, four surfaces two across. |
| [sections/surface-tiles](sections/surface-tiles.md) | The surfaces a product runs on, as a row of tiles with one picked out, and the panel that explains the chosen one. |
| [sections/team-card](sections/team-card.md) | The about-page person card and its grid: a name as a heading, a role in the small-caps monospace register, an optional bio/links block, and an optional square media frame that only exists when media does. The card is designed for the no-photo state — type on a bordered panel that looks finished — so a team page needs no photographer and no stock placeholder. Cards are `<li>`s and `TeamGrid` is the `<ul>`, the same peers-in-a-list argument `TrustPanel` makes for its facts. |
| [sections/terminal-block](sections/terminal-block.md) | The install block: a terminal frame holding one or more shell lines, each with its own copy button. |
| [sections/traffic-lights](sections/traffic-lights.md) | The three window-chrome dots — red, yellow, green, 10px each — as one `aria-hidden` group at an 8px gap. It exists so a shell bar, an editor header or a GitHub file header can carry window furniture without hand-rolling three spans, which is what five files were doing: `TerminalBlock` and `PrReview` in this package, and two skene-site routes that each declared a local copy plus a comment saying the fix belonged upstream. |
| [sections/trust-panel](sections/trust-panel.md) | A cream panel split into a claim and its evidence: heading, lede and a row of links on the left, a stack of `TrustFact` items on a deeper cream to the right. The split is the argument — the reassurance is not a paragraph asserting itself but a paragraph standing next to checkable facts, which is why the evidence track is the wider of the two (`0.9fr 1.1fr`). It is a fixed-polarity object: the root carries `light` unconditionally, so it is a cream card on a dark page, with a peach bloom rising from the bottom-left corner that the token system gets right on cream for free. |
| [sections/value-cards](sections/value-cards.md) | A responsive card band whose argument is carried by tone rather than by copy: N cards stating what the current situation costs, then one stating what it becomes. `ValueCard` takes a mono `label`, a `title` line and an optional body, and the `tone` union (`'cost' \| 'gain'`) is the only thing that moves — `cost` borrows `semantic.errorRed` for the label and a neutral 14% hairline, `gain` takes `brand.peach` with a 34% border and a left-to-right directional wash so the last card reads as an arrival rather than one more tile. It exists so a band can make a turn; a row of identically-weighted cards degrades into a feature list and says nothing. |

## Rules that are not negotiable

1. **`chrome.*` is invariant and cannot invert.** `color.chrome.surface.*` and
   `color.chrome.text.*` are always dark. Use them only on a surface that never
   flips — a terminal, a log panel, a code frame. Anything on a surface that
   flips uses the theme-aware `color.text.*` / `color.surface.*`. The two share
   their dark values and diverge only in light, so the wrong pick looks correct
   until someone opens light mode.
2. **A light surface on a dark page needs the `light` class on its own root.**
   Without it the mode-aware tokens resolve to their dark values against a light
   fill. That has shipped text at 1.08:1. Check the module's `polarity` first —
   `applies-light` means the module already does this for you.
3. **Content is props.** No section hardcodes copy.

Theming is class-based and the package ships base mode `dark`. Contrast floors: body text 4.5:1, large text 3:1 (WCAG 2.2).

---

Rules, scales, contrast floors and the page templates: [DESIGN.md](../DESIGN.md).
