# sections/finding-card

<!-- @design:generated -->

_Generated by `scripts/generate-design-md.mjs` from `machine/context.yaml`, `machine/components.yaml`, `machine/compositions.yaml`._
_Do not edit. Change the source and run `npm run design`; `npm run design:check` fails the build if this file is stale._
_@skene/design-system 0.24.0._

|  |  |
| --- | --- |
| import | `@skene/design-system/sections/finding-card` |
| polarity | `inherits` |
| boundary | server-renderable |
| intent | `show-status`, `show-a-metric` |

## Use for

Three small pieces that together fill a product window with Skene's actual argument: `Finding` (a status-tagged row — tag, title, consequence), `MetricCard` (the headline number with a coloured delta), and `Sparkline` (an authored bar shape). The point of the set is the contradiction — a metric that looks fine above a list of steps that are unmeasured or renamed — so the status vocabulary is fixed at `good | warn | danger` bound to `semantic.matcha / warningAmber / errorRed`, the same three the dashboard uses. All content is props; nothing here knows what a Skene finding says.

## Exports

`Finding`, `MetricCard`, `Sparkline`

## Props

| export | prop | type / allowed | required | default |
| --- | --- | --- | --- | --- |
| `Finding` | `status` | `FindingStatus` | **yes** |  |
| `Finding` | `tag` | `React.ReactNode` | **yes** |  |
| `Finding` | `title` | `React.ReactNode` | **yes** |  |
| `Finding` | `note` | `React.ReactNode` |  |  |
| `Finding` | `onLight` | `boolean` |  | `true` |
| `MetricCard` | `label` | `React.ReactNode` | **yes** |  |
| `MetricCard` | `value` | `React.ReactNode` | **yes** |  |
| `MetricCard` | `delta` | `React.ReactNode` |  |  |
| `MetricCard` | `trend` | `FindingStatus` |  | `'danger'` |
| `MetricCard` | `children` | `React.ReactNode` |  |  |
| `Sparkline` | `bars` | `number[]` | **yes** |  |
| `Sparkline` | `highlight` | `number` |  |  |

## Also for

Every claim cites the prop, default or export that makes it true. A claim that cannot cite one is rejected by a test.

| claim | via |
| --- | --- |
| Any KPI tile: `MetricCard` takes `label`, `value`, `delta` and `trend` as ReactNodes and has no light/dark flag at all — it inherits colour from its parent — and `children` is an open slot under the number for whatever the caller wants there. | `export MetricCard` |
| Status rows on the dark page: `Finding onLight={false}` switches the card to `chrome.line.subtle` / `chrome.surface.2` / `chrome.text.primary`, which is what you want directly on the page ground or inside a `tone="dark"` ProductWindow. | `prop Finding.onLight` |
| Labelled list rows generally — `tag` is a ReactNode, not a number, so "GAP", "01", "P2" or "PII" all work; the same row shape serves audit results, a check-run summary, or a release-note list. | `prop Finding.tag` |
| A compact one-line variant: `note` is optional, and with it omitted the grid collapses to tag plus title with no second row. | `prop Finding.note` |
| `Sparkline` on its own: it is a pure `bars: number[]` (clamped 0–100) plus a `highlight` index with no dependency on `MetricCard`, and `className` merges last (twMerge) so its `h-[74px]` and `mt-6` defaults are adjustable in a table cell or a card footer. | `export Sparkline` |

## Not for — reach for this instead

| instead | why |
| --- | --- |
| `Sparkline` | Real data visualization. `Sparkline` has no scale, axes, tooltips or empty state, and its values are authored — reach for an actual charting component when the numbers are measured. |
| `Eyebrow` | Product-surface status inside the app. These are marketing components with hardcoded typographic scale and no loading/empty states; `ui/badge` is the shadcn status primitive for product surfaces (and `Eyebrow` is the section kicker — the three are not interchangeable). |

## Watch for

| note | via |
| --- | --- |
| `Finding`'s tag column is a hard `grid-cols-[68px_1fr]`. The tag does not widen the column — a longer string wraps or overflows inside 68px, and the type is 9px mono uppercase sized for roughly "STEP 3". | `export Finding` |
| `onLight` defaults to **true**, the opposite of most flags in this package, because the default frame is a light `ProductWindow`. Put a `Finding` on the dark page without `onLight={false}` and you get a white card. Note also that the light case is `bg-white`, not the cream `brand.light` of its parent — deliberate, so the card floats on the window's fill. | `default onLight=true` |
| `Sparkline` is `aria-hidden` and is explicitly not a chart: no axes, no scale, and `highlight` is an index because the copy decides which bar matters, not the data. Anything a reader must actually read has to live in `MetricCard`'s `value` or `delta`. | `export Sparkline` |
| `MetricCard`'s `trend` defaults to `'danger'`, so an unspecified delta renders red; and its numeral is intentionally `font-normal` at 2.6rem with tight tracking, because bolding it makes the page read like a dashboard instead of an argument about one. | `export MetricCard` |

## Reachable from outside

`style`, `className`

`style` means this module writes an inline style that beats any class you pass.

## Composes

`lib/status`

## Constraints

From `machine/components.yaml` (`sections.Finding`).

| prop | allowed |
| --- | --- |
| `status` | `good`, `warn`, `danger` |
| `onLight` | boolean — true (default) means inside a light ProductWindow |

- status binds to semantic.matcha / warningAmber / errorRed, the same three the dashboard uses. Do not introduce a marketing-only green or red; that disagreement is what the token reconciliation removed.
- Sparkline is not a chart. No axes, no scale, values authored rather than measured. `highlight` is an index because the copy decides which bar matters, not the data.

## Rendered in

Gallery cases with a committed light and dark baseline: `section-finding-card`.

## Page templates that carry it

| template | role |
| --- | --- |
| [use-case-page](../pages/use-case-page.md) | optional, in 1 of 3 |
| [developer-page](../pages/developer-page.md) | optional, in 1 of 2 |
| [comparison-page](../pages/comparison-page.md) | optional, in 1 of 2 |
| [company-page](../pages/company-page.md) | optional, in 1 of 2 |
| [pricing-page](../pages/pricing-page.md) | observed on 1 route |

## What binds this module

- **Polarity `inherits`.** It puts no theme class on its own root, so it takes the page. Place it on a light fill and the `light` class is yours to add, or its mode-aware tokens resolve to their DARK values against that fill — measured at 1.07:1 on rendered pixels.
- **`chrome.*` is invariant.** This module is in the fixed-dark world, so the distinction bites here: `chrome.*` never inverts and belongs only on a surface that never flips. Anything on a surface that follows the theme uses `text.*` / `surface.*`. The two share their dark values, so the wrong pick looks correct until someone opens light mode.
- **Content is props.** This section hardcodes no copy; the page supplies it.

These are the rules this module can break. The full set, the surface roles and
the contrast floors are in [DESIGN.md](../../DESIGN.md).

---

System-wide tokens, scales and the full module index: [DESIGN.md](../../DESIGN.md).
