# sections/integration-rows

<!-- @design:generated -->

_Generated by `scripts/generate-design-md.mjs` from `machine/context.yaml`, `machine/components.yaml`, `machine/compositions.yaml`._
_Do not edit. Change the source and run `npm run design`; `npm run design:check` fails the build if this file is stale._
_@skene/design-system 0.25.0._

|  |  |
| --- | --- |
| import | `@skene/design-system/sections/integration-rows` |
| polarity | `inherits` |
| boundary | server-renderable |
| intent | `show-status`, `tabulate-data` |

## Use for

The integrations artifact: what Skene is attached to, and whether each connection is live.

## Exports

`IntegrationRows`

## Props

| export | prop | type / allowed | required | default |
| --- | --- | --- | --- | --- |
| `IntegrationRows` | `rows` | `Integration[]` | **yes** |  |
| `IntegrationRows` | `title` | `React.ReactNode` |  |  |
| `IntegrationRows` | `source` | `React.ReactNode` |  |  |
| `IntegrationRows` | `separator` | `React.ReactNode` |  | `'/'` |
| `IntegrationRows` | `summary` | `React.ReactNode` |  |  |
| `IntegrationRows` | `summaryStatus` | `StatPillStatus` |  | `'ok'` |
| `IntegrationRows` | `actions` | `React.ReactNode` |  |  |

## Types — not components

These are TypeScript types, not exports you can render. `<Integration />` is not a component; the type describes the shape of a value you pass to one of the exports above. The name usually reads like a component, which is why this heading says so.

| type | shape |
| --- | --- |
| `Integration` | `id`, `name`, `note`, `status`, `statusLabel`, `aside`, `className` |

## Also for

Every claim cites the prop, default or export that makes it true. A claim that cannot cite one is rejected by a test.

| claim | via |
| --- | --- |
| Any connection or account list where the right-hand cell is a state — data sources, webhooks, installed apps. | `type Integration.status` |
| A row whose supporting line is an identifier rather than a sentence, by passing a TagChip as the note — which is why `note` is a node. | `type Integration.note` |
| An extra cell per row via `aside`, for a timestamp or a link. | `type Integration.aside` |

## Not for — reach for this instead

| instead | why |
| --- | --- |
| `DataRow` | A real column contract with headers. That is DataTable. |
| `KeyValueTable` | Key/value reference data. That is KeyValueTable. |

## Watch for

| note | via |
| --- | --- |
| It is rows, not a table, and that is load-bearing at 390px: the left cell is prose of varying length and the right is a pill whose width is its word. A table column has one width for every row. | `prop IntegrationRows.rows` |

## Reachable from outside

`className`

## Composes

`same-layer/artifact-shell`

## Rendered in

Gallery cases with a committed light and dark baseline: `section-integration-rows`.

## Page templates that carry it

| template | role |
| --- | --- |
| [product-page](../pages/product-page.md) | optional, in 2 of 4 |
| [comparison-page](../pages/comparison-page.md) | optional, in 1 of 2 |

## What binds this module

- **Polarity `inherits`.** It puts no theme class on its own root, so it takes the page. Place it on a light fill and the `light` class is yours to add, or its mode-aware tokens resolve to their DARK values against that fill — measured at 1.07:1 on rendered pixels.
- **Content is props.** This section hardcodes no copy; the page supplies it.

These are the rules this module can break. The full set, the surface roles and
the contrast floors are in [DESIGN.md](../../DESIGN.md).

---

System-wide tokens, scales and the full module index: [DESIGN.md](../../DESIGN.md).
