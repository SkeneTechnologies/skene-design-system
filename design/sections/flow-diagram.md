# sections/flow-diagram

<!-- @design:generated -->

_Generated by `scripts/generate-design-md.mjs` from `machine/context.yaml`, `machine/components.yaml`, `machine/compositions.yaml`._
_Do not edit. Change the source and run `npm run design`; `npm run design:check` fails the build if this file is stale._
_@skene/design-system 0.15.0._

|  |  |
| --- | --- |
| import | `@skene/design-system/sections/flow-diagram` |
| polarity | `inherits` |
| boundary | server-renderable |
| intent | `explain-a-process` |

## Use for

Nodes in a row joined by arrows, with a value and a note on each edge.

## Exports

`FlowDiagram`, `FlowNode`, `FlowEdge`

## Props

| export | prop | type / allowed | required | default |
| --- | --- | --- | --- | --- |
| `FlowDiagram` | `children` | `React.ReactNode` | **yes** |  |
| `FlowDiagram` | `note` | `React.ReactNode` |  |  |
| `FlowNode` | `label` | `React.ReactNode` | **yes** |  |
| `FlowNode` | `detail` | `React.ReactNode` |  |  |
| `FlowEdge` | `value` | `React.ReactNode` |  |  |
| `FlowEdge` | `meta` | `React.ReactNode` |  |  |

## Also for

Every claim cites the prop, default or export that makes it true. A claim that cannot cite one is rejected by a test.

| claim | via |
| --- | --- |
| Any pipeline or path with a measurable step-to-step drop — a funnel, a queue, a state machine. | `prop FlowEdge.value` |
| A plain sequence with unlabelled edges: `value` and `meta` are both optional. | `prop FlowEdge.meta` |

## Not for — reach for this instead

| instead | why |
| --- | --- |
| `PipelineStepper` | Progress through named stages, with done/active/pending states. That is PipelineStepper. |
| `Funnel` | Proportional bars rather than nodes. That is Funnel. |

## Watch for

| note | via |
| --- | --- |
| It scrolls horizontally rather than wrapping, so a long chain stays a chain. That is deliberate and a wrap would read as two flows. | `prop FlowDiagram.children` |

## Reachable from outside

`className`

## Rendered in

Gallery cases with a committed light and dark baseline: `section-flow-diagram`.

## Page templates that carry it

| template | role |
| --- | --- |
| [use-case-page](../pages/use-case-page.md) | optional, in 2 of 3 |

## What binds this module

- **Polarity `inherits`.** It puts no theme class on its own root, so it takes the page. Place it on a light fill and the `light` class is yours to add, or its mode-aware tokens resolve to their DARK values against that fill — measured at 1.07:1 on rendered pixels.
- **Content is props.** This section hardcodes no copy; the page supplies it.

These are the rules this module can break. The full set, the surface roles and
the contrast floors are in [DESIGN.md](../../DESIGN.md).

---

System-wide tokens, scales and the full module index: [DESIGN.md](../../DESIGN.md).
