# sections/evaluator-panel

<!-- @design:generated -->

_Generated by `scripts/generate-design-md.mjs` from `machine/context.yaml`, `machine/components.yaml`, `machine/compositions.yaml`._
_Do not edit. Change the source and run `npm run design`; `npm run design:check` fails the build if this file is stale._
_@skene/design-system 0.15.0._

|  |  |
| --- | --- |
| import | `@skene/design-system/sections/evaluator-panel` |
| polarity | `applies-dark` |
| boundary | server-renderable |
| intent | `tabulate-data`, `show-status` |

## Use for

The two-pane evaluator: the index on one side, one evaluation's requirements on the other.

## Exports

`EvaluatorPanel`

## Props

| export | prop | type / allowed | required | default |
| --- | --- | --- | --- | --- |
| `EvaluatorPanel` | `crumb` | `React.ReactNode` |  |  |
| `EvaluatorPanel` | `summary` | `{ status: StatPillStatus; label: React.ReactNode }` |  |  |
| `EvaluatorPanel` | `list` | `EvaluatorPanelList` | **yes** |  |
| `EvaluatorPanel` | `detail` | `EvaluatorPanelDetail` | **yes** |  |
| `EvaluatorPanel` | `note` | `React.ReactNode` |  |  |
| `EvaluatorPanel` | `split` | `boolean` |  | `false` |
| `EvaluatorPanel` | `activeIndex` | `number` |  | `0` |
| `EvaluatorPanel` | `frame` | `ArtFrameKind \| false` |  | `'jr'` |

## Types — not components

These are TypeScript types, not exports you can render. `<EvaluatorPanelList />` is not a component; the type describes the shape of a value you pass to one of the exports above. The name usually reads like a component, which is why this heading says so.

| type | shape |
| --- | --- |
| `EvaluatorPanelList` | `columns`, `evaluations` |
| `EvaluatorPanelDetail` | `title`, `subtitle`, `requirements` |

## Also for

Every claim cites the prop, default or export that makes it true. A claim that cannot cite one is rejected by a test.

| claim | via |
| --- | --- |
| Any master-detail artifact where the detail is a checklist — a rule and its conditions, a policy and its clauses. | `prop EvaluatorPanel.detail` |
| The same list as EvaluatorList with a different detail pane, since `list` and `detail` are independent props. | `prop EvaluatorPanel.list` |
| The marketing wireframes' side-by-side cut with `split`: the index in a dark left pane (the package's own `dark` subtree switch, nested inside the window's forced `light` the way the product nests its sidebar) with `activeIndex` picking out the open row, the requirements in the cream right pane. Stacks back below `md`. | `prop EvaluatorPanel.split` |

## Not for — reach for this instead

| instead | why |
| --- | --- |
| `EvaluatorVerify` | Only the requirements, with no index beside them. That is EvaluatorVerify. |

## Watch for

| note | via |
| --- | --- |
| Its VerifyRow and MetaPill are copied line-for-line from evaluator-verify. The source says so and defers the merge; do not add a third copy. | `prop EvaluatorPanel.detail` |
| In `split` mode the index renders at lower resolution — name and confirmed count per row; the check pill and metric belong to the stacked table. A caller needing all four columns beside the detail has outgrown the split and wants two windows. | `prop EvaluatorPanel.activeIndex` |

## Reachable from outside

`className`

## Composes

`same-layer/artifact-shell`, `same-layer/evaluator-list`, `same-layer/evaluator-verify`

## Rendered in

Gallery cases with a committed light and dark baseline: `section-evaluator-panel`.

## Page templates that carry it

| template | role |
| --- | --- |
| [capability-deep-dive](../pages/capability-deep-dive.md) | observed on 1 route |

## Rules that are not negotiable

1. **`chrome.*` is invariant and cannot invert.** `color.chrome.surface.*` and
   `color.chrome.text.*` are always dark. Use them only on a surface that never
   flips — a terminal, a log panel, a code frame. Anything on a surface that
   flips uses the theme-aware `color.text.*` / `color.surface.*`. The two share
   their dark values and diverge only in light, so the wrong pick looks correct
   until someone opens light mode.
2. **A light surface on a dark page needs the `light` class on its own root.**
   Without it the mode-aware tokens resolve to their dark values against a light
   fill. That has shipped text at 1.08:1. Check the module's `polarity` first —
   `applies-light` means the module already does this for you.
3. **Content is props.** No section hardcodes copy.

Theming is class-based and the package ships base mode `dark`. Contrast floors: body text 4.5:1, large text 3:1 (WCAG 2.2).

---

System-wide tokens, scales and the full module index: [DESIGN.md](../../DESIGN.md).
