# sections/stat-chip

<!-- @design:generated -->

_Generated by `scripts/generate-design-md.mjs` from `machine/context.yaml`, `machine/components.yaml`, `machine/compositions.yaml`._
_Do not edit. Change the source and run `npm run design`; `npm run design:check` fails the build if this file is stale._
_@skene/design-system 0.16.0._

|  |  |
| --- | --- |
| import | `@skene/design-system/sections/stat-chip` |
| polarity | `inherits` |
| boundary | server-renderable |
| intent | `mark-a-label`, `prove-a-claim` |

## Use for

Two small pills that sit beside headings: `StatChip` asserts something true right now ("121 stars"), `MetaChip` marks something not shipped yet (prose, a hairline divider, then a peach status word). They are two components rather than one with a `variant` precisely so a forgotten prop can never turn a roadmap item into a live claim about the product — two names cannot be defaulted into each other. Both derive their border and fill from `currentColor` via `color-mix`, which is what lets one chip survive both the dark page and the cream `LightSectionCard` with no `onLight` prop.

## Exports

`StatChip`, `MetaChip`

## Props

| export | prop | type / allowed | required | default |
| --- | --- | --- | --- | --- |
| `StatChip` | `icon` | `React.ReactNode` |  |  |
| `StatChip` | `children` | `React.ReactNode` | **yes** |  |
| `MetaChip` | `icon` | `React.ReactNode` |  |  |
| `MetaChip` | `children` | `React.ReactNode` | **yes** |  |
| `MetaChip` | `status` | `React.ReactNode` | **yes** |  |

## Also for

Every claim cites the prop, default or export that makes it true. A claim that cannot cite one is rejected by a test.

| claim | via |
| --- | --- |
| Recolouring the entire chip by setting only the text colour. Because border (26%/22%) and fill (7%/6%) are `color-mix` on `currentColor`, passing e.g. `className="text-semantic-matcha"` retints border, fill, and type together. That `currentColor` derivation IS the tone mechanism — there is no `tone` prop and none is needed. | `className` |
| `MetaChip` as a general "description · marker" chip. `status` is a `ReactNode`, not an enum — BETA, SOON, DEPRECATED, `v2.1`, a region code — rendered monospace, uppercased, tracked at 0.07em. The divider is a real 13px element rather than a `│` glyph, so it tracks the chip height whatever you put on either side. | `export MetaChip` |
| Any leading mark, not just the shipped emoji. The `icon` slot is a `ReactNode` with `shrink-0`, so a small inline SVG or a coloured status dot drops in — but it is `aria-hidden`, so the visible text has to carry the whole meaning. | `prop MetaChip.icon` |
| Composed content in `children` (also a `ReactNode`): a `<strong>` figure with a muted trailing word, a linked count, a short unit. Both chips are `inline-flex` `<span>`s, so they flow in prose and wrap cleanly in a `flex flex-wrap gap-3` row — the shape the gallery uses. | `prop MetaChip.children` |
| Dropping onto a cream surface unchanged. The type uses the mode-aware `text-muted-strong` / `text-muted` roles, so a `light` ancestor inverts everything including the derived edges. No variant, no second call site. | `inline style` |

## Not for — reach for this instead

| instead | why |
| --- | --- |
| `Badge` | Product-surface state. These are section furniture next to headings; for a status marker in a UI use `Badge` (the shadcn primitive) or `Chip` / `WindowStatus` for the 10px mono rectangle. |
| `Eyebrow` | A section-heading kicker. That is `Eyebrow` (pattern), which owns the `font.tracking.eyebrow` token — a different job at a different tracking. |

## Easy to confuse with

`sections/chip#Chip`

These edges are symmetric — the same pairing is recorded from the other side.

## Watch for

| note | via |
| --- | --- |
| The border and background are inline `style` rules, so a `border-*` or `bg-*` utility passed via `className` loses to them. Change the colour by changing the text colour, not the border colour. | `inline style` |
| `MetaChip`'s status word is hardcoded `text-brand-peach` — it is the one part NOT derived from `currentColor`. Retinting the chip leaves the status peach, which is usually right and occasionally a surprise. | `export MetaChip` |
| Never reach for `chrome.*` here. `chrome.text.*` is invariant cream by definition, and these chips render on cream; the whole colour scheme (including the derived edges) is built on the theme-aware `text.*` role for that reason. There is no mode-aware line token, which is exactly why the edges are derived rather than tokenised. | `inline style` |
| Geometry is `rounded-full`, 12px, non-mono — deliberately NOT the 10px mono rectangle of `Chip`/`WindowStatus`. `docs/sections.md` §2 point 3 records this as an open decision (adopt the window-chip geometry or justify the pill), so do not quietly "harmonise" one side of it. | `export StatChip` |

## Reachable from outside

`style`, `className`

`style` means this module writes an inline style that beats any class you pass.

## Constraints

From `machine/components.yaml` (`sections.StatChip`).

- StatChip and MetaChip are deliberately NOT one component with a variant. StatChip states a fact that is true now; MetaChip marks something not-yet-shipped. Merging them lets a roadmap marker be read as a live statistic.
- Both render on dark and on the cream LightSectionCard, so both use the theme-aware text role rather than chrome.text.*.

## Rendered in

Gallery cases with a committed light and dark baseline: `section-stat-chip`.

## Page templates that carry it

| template | role |
| --- | --- |
| [product-page](../pages/product-page.md) | optional, in 1 of 4 |

## What binds this module

- **Polarity `inherits`.** It puts no theme class on its own root, so it takes the page. Place it on a light fill and the `light` class is yours to add, or its mode-aware tokens resolve to their DARK values against that fill — measured at 1.07:1 on rendered pixels.
- **`chrome.*` is invariant.** This module is in the fixed-dark world, so the distinction bites here: `chrome.*` never inverts and belongs only on a surface that never flips. Anything on a surface that follows the theme uses `text.*` / `surface.*`. The two share their dark values, so the wrong pick looks correct until someone opens light mode.
- **Content is props.** This section hardcodes no copy; the page supplies it.

These are the rules this module can break. The full set, the surface roles and
the contrast floors are in [DESIGN.md](../../DESIGN.md).

---

System-wide tokens, scales and the full module index: [DESIGN.md](../../DESIGN.md).
