# sections/team-card

<!-- @design:generated -->

_Generated by `scripts/generate-design-md.mjs` from `machine/context.yaml`, `machine/components.yaml`, `machine/compositions.yaml`._
_Do not edit. Change the source and run `npm run design`; `npm run design:check` fails the build if this file is stale._
_@skene/design-system 0.15.0._

|  |  |
| --- | --- |
| import | `@skene/design-system/sections/team-card` |
| polarity | `inherits` |
| boundary | server-renderable |
| intent | `group-related-items`, `prove-a-claim` |

## Use for

The about-page person card and its grid: a name as a heading, a role in the small-caps monospace register, an optional bio/links block, and an optional square media frame that only exists when media does. The card is designed for the no-photo state — type on a bordered panel that looks finished — so a team page needs no photographer and no stock placeholder. Cards are `<li>`s and `TeamGrid` is the `<ul>`, the same peers-in-a-list argument `TrustPanel` makes for its facts.

## Exports

`TeamCard`, `TeamGrid`

## Props

| export | prop | type / allowed | required | default |
| --- | --- | --- | --- | --- |
| `TeamCard` | `name` | `React.ReactNode` | **yes** |  |
| `TeamCard` | `role` | `React.ReactNode` | **yes** |  |
| `TeamCard` | `media` | `React.ReactNode` |  |  |
| `TeamCard` | `as` | `'h2' \| 'h3' \| 'h4'` |  |  |
| `TeamCard` | `children` | `React.ReactNode` |  |  |
| `TeamGrid` | `children` | `React.ReactNode` | **yes** |  |

## Also for

Every claim cites the prop, default or export that makes it true. A claim that cannot cite one is rejected by a test.

| claim | via |
| --- | --- |
| Any person-shaped entry, not only employees: advisors, authors on a contributors page, speakers — `name` and `role` are slots and no copy is baked in. | `prop TeamCard.name` |
| A card with links: anchors passed in `children` are styled here (underline, offset, hover to `text.primary`), so a bare `<a href>` at the call site is already visible. | `prop TeamCard.children` |
| An illustration or monogram instead of a photo — `media` is a `ReactNode` slot, not a `src`, and the frame's sizing rules make whatever is passed fill it without call-site styling. | `prop TeamCard.media` |
| A grid that IS the section: `as` moves the name heading to `h2` when no section heading sits above the stack. | `prop TeamCard.as` |

## Not for — reach for this instead

| instead | why |
| --- | --- |
| `TrustFact` | A fact with a qualifier in a stack — the icon-title-body row on cream. A person is not a fact and the card carries its own surface. |
| `ValueCard` | A benefit or capability tile. If the "person" is really a role description with no name, it is a value card wearing a face. |

## Watch for

| note | via |
| --- | --- |
| `TeamGrid`'s slot renders inside a `<ul>`, so pass `TeamCard`s, not arbitrary markup — a free-floating `<div>` child is invalid list nesting the browser will repair for you, wrongly. | `prop TeamGrid.children` |
| `alt` on a portrait is the caller's job, and beside the printed name it is usually `alt=""` — the media slot cannot know what it is handed. | `prop TeamCard.media` |

## Reachable from outside

`className`

## Custom properties

`--radius-lg`, `--radius-md`

## Rendered in

Gallery cases with a committed light and dark baseline: `section-team-card`.

## Rules that are not negotiable

1. **`chrome.*` is invariant and cannot invert.** `color.chrome.surface.*` and
   `color.chrome.text.*` are always dark. Use them only on a surface that never
   flips — a terminal, a log panel, a code frame. Anything on a surface that
   flips uses the theme-aware `color.text.*` / `color.surface.*`. The two share
   their dark values and diverge only in light, so the wrong pick looks correct
   until someone opens light mode.
2. **A light surface on a dark page needs the `light` class on its own root.**
   Without it the mode-aware tokens resolve to their dark values against a light
   fill. That has shipped text at 1.08:1. Check the module's `polarity` first —
   `applies-light` means the module already does this for you.
3. **Content is props.** No section hardcodes copy.

Theming is class-based and the package ships base mode `dark`. Contrast floors: body text 4.5:1, large text 3:1 (WCAG 2.2).

---

System-wide tokens, scales and the full module index: [DESIGN.md](../../DESIGN.md).
