# sections/agent-callout

<!-- @design:generated -->

_Generated by `scripts/generate-design-md.mjs` from `machine/context.yaml`, `machine/components.yaml`, `machine/compositions.yaml`._
_Do not edit. Change the source and run `npm run design`; `npm run design:check` fails the build if this file is stale._
_@skene/design-system 0.18.0._

|  |  |
| --- | --- |
| import | `@skene/design-system/sections/agent-callout` |
| polarity | `inherits` |
| boundary | server-renderable |
| intent | `prove-a-claim` |

## Use for

The verdict block: an avatar, a claim in bold, and the evidence under it, in a peach-bordered frame on its own tinted ground. It is the moment Skene speaks on the page.

## Exports

`AgentCallout`

## Props

| export | prop | type / allowed | required | default |
| --- | --- | --- | --- | --- |
| `AgentCallout` | `avatar` | `React.ReactNode` |  |  |
| `AgentCallout` | `eyebrow` | `React.ReactNode` |  |  |
| `AgentCallout` | `children` | `React.ReactNode` | **yes** |  |
| `AgentCallout` | `evidence` | `React.ReactNode` |  |  |

## Also for

Every claim cites the prop, default or export that makes it true. A claim that cannot cite one is rejected by a test.

| claim | via |
| --- | --- |
| Any attributed statement — a customer quote, a reviewer's note, an assistant's summary. Nothing in it is Skene-specific except the default avatar. | `prop AgentCallout.avatar` |
| A one-line callout with no kicker and no evidence: both slots are optional, which is exactly how the nested instance in the release window renders. | `prop AgentCallout.eyebrow` |

## Not for — reach for this instead

| instead | why |
| --- | --- |
| `RecommendationCard` | A proposal rather than a verdict. That is RecommendationCard, which deliberately carries no status colour. |
| `Finding` | A measured state with a status. That is Finding. |

## Watch for

| note | via |
| --- | --- |
| It paints its own near-black fill, which is why the invariant chrome.text.* roles are correct inside it. Give it a light variant and every text class in it has to move to the theme-aware role. | `inline style` |
| The evidence separators are content, not styling — pass the dots you want. | `prop AgentCallout.evidence` |

## Reachable from outside

`style`, `className`

`style` means this module writes an inline style that beats any class you pass.

## Composes

`patterns/skene-mark`

## Constraints

From `machine/components.yaml` (`sections.AgentCallout`).

| prop | allowed |
| --- | --- |
| `eyebrow` | mono kicker over the claim; absent on the nested instance |
| `evidence` | the dot-separated line under the claim; separators are content |
| `avatar` | override the mark; defaults to SkeneMark |

- chrome.* is correct here and this is the exception, not a precedent: the block paints its OWN near-black fill inside its own border, exactly like AskWidget, so nothing under its type can invert. Give it a light variant and every chrome.text.* in it has to become the theme-aware text.* role.
- Border and fill both derive from brand.peach through color-mix, so a token change moves them together. Do not hand-pick a second tint.
- The default avatar is SkeneMark, not a letter in a disc. Wherever the product speaks for itself the real symbol is the mark — the demo drew an "S" because it had no access to the artwork.

## Rendered in

Gallery cases with a committed light and dark baseline: `section-agent-callout`.

## Page templates that carry it

| template | role |
| --- | --- |
| [developer-page](../pages/developer-page.md) | optional, in 1 of 2 |
| [capability-deep-dive](../pages/capability-deep-dive.md) | observed on 1 route |
| [home-page](../pages/home-page.md) | observed on 1 route |

## What binds this module

- **Polarity `inherits`.** It puts no theme class on its own root, so it takes the page. Place it on a light fill and the `light` class is yours to add, or its mode-aware tokens resolve to their DARK values against that fill — measured at 1.07:1 on rendered pixels.
- **`chrome.*` is invariant.** This module is in the fixed-dark world, so the distinction bites here: `chrome.*` never inverts and belongs only on a surface that never flips. Anything on a surface that follows the theme uses `text.*` / `surface.*`. The two share their dark values, so the wrong pick looks correct until someone opens light mode.
- **Content is props.** This section hardcodes no copy; the page supplies it.

These are the rules this module can break. The full set, the surface roles and
the contrast floors are in [DESIGN.md](../../DESIGN.md).

---

System-wide tokens, scales and the full module index: [DESIGN.md](../../DESIGN.md).
