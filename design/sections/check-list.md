# sections/check-list

<!-- @design:generated -->

_Generated by `scripts/generate-design-md.mjs` from `machine/context.yaml`, `machine/components.yaml`, `machine/compositions.yaml`._
_Do not edit. Change the source and run `npm run design`; `npm run design:check` fails the build if this file is stale._
_@skene/design-system 0.17.0._

|  |  |
| --- | --- |
| import | `@skene/design-system/sections/check-list` |
| polarity | `inherits` |
| boundary | server-renderable |
| intent | `prove-a-claim`, `group-related-items` |

## Use for

The rule-separated feature list with a peach check — the single highest-frequency element on a Skene marketing page, previously hand-written at every call site. `CheckList` is a bare `<ul>` that sets one CSS variable (`--check-rule`); `CheckItem` is an `<li>` whose separator is a *top border on each row* rather than a divider element, so a list of one still reads as part of its card instead of floating. The check is a real `aria-hidden` `<span>`, not a `::before`, precisely so a screen reader does not announce "tick" before every line.

## Exports

`CheckList`, `CheckItem`

## Props

| export | prop | type / allowed | required | default |
| --- | --- | --- | --- | --- |
| `CheckList` | `onLight` | `boolean` |  | `false` |
| `CheckList` | `dense` | `boolean` |  | `false` |
| `CheckList` | `children` | `React.ReactNode` | **yes** |  |
| `CheckItem` | `dense` | `boolean` |  | `false` |
| `CheckItem` | `children` | `React.ReactNode` | **yes** |  |

## Also for

Every claim cites the prop, default or export that makes it true. A claim that cannot cite one is rejected by a test.

| claim | via |
| --- | --- |
| Any proof list in a section body, not just plan cards — the gallery uses it directly inside `FeatureRow` (dark, no flags) and inside `LightSectionCard` (`onLight`). | `prop CheckList.onLight` |
| Requirement / "what you get" lists on light product chrome: nest it in a `ProductWindow` (whose `tone="light"` default carries the `light` class) and pass `onLight`. | `prop CheckList.onLight` |
| Rows with structure, not just strings. `CheckItem` children are `ReactNode`, so links, `<strong>`, or a trailing `<Chip>` all render, and the aria-hidden glyph keeps the announcement clean. | `export CheckItem` |
| Two densities on the same page: `dense` (13px, `py-[11px]`) for card interiors, the default (14px, `py-3`) for section copy. | `prop CheckItem.dense` |
| Per-row tweaks via `className` on `CheckItem` — `cn` is tailwind-merge, so `border-t-0` on the first row or a different `py-*` actually replaces the base utility. | `export CheckItem` |

## Not for — reach for this instead

| instead | why |
| --- | --- |
| `Checkbox` | An interactive checklist or to-do list. There is no checked state, no input, and the glyph is `aria-hidden` — use `ui/checkbox` (or `SettingsField`) for anything the user toggles. |
| `CheckItem` | An included/not-included comparison. The ✓ glyph and its `text-brand-peach` colour are hardcoded with no icon or tone prop; a cross/dash row would need a code change (e.g. a `tone`/`icon` prop on `CheckItem`). `ComparisonTable`'s `TableCheck` / `TableDash` already do this. |

## Watch for

| note | via |
| --- | --- |
| `dense` does not propagate. It is a separate prop on `CheckList` (margin) and on `CheckItem` (padding + type size); set it on both or you get tight spacing with 14px text. | `prop CheckItem.dense` |
| `onLight` does NOT create the light context — it only chooses between `--color-chrome-line-on-light` and `--color-chrome-line-subtle` for the rule. The peach check and the `text.*` type invert only if a `light` ancestor exists (featured `PlanCard`, `LightSectionCard`, light `ProductWindow`). On a bare cream `<div>` you must add `light` yourself, as the gallery case does. | `prop CheckList.onLight` |
| The rule colour is an inline `style={{ borderTopColor: 'var(--check-rule)' }}` on every item, so a `border-*` colour utility in `className` will not override it — re-set `--check-rule` instead. | `inline style` |
| Every item carries `border-t`, including the first, so the list always opens with a hairline. That is intentional inside a card; strip it with `className="border-t-0"` on the first `CheckItem` if the list stands alone. | `export CheckItem` |
| Type is `text-text-muted-strong` (theme-aware) by design — swapping in a `chrome.text.*` class reintroduces the exact bug the header warns about. | `className` |

## Reachable from outside

`style`, `className`

`style` means this module writes an inline style that beats any class you pass.

## Custom properties

`--check-rule`

## Constraints

From `machine/components.yaml` (`sections.CheckList`).

| prop | allowed |
| --- | --- |
| `onLight` | boolean — the list sits on a cream/featured surface |
| `dense` | boolean — tightens padding and type, for a plan card |

- Type uses the theme-aware text.* role, never chrome.text.*. This list renders on cream featured plan cards as well as dark ones, and chrome.* is invariant by definition: cream text that can never invert, which on a cream card is invisible. Only the mode-aware role follows a `light` ancestor.
- `onLight` switches the RULE token only (--check-rule). Everything else in the light case resolves itself from a `light` ancestor (PlanCard--featured, ProductWindow tone=light), so `onLight` is not a substitute for putting the list inside one.
- The separator is a top border per item, not a divider element, so a list of one still reads as part of its card rather than floating.
- The check is a real span, not a ::before, and it is aria-hidden. A screen reader announcing "tick" before every line is noise, and aria-hidden on a pseudo-element is not expressible. Do not reintroduce it as CSS.
- `dense` sets both the padding and the type. Pass the prop rather than tightening one of the two from a call site; the six homepage instances that were hand-written each time are why this component exists.

## Rendered in

Gallery cases with a committed light and dark baseline: `section-check-list`.

## Page templates that carry it

| template | role |
| --- | --- |
| [product-page](../pages/product-page.md) | optional, in 1 of 4 |
| [community-page](../pages/community-page.md) | optional, in 1 of 2 |
| [company-page](../pages/company-page.md) | optional, in 1 of 2 |
| [home-page](../pages/home-page.md) | observed on 1 route |
| [pricing-page](../pages/pricing-page.md) | observed on 1 route |

## What binds this module

- **Polarity `inherits`.** It puts no theme class on its own root, so it takes the page. Place it on a light fill and the `light` class is yours to add, or its mode-aware tokens resolve to their DARK values against that fill — measured at 1.07:1 on rendered pixels.
- **`chrome.*` is invariant.** This module is in the fixed-dark world, so the distinction bites here: `chrome.*` never inverts and belongs only on a surface that never flips. Anything on a surface that follows the theme uses `text.*` / `surface.*`. The two share their dark values, so the wrong pick looks correct until someone opens light mode.
- **Content is props.** This section hardcodes no copy; the page supplies it.

These are the rules this module can break. The full set, the surface roles and
the contrast floors are in [DESIGN.md](../../DESIGN.md).

---

System-wide tokens, scales and the full module index: [DESIGN.md](../../DESIGN.md).
