# sections/evaluator-list

<!-- @design:generated -->

_Generated by `scripts/generate-design-md.mjs` from `machine/context.yaml`, `machine/components.yaml`, `machine/compositions.yaml`._
_Do not edit. Change the source and run `npm run design`; `npm run design:check` fails the build if this file is stale._
_@skene/design-system 0.15.0._

|  |  |
| --- | --- |
| import | `@skene/design-system/sections/evaluator-list` |
| polarity | `inherits` |
| boundary | server-renderable |
| intent | `tabulate-data`, `show-status` |

## Use for

The evaluator index: every evaluation in a workspace, and whether the signals it depends on were found.

## Exports

`EvaluatorList`, `EvaluatorNote`

## Props

| export | prop | type | required | default |
| --- | --- | --- | --- | --- |
| `EvaluatorList` | `crumb` | `React.ReactNode` |  |  |
| `EvaluatorList` | `summary` | `{ status: StatPillStatus; label: React.ReactNode }` |  |  |
| `EvaluatorList` | `columns` | `EvaluatorListColumns` | yes |  |
| `EvaluatorList` | `evaluations` | `EvaluationEntry[]` | yes |  |
| `EvaluatorList` | `note` | `React.ReactNode` |  |  |
| `EvaluatorList` | `frame` | `ArtFrameKind \| false` |  | `'jr'` |
| `EvaluatorNote` | `children` | `React.ReactNode` | yes |  |

## Types

| type | shape |
| --- | --- |
| `EvaluatorListColumns` | `name`, `check`, `metric`, `confirmed` |
| `EvaluationEntry` | `name`, `check`, `metric`, `confirmed` |

## Also for

Every claim cites the prop, default or export that makes it true. A claim that cannot cite one is rejected by a test.

| claim | via |
| --- | --- |
| Any status list with a per-row check — jobs, monitors, integrations tests. Four columns, all named by prop. | `prop EvaluatorList.columns` |
| Unframed, by passing `frame={false}` — the textured field is a placement decision, not a property of the artifact. | `prop EvaluatorList.frame` |

## Not for — reach for this instead

| instead | why |
| --- | --- |
| `EvaluatorPanel` | The two-pane version, list beside detail. That is EvaluatorPanel. |
| `DiscoveryTable` | Found events rather than evaluations. That is DiscoveryTable. |

## Watch for

| note | via |
| --- | --- |
| EvaluatorNote is exported from here and re-typed in three other files. Import it rather than adding a fourth copy. | `export EvaluatorNote` |

## Reachable from outside

`className`

## Composes

`same-layer/artifact-shell`

## Rendered in

Gallery cases with a committed light and dark baseline: `section-evaluator-list`.

## Page templates that carry it

| template | role |
| --- | --- |
| [home-page](../pages/home-page.md) | observed on 1 route |

## Rules that are not negotiable

1. **`chrome.*` is invariant and cannot invert.** `color.chrome.surface.*` and
   `color.chrome.text.*` are always dark. Use them only on a surface that never
   flips — a terminal, a log panel, a code frame. Anything on a surface that
   flips uses the theme-aware `color.text.*` / `color.surface.*`. The two share
   their dark values and diverge only in light, so the wrong pick looks correct
   until someone opens light mode.
2. **A light surface on a dark page needs the `light` class on its own root.**
   Without it the mode-aware tokens resolve to their dark values against a light
   fill. That has shipped text at 1.08:1. Check the module's `polarity` first —
   `applies-light` means the module already does this for you.
3. **Content is props.** No section hardcodes copy.

Theming is class-based and the package ships base mode `dark`. Contrast floors: body text 4.5:1, large text 3:1 (WCAG 2.2).

---

System-wide tokens, scales and the full module index: [DESIGN.md](../../DESIGN.md).
