# sections/evaluator-verify

<!-- @design:generated -->

_Generated by `scripts/generate-design-md.mjs` from `machine/context.yaml`, `machine/components.yaml`, `machine/compositions.yaml`._
_Do not edit. Change the source and run `npm run design`; `npm run design:check` fails the build if this file is stale._
_@skene/design-system 0.26.0._

|  |  |
| --- | --- |
| import | `@skene/design-system/sections/evaluator-verify` |
| polarity | `inherits` |
| boundary | server-renderable |
| intent | `tabulate-data`, `show-status` |

## Use for

The requirements an evaluation depends on: one row per event, its verdict, and the fields it must carry.

## Exports

`MetaPill`, `VerifyRow`, `EvaluatorVerify`

## Props

| export | prop | type / allowed | required | default |
| --- | --- | --- | --- | --- |
| `EvaluatorVerify` | `crumb` | `React.ReactNode` |  |  |
| `EvaluatorVerify` | `summary` | `{ status: StatPillStatus; label: React.ReactNode }` |  |  |
| `EvaluatorVerify` | `title` | `React.ReactNode` | **yes** |  |
| `EvaluatorVerify` | `subtitle` | `React.ReactNode` |  |  |
| `EvaluatorVerify` | `requirements` | `VerifyRequirement[]` | **yes** |  |
| `EvaluatorVerify` | `note` | `React.ReactNode` |  |  |
| `EvaluatorVerify` | `frame` | `ArtFrameKind \| false` |  | `'jr'` |

## Types — not components

These are TypeScript types, not exports you can render. `<VerifyField />` is not a component; the type describes the shape of a value you pass to one of the exports above. The name usually reads like a component, which is why this heading says so.

| type | shape |
| --- | --- |
| `VerifyField` | `name`, `note`, `status`, `verdict` |
| `VerifyRequirement` | `tags`, `fields` |

## Also for

Every claim cites the prop, default or export that makes it true. A claim that cannot cite one is rejected by a test.

| claim | via |
| --- | --- |
| Any requirements checklist with nested sub-requirements — a schema contract, a compliance list, a validation report. | `prop EvaluatorVerify.requirements` |
| A flat list, by omitting `fields` on every requirement: the indent is what says which row to read, so uniform rows are a deliberate choice. | `type VerifyRequirement.fields` |

## Not for — reach for this instead

| instead | why |
| --- | --- |
| `EvaluatorPanel` | The same rows with the index beside them. That is EvaluatorPanel. |

## Watch for

| note | via |
| --- | --- |
| `tags` is free-form because the package does not know the product's vocabulary. That is also why nothing validates them. | `type VerifyRequirement.tags` |

## Reachable from outside

`style`, `className`

`style` means this module writes an inline style that beats any class you pass.

## Custom properties

`--border`

## Composes

`same-layer/evaluator-list`, `same-layer/key-value-table`, `same-layer/artifact-shell`

## Rendered in

Gallery cases with a committed light and dark baseline: `section-evaluator-verify`.

## Page templates that carry it

| template | role |
| --- | --- |
| [use-case-page](../pages/use-case-page.md) | optional, in 2 of 3 |
| [capability-deep-dive](../pages/capability-deep-dive.md) | observed on 1 route |

## What binds this module

- **Polarity `inherits`.** It puts no theme class on its own root, so it takes the page. Place it on a light fill and the `light` class is yours to add, or its mode-aware tokens resolve to their DARK values against that fill — measured at 1.07:1 on rendered pixels.
- **Content is props.** This section hardcodes no copy; the page supplies it.

These are the rules this module can break. The full set, the surface roles and
the contrast floors are in [DESIGN.md](../../DESIGN.md).

---

System-wide tokens, scales and the full module index: [DESIGN.md](../../DESIGN.md).
