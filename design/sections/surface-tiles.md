# sections/surface-tiles

<!-- @design:generated -->

_Generated by `scripts/generate-design-md.mjs` from `machine/context.yaml`, `machine/components.yaml`, `machine/compositions.yaml`._
_Do not edit. Change the source and run `npm run design`; `npm run design:check` fails the build if this file is stale._
_@skene/design-system 0.15.0._

|  |  |
| --- | --- |
| import | `@skene/design-system/sections/surface-tiles` |
| polarity | `applies-light` |
| boundary | server-renderable |
| intent | `compare-options`, `pitch-a-capability` |

## Use for

The surfaces a product runs on, as a row of tiles with one picked out, and the panel that explains the chosen one.

## Exports

`SurfaceTiles`, `SurfaceTile`, `SurfaceDetail`

## Props

| export | prop | type | required | default |
| --- | --- | --- | --- | --- |
| `SurfaceTiles` | `children` | `React.ReactNode` | yes |  |
| `SurfaceTile` | `icon` | `React.ReactNode` |  |  |
| `SurfaceTile` | `accent` | `SurfaceAccent` |  | `'neutral'` |
| `SurfaceTile` | `name` | `React.ReactNode` | yes |  |
| `SurfaceTile` | `note` | `React.ReactNode` |  |  |
| `SurfaceTile` | `selected` | `boolean` |  |  |
| `SurfaceDetail` | `tag` | `React.ReactNode` |  |  |
| `SurfaceDetail` | `children` | `React.ReactNode` | yes |  |
| `SurfaceDetail` | `code` | `React.ReactNode` |  |  |

## Also for

Every claim cites the prop, default or export that makes it true. A claim that cannot cite one is rejected by a test.

| claim | via |
| --- | --- |
| Any set of options where one is editorially highlighted — plans, integrations, entry points. | `prop SurfaceTile.selected` |
| A command or identifier under the row, via SurfaceDetail's `code`, which renders a mono chip rather than a terminal. | `prop SurfaceDetail.code` |
| Tinted icons per tile through `accent`, which reuses FeatureIcon's vocabulary plus a neutral. | `prop SurfaceTile.accent` |

## Not for — reach for this instead

| instead | why |
| --- | --- |
| `OverviewTile` | Commensurable numbers rather than choices. That is OverviewTiles, where nothing can be selected. |
| `PlanCard` | Options that each need a price, a feature list and a CTA. That is PlanCard. |

## Easy to confuse with

`sections/surface-cards#SurfaceCards`

These edges are symmetric — the same pairing is recorded from the other side.

## Watch for

| note | via |
| --- | --- |
| `selected` carries the `light` class, which is the whole mechanism. Without it, cream type lands on a cream fill — the bug that shipped invisible twice in this package. | `prop SurfaceTile.selected` |
| Selection is presentational. There is no onSelect and no internal state, so a real picker means the caller owns the state. | `prop SurfaceTile.selected` |

## Reachable from outside

`style`, `className`

`style` means this module writes an inline style that beats any class you pass.

## Constraints

From `machine/components.yaml` (`sections.SurfaceTiles`).

| prop | allowed |
| --- | --- |
| `selected` | the cream tile — applies `light` to that tile |
| `accent` | `peach`, `violet`, `blue`, `neutral` |

- Not OverviewTiles. That one is caption/number/note and exists so four counts read as commensurable; these are icon/name/note with exactly one selected. A metric tile cannot be "chosen".
- selected carries the `light` class, which is the whole mechanism. On a cream tile without it, text.primary is #faf1e9 on #faf1e9. The unselected tiles paint their own near-black ground and so take the invariant chrome.* roles legitimately.
- Selection is presentational: no onSelect, no internal state. The band that uses this is editorial, and a caller who wants a real picker owns the state and passes the flag.
- SurfaceDetail is a SIBLING of the row, not a child. Nesting it would force the row to own selection state to know where to render it.
- These render on a SectionBackdrop field, and SurfaceDetail is deliberately translucent so the halftone reads through it. A solid fill punches a rectangle out of the field.

## Rendered in

Gallery cases with a committed light and dark baseline: `section-surface-tiles`.

## Page templates that carry it

| template | role |
| --- | --- |
| [product-page](../pages/product-page.md) | optional, in 1 of 4 |

## Rules that are not negotiable

1. **`chrome.*` is invariant and cannot invert.** `color.chrome.surface.*` and
   `color.chrome.text.*` are always dark. Use them only on a surface that never
   flips — a terminal, a log panel, a code frame. Anything on a surface that
   flips uses the theme-aware `color.text.*` / `color.surface.*`. The two share
   their dark values and diverge only in light, so the wrong pick looks correct
   until someone opens light mode.
2. **A light surface on a dark page needs the `light` class on its own root.**
   Without it the mode-aware tokens resolve to their dark values against a light
   fill. That has shipped text at 1.08:1. Check the module's `polarity` first —
   `applies-light` means the module already does this for you.
3. **Content is props.** No section hardcodes copy.

Theming is class-based and the package ships base mode `dark`. Contrast floors: body text 4.5:1, large text 3:1 (WCAG 2.2).

---

System-wide tokens, scales and the full module index: [DESIGN.md](../../DESIGN.md).
