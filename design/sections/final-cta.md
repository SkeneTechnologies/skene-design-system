# sections/final-cta

<!-- @design:generated -->

_Generated by `scripts/generate-design-md.mjs` from `machine/context.yaml`, `machine/components.yaml`, `machine/compositions.yaml`._
_Do not edit. Change the source and run `npm run design`; `npm run design:check` fails the build if this file is stale._
_@skene/design-system 0.24.0._

|  |  |
| --- | --- |
| import | `@skene/design-system/sections/final-cta` |
| polarity | `inherits` |
| boundary | server-renderable |
| intent | `drive-an-action`, `texture-a-surface` |

## Use for

The closing band: full-bleed, centred, one large heading, an optional lede and an actions row, over a textured dark backdrop. It appears twice on the live homepage and once per subpage, and every instance had previously been rebuilt by hand. The default backdrop (`pixel-bg.webp`, 143 KB) ships with the package and is resolved from the module itself, on the principle that the default should produce the section the site actually has rather than a stripped-down gradient the caller has to re-dress.

## Exports

`FinalCta`

## Props

| export | prop | type / allowed | required | default |
| --- | --- | --- | --- | --- |
| `FinalCta` | `backdrop` | `string \| false` |  |  |
| `FinalCta` | `actions` | `React.ReactNode` |  |  |
| `FinalCta` | `lede` | `React.ReactNode` |  |  |
| `FinalCta` | `eyebrow` | `React.ReactNode` |  |  |
| `FinalCta` | `children` | `React.ReactNode` | **yes** |  |

## Also for

Every claim cites the prop, default or export that makes it true. A claim that cannot cite one is rejected by a test.

| claim | via |
| --- | --- |
| Any full-bleed closing band, not just signup — `children` is the heading node and `actions` is a free slot, so waitlist, newsletter, "book a demo", or an end-of-doc band all fit without changes. | `prop FinalCta.children` |
| Per-page texture: `backdrop` takes any URL string (including a `data:` URI); `backdrop={false}` renders the gradient alone for a quieter band on a page that already has texture. | `prop FinalCta.backdrop` |
| Non-button actions. The wrapper is `flex flex-col … sm:flex-row gap-3`, so an email input plus a submit button, or a client `<form>` island, drops in as `actions` while the band itself stays a server component. | `prop FinalCta.actions` |
| Height and rhythm tuning via `className` — `min-h-*` and padding merge through tailwind-merge, so `min-h-[380px]` makes a mid-page band out of the same component. | `className` |

## Not for — reach for this instead

| instead | why |
| --- | --- |
| `LightSectionCard` | A cream or inverted CTA panel, or an inline mid-page CTA card — use `LightSectionCard`, which is the documented choice for "a cream panel with copy and buttons" and carries the `light` class properly. |
| `DitheredMedia` | A hero. The fixed dark scrim, `<h2>`, and centred single-column layout are closing-band shape; use `DitheredMedia` for a full hero composition or `HeroBackdrop` for a hero strip. |

## Watch for

| note | via |
| --- | --- |
| Every colour is an inline `style`: the section background (`--color-chrome-surface-deep`) and the `linear-gradient(...) , url(...)` scrim with `saturate(70%)`. Utility classes in `className` lose to inline styles, so you cannot recolour or re-scrim it from the call site. | `inline style` |
| All type uses invariant `chrome.text.*`. This band is dark by construction — adding `light` will not invert it and will break any mode-aware node you pass into `actions`. | `prop FinalCta.actions` |
| The heading is a hardcoded `<h2>` at `clamp(2.75rem, 5vw, 5rem)`. A page that needs this to be its `<h1>`, or a visibly smaller heading, needs a prop addition (`as`/`headingLevel`) rather than a class. | `prop FinalCta.title` |
| The default backdrop resolves via `new URL('../../assets/pixel-bg.webp', import.meta.url)`. Bundlers that understand that pattern (Next included) emit a real asset URL; one that does not will emit a broken path — pass an explicit URL or `false` as the escape hatch. | `prop FinalCta.backdrop` |
| `min-h-[560px]` / `md:min-h-[610px]` plus `py-[90px]`: it is a whole band and will dominate a short page. | `className` |

## Reachable from outside

`style`, `className`

`style` means this module writes an inline style that beats any class you pass.

## Rendered in

Gallery cases with a committed light and dark baseline: `section-final-cta`, `section-final-cta-eyebrow`.

## Page templates that carry it

| template | role |
| --- | --- |
| [home-page](../pages/home-page.md) | observed on 1 route |

## What binds this module

- **Polarity `inherits`.** It puts no theme class on its own root, so it takes the page. Place it on a light fill and the `light` class is yours to add, or its mode-aware tokens resolve to their DARK values against that fill — measured at 1.07:1 on rendered pixels.
- **`chrome.*` is invariant.** This module is in the fixed-dark world, so the distinction bites here: `chrome.*` never inverts and belongs only on a surface that never flips. Anything on a surface that follows the theme uses `text.*` / `surface.*`. The two share their dark values, so the wrong pick looks correct until someone opens light mode.
- **Content is props.** This section hardcodes no copy; the page supplies it.

These are the rules this module can break. The full set, the surface roles and
the contrast floors are in [DESIGN.md](../../DESIGN.md).

---

System-wide tokens, scales and the full module index: [DESIGN.md](../../DESIGN.md).
