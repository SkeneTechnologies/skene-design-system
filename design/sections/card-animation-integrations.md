# sections/card-animation-integrations

<!-- @design:generated -->

_Generated by `scripts/generate-design-md.mjs` from `machine/context.yaml`, `machine/components.yaml`, `machine/compositions.yaml`._
_Do not edit. Change the source and run `npm run design`; `npm run design:check` fails the build if this file is stale._
_@skene/design-system 0.23.1._

|  |  |
| --- | --- |
| import | `@skene/design-system/sections/card-animation-integrations` |
| polarity | `inherits` |
| boundary | `"use client"` — client component |
| intent | `pitch-a-capability`, `group-related-items` |

## Use for

Four integration cards on a textured field, cycling detail copy with GSAP: MCP server, GitHub App, Cloud API, and repo audit. Ported from skene-marketing-website's CardAnimationIntegrations.

## Exports

`INTEGRATION_ANIMATION_CARDS`, `INTEGRATION_ANIMATION_DETAILS`, `CardAnimationIntegrations`

## Props

| export | prop | type / allowed | required | default |
| --- | --- | --- | --- | --- |
| `CardAnimationIntegrations` | `backgroundImage` | `string` |  | `INTEGRATIONS_TEXTURE` |
| `CardAnimationIntegrations` | `cards` | `IntegrationAnimationCard[]` |  | `INTEGRATION_ANIMATION_CARDS` |
| `CardAnimationIntegrations` | `details` | `IntegrationAnimationDetail[]` |  | `INTEGRATION_ANIMATION_DETAILS` |

## Types — not components

These are TypeScript types, not exports you can render. `<IntegrationAnimationCard />` is not a component; the type describes the shape of a value you pass to one of the exports above. The name usually reads like a component, which is why this heading says so.

| type | shape |
| --- | --- |
| `IntegrationAnimationCard` | `variant`, `icon`, `title`, `context` |
| `IntegrationAnimationDetail` | `badge`, `badgeVariant`, `text`, `code` |

## Also for

Every claim cites the prop, default or export that makes it true. A claim that cannot cite one is rejected by a test.

| claim | via |
| --- | --- |
| The visual slot of a cream band — drop it in `LightSectionCard.visual` or `IntegrationsHighlight`, which is the pre-composed homepage band. | `export CardAnimationIntegrations` |
| Custom card or detail sets by passing `cards` and `details`; defaults match the live marketing animation. | `prop CardAnimationIntegrations.cards` |
| A different halftone field via `backgroundImage`; defaults to `assetUrls.integrationsField`. | `prop CardAnimationIntegrations.backgroundImage` |

## Not for — reach for this instead

| instead | why |
| --- | --- |
| `SurfaceCards` | A static 2x2 grid where every detail is visible at once. Use this when you need motion and one active card at a time. |
| `SurfaceTiles` | A row of tiles with a single detail panel under the chosen one. |

## Watch for

| note | via |
| --- | --- |
| Client-only: uses GSAP and ScrollTrigger. Importing it pulls a client boundary at this file. | `export CardAnimationIntegrations` |
| Cards and detail panel start at `visibility: hidden`; GSAP drives `autoAlpha`. Do not remove that or the first paint flashes every card. | `export CardAnimationIntegrations` |

## Reachable from outside

`style`, `className`, `use client`

`style` means this module writes an inline style that beats any class you pass.

## Custom properties

`--cursor`

## Composes

`lib/use-container-scale`

## Rendered in

Gallery cases with a committed light and dark baseline: `section-card-animation-integrations`, `section-card-animation-integrations-last`.

## What binds this module

- **Polarity `inherits`.** It puts no theme class on its own root, so it takes the page. Place it on a light fill and the `light` class is yours to add, or its mode-aware tokens resolve to their DARK values against that fill — measured at 1.07:1 on rendered pixels.
- **Content is props.** This section hardcodes no copy; the page supplies it.

These are the rules this module can break. The full set, the surface roles and
the contrast floors are in [DESIGN.md](../../DESIGN.md).

---

System-wide tokens, scales and the full module index: [DESIGN.md](../../DESIGN.md).
