# sections/footer

<!-- @design:generated -->

_Generated by `scripts/generate-design-md.mjs` from `machine/context.yaml`, `machine/components.yaml`, `machine/compositions.yaml`._
_Do not edit. Change the source and run `npm run design`; `npm run design:check` fails the build if this file is stale._
_@skene/design-system 0.23.0._

|  |  |
| --- | --- |
| import | `@skene/design-system/sections/footer` |
| polarity | `inherits` |
| boundary | server-renderable |
| intent | `navigate`, `brand-the-surface` |

## Use for

The site footer as a set of composable parts — `SiteFooter` (brand column, link columns, bottom bar), `FooterColumn`, `FooterLink`, `SocialLinks`, `SocialLink`. The part actually worth reusing is the oversized wordmark: 29vw at 1.7% opacity, clipped by the footer's own `overflow-hidden`, so it reads as texture at a glance and only resolves into a word if you look. Rendering it at a "sensible" size or opacity is the obvious change and it is wrong every time.

## Exports

`SiteFooter`, `FooterColumn`, `FooterLink`, `SocialLinks`, `SocialLink`

## Props

| export | prop | type / allowed | required | default |
| --- | --- | --- | --- | --- |
| `SiteFooter` | `brand` | `React.ReactNode` |  |  |
| `SiteFooter` | `wordmark` | `React.ReactNode` |  |  |
| `SiteFooter` | `copyright` | `React.ReactNode` |  |  |
| `SiteFooter` | `legal` | `React.ReactNode` |  |  |
| `SiteFooter` | `children` | `React.ReactNode` | **yes** |  |
| `FooterColumn` | `title` | `React.ReactNode` | **yes** |  |
| `FooterColumn` | `children` | `React.ReactNode` | **yes** |  |

## Also for

Every claim cites the prop, default or export that makes it true. A claim that cannot cite one is rejected by a test.

| claim | via |
| --- | --- |
| The sub-components stand alone. `FooterColumn` is a self-contained `<nav>` with its own heading and grid, so a link cluster works in a mega-menu, a sitemap block, or a docs index without `SiteFooter` around it. | `export FooterColumn` |
| Router-aware links: `FooterColumn` children are `ReactNode`, so `next/link` (or any framework Link) can be used directly where client-side navigation matters — `FooterLink` is a plain `<a href>` with no `asChild`. | `export FooterColumn` |
| A bar-only footer. `brand`, `wordmark`, `copyright` and `legal` are all optional and the bottom bar only renders when `copyright \|\| legal` is present, so a minimal legal strip is reachable. | `prop SiteFooter.brand` |
| `wordmark` takes any `ReactNode` — a product name, a single tagline word, an inline SVG — and it is `aria-hidden`, so it never enters the accessibility tree whatever you put there. | `prop SiteFooter.wordmark` |
| `brand` is a free slot: logo plus copy plus `SocialLinks` in the demo, but a newsletter form or a status badge fits the same column (`col-span-full lg:col-span-1`). | `prop SiteFooter.brand` |

## Not for — reach for this instead

| instead | why |
| --- | --- |
| `Card` | An in-app or workspace footer, or any footer on a light page. The invariant `chrome.*` tokens cannot invert; build product-surface footers from `ui/card` / plain nav markup. |
| `FooterColumn` | Primary site navigation. `FooterColumn` is a column of quiet 14px links with a label-sized `<h2>`; use the `PillNav` pattern from `patterns/marketing` for the top bar. |

## Watch for

| note | via |
| --- | --- |
| `FooterColumn` does `aria-label={String(title)}`. Pass a string. Pass JSX and every column's landmark is labelled "[object Object]" — the gallery case casts `title as string` for this reason. | `export FooterColumn` |
| The link grid template is `lg:grid-cols-[1.7fr_repeat(3,1fr)]` — sized for brand plus exactly three columns — and it sits on an inner `<div>` that has no `className` hook. `className` lands on the `<footer>` root, so you cannot retemplate the columns from the call site; a fourth column wraps to a second row. | `prop SiteFooter.brand` |
| The wordmark's `color: rgba(255,255,255,0.017)` is an inline style, so `text-*` utilities cannot touch it, and the value is deliberate — see the file header before "fixing" it. | `prop SiteFooter.wordmark` |
| `overflow-hidden` on the root is what clips the wordmark, and it will equally clip anything absolutely positioned that you put in `brand` (a popover, a tooltip, a dropdown). | `prop SiteFooter.brand` |
| Everything is invariant `chrome.*` (`bg-chrome-surface-deep-2`, `chrome-text-muted-warm`), so the whole component assumes an always-dark page. | `className` |
| `SocialLink` requires `label` — the child is an icon, so without it the link has no accessible name. That requirement is in the type; do not work around it. | `export SocialLink` |

## Reachable from outside

`style`, `className`

`style` means this module writes an inline style that beats any class you pass.

## Rendered in

Gallery cases with a committed light and dark baseline: `section-footer`.

## What binds this module

- **Polarity `inherits`.** It puts no theme class on its own root, so it takes the page. Place it on a light fill and the `light` class is yours to add, or its mode-aware tokens resolve to their DARK values against that fill — measured at 1.07:1 on rendered pixels.
- **`chrome.*` is invariant.** This module is in the fixed-dark world, so the distinction bites here: `chrome.*` never inverts and belongs only on a surface that never flips. Anything on a surface that follows the theme uses `text.*` / `surface.*`. The two share their dark values, so the wrong pick looks correct until someone opens light mode.
- **Content is props.** This section hardcodes no copy; the page supplies it.

These are the rules this module can break. The full set, the surface roles and
the contrast floors are in [DESIGN.md](../../DESIGN.md).

---

System-wide tokens, scales and the full module index: [DESIGN.md](../../DESIGN.md).
