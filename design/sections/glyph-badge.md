# sections/glyph-badge

<!-- @design:generated -->

_Generated by `scripts/generate-design-md.mjs` from `machine/context.yaml`, `machine/components.yaml`, `machine/compositions.yaml`._
_Do not edit. Change the source and run `npm run design`; `npm run design:check` fails the build if this file is stale._
_@skene/design-system 0.18.0._

|  |  |
| --- | --- |
| import | `@skene/design-system/sections/glyph-badge` |
| polarity | `inherits` |
| boundary | server-renderable |
| intent | `mark-a-label` |

## Use for

The round disc a glyph sits in at the head of a row: a circle, a hairline, a peach mark centred in it, `aria-hidden` always. It is the disc `TrustFact` draws, lifted out so it is reachable without the `<li>`, the separating rule and the qualifier slot that come with a fact row. `tone` picks the ground — `tint` is the cream-panel pair (`chrome.line.onLight` over a 12% peach wash), `muted` is the themed pair that follows the page — and `size` is one number, written to `style` so the glyph inside keeps whatever type size it inherits.

## Exports

`GlyphBadge`

## Props

| export | prop | type / allowed | required | default |
| --- | --- | --- | --- | --- |
| `GlyphBadge` | `tone` | `GlyphBadgeTone` |  | `'tint'` |
| `GlyphBadge` | `size` | `number` |  | `38` |
| `GlyphBadge` | `glyphSize` | `number \| string` |  |  |
| `GlyphBadge` | `children` | `React.ReactNode` |  |  |

## Also for

Every claim cites the prop, default or export that makes it true. A claim that cannot cite one is rejected by a test.

| claim | via |
| --- | --- |
| Any icon-led row, on cream or on the page ground: a status glyph, a numbered marker, a two- or three-letter code. Nothing here knows what the mark means, and the disc still renders when `children` is empty — which is what keeps a stack of rows aligned on their titles when only some of them carry one. | `prop GlyphBadge.children` |
| The 32px events-row badge skene-site keeps a local file for: `size={32}` with `tone="muted"` and `font-mono` through `className` is that component, exactly. | `prop GlyphBadge.size` |
| A quiet initials or code disc — two or three characters, `tone="muted"`. At that point it is a circle with the themed border over `bg-muted` and carries no state at all. | `prop GlyphBadge.tone` |

## Not for — reach for this instead

| instead | why |
| --- | --- |
| `FeatureIcon` | The mark that carries a feature row's identity. `FeatureIcon` is 44px with an accent-tinted border at 47%, a 7px inset ring and a peach/violet/blue union, and it is deliberately not `aria-hidden`. |
| `TrustFact` | A whole fact row — the disc plus a title, a qualifier and the rule under it, as an `<li>`. That is `TrustFact`, which composes this at its defaults. |

## Easy to confuse with

`sections/trust-panel#TrustFact`, `sections/feature-row#FeatureIcon`

These edges are symmetric — the same pairing is recorded from the other side.

## Watch for

| note | via |
| --- | --- |
| `size` is written to `style`, following `ScoreRing`, so a `size-*` / `h-*` / `w-*` utility passed through `className` cannot beat it: inline styles win and `cn` does not merge across the two. Pass the number. | `prop GlyphBadge.size` |
| `tint` is cream-only. Its hairline is `chrome.line.onLight` — the invariant dark rule made for a cream fill — and its wash is a 12% peach mix written inline. On the dark page that border is all but gone; `muted` is the tone for a themed ground. | `default tone='tint'` |
| `aria-hidden` is unconditional and there is no prop to lift it. Every glyph this has held is decoration beside a real title, and announcing "check mark" before the sentence only delays it. If your mark IS the content — an avatar identifying a person — it needs an element that will be read out. | `export GlyphBadge` |
| Nothing scales with `size`. The glyph takes the ambient type size, because a tick and a three-letter city code do not want the same one — which is also why `TrustFact`'s 38px rendering did not move when the disc was extracted. | `prop GlyphBadge.size` |

## Reachable from outside

`style`, `className`

`style` means this module writes an inline style that beats any class you pass.

## Rendered in

Gallery cases with a committed light and dark baseline: `section-glyph-badge`.

## Page templates that carry it

| template | role |
| --- | --- |
| [company-page](../pages/company-page.md) | optional, in 1 of 2 |
| [home-page](../pages/home-page.md) | observed on 1 route |

## What binds this module

- **Polarity `inherits`.** It puts no theme class on its own root, so it takes the page. Place it on a light fill and the `light` class is yours to add, or its mode-aware tokens resolve to their DARK values against that fill — measured at 1.07:1 on rendered pixels.
- **`chrome.*` is invariant.** This module is in the fixed-dark world, so the distinction bites here: `chrome.*` never inverts and belongs only on a surface that never flips. Anything on a surface that follows the theme uses `text.*` / `surface.*`. The two share their dark values, so the wrong pick looks correct until someone opens light mode.
- **Content is props.** This section hardcodes no copy; the page supplies it.

These are the rules this module can break. The full set, the surface roles and
the contrast floors are in [DESIGN.md](../../DESIGN.md).

---

System-wide tokens, scales and the full module index: [DESIGN.md](../../DESIGN.md).
