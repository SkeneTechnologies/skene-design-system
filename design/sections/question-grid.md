# sections/question-grid

<!-- @design:generated -->

_Generated by `scripts/generate-design-md.mjs` from `machine/context.yaml`, `machine/components.yaml`, `machine/compositions.yaml`._
_Do not edit. Change the source and run `npm run design`; `npm run design:check` fails the build if this file is stale._
_@skene/design-system 0.25.0._

|  |  |
| --- | --- |
| import | `@skene/design-system/sections/question-grid` |
| polarity | `inherits` |
| boundary | server-renderable |
| intent | `answer-a-question`, `group-related-items` |

## Use for

A 1–4 column grid of near-empty cards, each one a mono category `tag` sitting at the top of the card with a question (or claim) below it and an optional answer. The card is deliberately a border, a 14px radius and a peach corner glow with no opaque fill — that is what lets it sit on a dark band or a cream one without needing a polarity class or a token audit. It exists for the "here is what you'll actually be able to answer" band, where the copy does all the work and the container must not compete with it.

## Exports

`QuestionGrid`, `QuestionCard`

## Props

| export | prop | type / allowed | required | default |
| --- | --- | --- | --- | --- |
| `QuestionGrid` | `columns` | `keyof typeof COLUMN_CLASS` |  | `3` |
| `QuestionGrid` | `children` | `React.ReactNode` | **yes** |  |
| `QuestionCard` | `tag` | `React.ReactNode` |  |  |
| `QuestionCard` | `title` | `React.ReactNode` | **yes** |  |
| `QuestionCard` | `children` | `React.ReactNode` |  |  |

## Also for

Every claim cites the prop, default or export that makes it true. A claim that cannot cite one is rejected by a test.

| claim | via |
| --- | --- |
| An FAQ or objection-handling teaser band: `tag` as the category ("BILLING", "OBJECTION"), `title` as the question, `children` as the short answer. Static only — see notFor. | `prop QuestionCard.tag` |
| A use-case or persona grid: `tag` as the audience ("FOR ENGINEERS"), `title` as the outcome. Same three slots, no change. | `prop QuestionCard.tag` |
| A 2-up or 4-up feature grid via the `columns` prop. `columns={4}` is not simply four across — the static map gives `grid-cols-1 md:grid-cols-2 lg:grid-cols-4`, which is the sensible ladder for four short cards. | `prop QuestionGrid.columns` |
| A single full-width statement card with `columns={1}` — though the fixed 58px tag gap and `min-h` were tuned for a ~270px-wide card and will read as a lot of air at full bleed. | `prop QuestionGrid.columns` |
| Clickable cards, by wrapping each `QuestionCard` in the caller's own `<a>`/`<Link>`. `className` is forwarded to the `<article>`, so hover/focus styling can be supplied from outside — but the component ships no interactive affordance of its own. | `export QuestionCard` |

## Not for — reach for this instead

| instead | why |
| --- | --- |
| `Accordion` | A real FAQ accordion. There is no disclosure behaviour and no `use client` — this is props in, markup out. Use `ui/accordion`. |
| `StatChip` | A stats or metrics band. The fixed tag gap and min-height are tuned for one line of question plus a sentence; a number and a unit leave the card mostly empty. Use `StatChip` or `ValueCards`. |

## Easy to confuse with

`sections/value-cards#ValueCard`

These edges are symmetric — the same pairing is recorded from the other side.

## Watch for

| note | via |
| --- | --- |
| The `mb-[58px]` under the tag is a fixed margin, not `mt-auto`, and it is load-bearing: it is what makes the tag read as a label on the card rather than a kicker on the sentence, and what keeps every tag on one baseline across the row. It only works because the grid's default `items-stretch` gives the cards a shared row height — drop these into a flex row, or pass `items-start`, and the alignment goes. | `prop QuestionCard.tag` |
| Omitting `tag` removes the 58px with it, so the question floats at the top of a card still held open by `min-h-[230px] md:min-h-[270px]` — a visible void underneath. The card is designed to be tagged. | `prop QuestionCard.tag` |
| The tag colour is hardcoded `text-brand-peach`; there is no per-card accent. Colour-coding categories would need a small prop addition — a `tone`/`accent` prop feeding the tag's colour, mirroring `ValueCard`'s `TONE_ACCENT` map. | `prop QuestionCard.tag` |
| `COLUMN_CLASS` is a static 1\|2\|3\|4 map because Tailwind cannot see an interpolated class name. Five or six columns is a compile error, not a runtime surprise — and fixing it means adding a map entry, not passing a number through. | `prop QuestionGrid.columns` |
| Adding a background fill via `className` works (the corner glow is `backgroundImage`, a different property) but it is exactly the change the file header warns against: a filled card acquires its own ground, at which point it needs a `light`/`dark` class and every token inside it needs re-checking. | `className` |

## Reachable from outside

`style`, `className`

`style` means this module writes an inline style that beats any class you pass.

## Custom properties

`--font-size-pill`, `--font-tracking-eyebrow`

## Rendered in

Gallery cases with a committed light and dark baseline: `section-question-grid`.

## Page templates that carry it

| template | role |
| --- | --- |
| [home-page](../pages/home-page.md) | observed on 1 route |

## What binds this module

- **Polarity `inherits`.** It puts no theme class on its own root, so it takes the page. Place it on a light fill and the `light` class is yours to add, or its mode-aware tokens resolve to their DARK values against that fill — measured at 1.07:1 on rendered pixels.
- **Content is props.** This section hardcodes no copy; the page supplies it.

These are the rules this module can break. The full set, the surface roles and
the contrast floors are in [DESIGN.md](../../DESIGN.md).

---

System-wide tokens, scales and the full module index: [DESIGN.md](../../DESIGN.md).
