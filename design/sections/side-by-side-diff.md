# sections/side-by-side-diff

<!-- @design:generated -->

_Generated by `scripts/generate-design-md.mjs` from `machine/context.yaml`, `machine/components.yaml`, `machine/compositions.yaml`._
_Do not edit. Change the source and run `npm run design`; `npm run design:check` fails the build if this file is stale._
_@skene/design-system 0.18.0._

|  |  |
| --- | --- |
| import | `@skene/design-system/sections/side-by-side-diff` |
| polarity | `applies-dark` |
| boundary | server-renderable |
| intent | `display-code`, `compare-options` |

## Use for

Two columns of code with added and removed lines marked — what was written beside what was expected.

## Exports

`DiffLine`, `DiffColumn`, `SideBySideDiff`

## Props

| export | prop | type / allowed | required | default |
| --- | --- | --- | --- | --- |
| `DiffLine` | `kind` | `DiffLineKind` |  | `'ctx'` |
| `DiffLine` | `addedLabel` | `React.ReactNode` |  | `'Added'` |
| `DiffLine` | `removedLabel` | `React.ReactNode` |  | `'Removed'` |
| `DiffLine` | `children` | `React.ReactNode` |  |  |
| `DiffColumn` | `side` | `DiffSide` | **yes** |  |
| `DiffColumn` | `label` | `React.ReactNode` | **yes** |  |
| `DiffColumn` | `lines` | `DiffLineSpec[]` | **yes** |  |
| `DiffColumn` | `addedLabel` | `React.ReactNode` |  |  |
| `DiffColumn` | `removedLabel` | `React.ReactNode` |  |  |
| `SideBySideDiff` | `children` | `React.ReactNode` | **yes** |  |

## Types — not components

These are TypeScript types, not exports you can render. `<DiffLineSpec />` is not a component; the type describes the shape of a value you pass to one of the exports above. The name usually reads like a component, which is why this heading says so.

| type | shape |
| --- | --- |
| `DiffLineSpec` | `kind`, `text` |

## Also for

Every claim cites the prop, default or export that makes it true. A claim that cannot cite one is rejected by a test.

| claim | via |
| --- | --- |
| Any before/after code comparison — a migration, a suggested fix, a config change. | `prop DiffColumn.lines` |
| A single column of annotated code, by rendering one DiffColumn. | `prop DiffColumn.side` |
| Localised or relabelled markers via `addedLabel` and `removedLabel`. | `prop DiffColumn.addedLabel` |

## Not for — reach for this instead

| instead | why |
| --- | --- |
| `PrReview` | The review around a diff — author, findings, fix. That is PrReview. |
| `McpCode` | A plain code block with no diff semantics. That is McpCode. |

## Watch for

| note | via |
| --- | --- |
| It applies `dark` to itself: the diff is GitHub chrome and stays dark on a light page, which is what the vendor tokens are for. | `export SideBySideDiff` |
| Its column header is byte-identical to ArtPanel's bar, in a file that does not import it. | `prop DiffColumn.label` |

## Reachable from outside

`style`, `className`

`style` means this module writes an inline style that beats any class you pass.

## Rendered in

Gallery cases with a committed light and dark baseline: `section-side-by-side-diff`.

## Page templates that carry it

| template | role |
| --- | --- |
| [developer-page](../pages/developer-page.md) | optional, in 1 of 2 |

## What binds this module

- **Polarity `applies-dark`.** It puts `dark` on its own root — a dark subtree wherever you place it, including inside a light surface.
- **`chrome.*` is invariant.** This module is in the fixed-dark world, so the distinction bites here: `chrome.*` never inverts and belongs only on a surface that never flips. Anything on a surface that follows the theme uses `text.*` / `surface.*`. The two share their dark values, so the wrong pick looks correct until someone opens light mode.
- **Content is props.** This section hardcodes no copy; the page supplies it.

These are the rules this module can break. The full set, the surface roles and
the contrast floors are in [DESIGN.md](../../DESIGN.md).

---

System-wide tokens, scales and the full module index: [DESIGN.md](../../DESIGN.md).
