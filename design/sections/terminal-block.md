# sections/terminal-block

<!-- @design:generated -->

_Generated by `scripts/generate-design-md.mjs` from `machine/context.yaml`, `machine/components.yaml`, `machine/compositions.yaml`._
_Do not edit. Change the source and run `npm run design`; `npm run design:check` fails the build if this file is stale._
_@skene/design-system 0.15.0._

|  |  |
| --- | --- |
| import | `@skene/design-system/sections/terminal-block` |
| polarity | `applies-dark` |
| boundary | `"use client"` — client component |
| intent | `display-code`, `drive-an-action` |

## Use for

The install block: a terminal frame holding one or more shell lines, each with its own copy button.

## Exports

`TerminalBlock`

## Props

| export | prop | type | required | default |
| --- | --- | --- | --- | --- |
| `TerminalBlock` | `lines` | `TerminalBlockLine[]` | yes |  |
| `TerminalBlock` | `title` | `React.ReactNode` |  |  |
| `TerminalBlock` | `note` | `React.ReactNode` |  |  |
| `TerminalBlock` | `copyLabel` | `React.ReactNode` |  | `'copy'` |
| `TerminalBlock` | `copiedLabel` | `React.ReactNode` |  | `'copied'` |
| `TerminalBlock` | `onCopy` | `(result: { command: string; index: number; ok: boolean }) => void` |  |  |

## Types

| type | shape |
| --- | --- |
| `TerminalBlockLine` | `command`, `display`, `prompt`, `copyable`, `wrap` |

## Also for

Every claim cites the prop, default or export that makes it true. A claim that cannot cite one is rejected by a test.

| claim | via |
| --- | --- |
| Any copyable command list — install steps, a CLI walkthrough, a set of environment exports. | `prop TerminalBlock.lines` |
| Output lines that are not copyable, by setting `copyable` false — which is how a transcript mixes commands and results. | `type TerminalBlockLine.copyable` |
| A different visible string from what is copied, via `display` against `command`. | `type TerminalBlockLine.display` |

## Not for — reach for this instead

| instead | why |
| --- | --- |
| `Terminal` | A transcript with no copy affordance. That is patterns/terminal. |
| `McpCode` | A config block to read rather than run. That is McpCode. |

## Easy to confuse with

`patterns/terminal#Terminal`, `sections/traffic-lights#TrafficLights`

These edges are symmetric — the same pairing is recorded from the other side.

## Watch for

| note | via |
| --- | --- |
| It is the client boundary and is deliberately absent from the root barrel: a use client directive on a re-exported module poisons the barrel for server rendering, which package-contract asserts against. Deep-import it. | `use client` |
| It draws the terminal frame itself rather than using the skene-terminal class in effects.css, so it does not match patterns/terminal — 12px radius against 8px. | `prop TerminalBlock.title` |

## Reachable from outside

`className`, `use client`

## Composes

`ui/button`, `same-layer/artifact-shell`, `same-layer/traffic-lights`

## Rendered in

Gallery cases with a committed light and dark baseline: `section-terminal-block`.

## Page templates that carry it

| template | role |
| --- | --- |
| [developer-page](../pages/developer-page.md) | load-bearing |

## Rules that are not negotiable

1. **`chrome.*` is invariant and cannot invert.** `color.chrome.surface.*` and
   `color.chrome.text.*` are always dark. Use them only on a surface that never
   flips — a terminal, a log panel, a code frame. Anything on a surface that
   flips uses the theme-aware `color.text.*` / `color.surface.*`. The two share
   their dark values and diverge only in light, so the wrong pick looks correct
   until someone opens light mode.
2. **A light surface on a dark page needs the `light` class on its own root.**
   Without it the mode-aware tokens resolve to their dark values against a light
   fill. That has shipped text at 1.08:1. Check the module's `polarity` first —
   `applies-light` means the module already does this for you.
3. **Content is props.** No section hardcodes copy.

Theming is class-based and the package ships base mode `dark`. Contrast floors: body text 4.5:1, large text 3:1 (WCAG 2.2).

---

System-wide tokens, scales and the full module index: [DESIGN.md](../../DESIGN.md).
