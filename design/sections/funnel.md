# sections/funnel

<!-- @design:generated -->

_Generated by `scripts/generate-design-md.mjs` from `machine/context.yaml`, `machine/components.yaml`, `machine/compositions.yaml`._
_Do not edit. Change the source and run `npm run design`; `npm run design:check` fails the build if this file is stale._
_@skene/design-system 0.15.0._

|  |  |
| --- | --- |
| import | `@skene/design-system/sections/funnel` |
| polarity | `inherits` |
| boundary | server-renderable |
| intent | `show-a-metric`, `show-status` |

## Use for

The activation funnel as the reader's own dashboard would draw it: one bar per step, and three states because a broken number is not a low number.

## Exports

`FunnelRow`, `Funnel`

## Props

| export | prop | type | required | default |
| --- | --- | --- | --- | --- |
| `FunnelRow` | `label` | `React.ReactNode` | yes |  |
| `FunnelRow` | `note` | `React.ReactNode` |  |  |
| `FunnelRow` | `value` | `React.ReactNode` |  |  |
| `FunnelRow` | `state` | `FunnelStepState` | yes |  |
| `FunnelRow` | `fill` | `number` |  |  |
| `Funnel` | `title` | `React.ReactNode` | yes |  |
| `Funnel` | `badge` | `React.ReactNode` |  |  |
| `Funnel` | `meta` | `React.ReactNode` |  |  |
| `Funnel` | `status` | `FunnelStepState` |  | `'ok'` |
| `Funnel` | `children` | `React.ReactNode` |  |  |

## Also for

Every claim cites the prop, default or export that makes it true. A claim that cannot cite one is rejected by a test.

| claim | via |
| --- | --- |
| Any staged conversion chart where some steps are untrustworthy — onboarding, checkout, a signup path. | `prop FunnelRow.state` |
| A plain proportional bar chart, by leaving every row `ok` and passing `fill`. | `prop FunnelRow.fill` |

## Not for — reach for this instead

| instead | why |
| --- | --- |
| `MiniFunnel` | A small label/value/bar readout beside a journey track. That is MiniFunnel. |
| `Progress` | A determinate control-surface bar. That is ui/progress. |

## Watch for

| note | via |
| --- | --- |
| `broken` renders a hatch, not a short solid bar, and `unknown` renders an empty dashed track with no fill element at all. A zero bar would be a lie, which is why the value cell says not measured instead of 0. | `prop FunnelRow.state` |
| It is built on the light app window on purpose: this chart is the reader's own tool, not a Skene screen. Giving it a Skene crumb breaks the argument. | `prop Funnel.title` |

## Reachable from outside

`style`, `className`

`style` means this module writes an inline style that beats any class you pass.

## Custom properties

`--muted-foreground`

## Composes

`same-layer/artifact-shell`

## Rendered in

Gallery cases with a committed light and dark baseline: `section-funnel`.

## Page templates that carry it

| template | role |
| --- | --- |
| [use-case-page](../pages/use-case-page.md) | optional, in 1 of 3 |
| [home-page](../pages/home-page.md) | observed on 1 route |

## Rules that are not negotiable

1. **`chrome.*` is invariant and cannot invert.** `color.chrome.surface.*` and
   `color.chrome.text.*` are always dark. Use them only on a surface that never
   flips — a terminal, a log panel, a code frame. Anything on a surface that
   flips uses the theme-aware `color.text.*` / `color.surface.*`. The two share
   their dark values and diverge only in light, so the wrong pick looks correct
   until someone opens light mode.
2. **A light surface on a dark page needs the `light` class on its own root.**
   Without it the mode-aware tokens resolve to their dark values against a light
   fill. That has shipped text at 1.08:1. Check the module's `polarity` first —
   `applies-light` means the module already does this for you.
3. **Content is props.** No section hardcodes copy.

Theming is class-based and the package ships base mode `dark`. Contrast floors: body text 4.5:1, large text 3:1 (WCAG 2.2).

---

System-wide tokens, scales and the full module index: [DESIGN.md](../../DESIGN.md).
