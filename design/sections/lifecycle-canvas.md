# sections/lifecycle-canvas

<!-- @design:generated -->

_Generated by `scripts/generate-design-md.mjs` from `machine/context.yaml`, `machine/components.yaml`, `machine/compositions.yaml`._
_Do not edit. Change the source and run `npm run design`; `npm run design:check` fails the build if this file is stale._
_@skene/design-system 0.21.0._

|  |  |
| --- | --- |
| import | `@skene/design-system/sections/lifecycle-canvas` |
| polarity | `inherits` |
| boundary | server-renderable |
| intent | `explain-a-process`, `group-related-items` |

## Use for

The lifecycle artifact: stages across the canvas, each holding milestones and the signals bound to them.

## Exports

`LifecycleCanvas`

## Props

| export | prop | type / allowed | required | default |
| --- | --- | --- | --- | --- |
| `LifecycleCanvas` | `stages` | `LifecycleStageItem[]` | **yes** |  |
| `LifecycleCanvas` | `title` | `React.ReactNode` |  |  |
| `LifecycleCanvas` | `source` | `React.ReactNode` |  |  |
| `LifecycleCanvas` | `separator` | `React.ReactNode` |  | `'/'` |
| `LifecycleCanvas` | `summary` | `React.ReactNode` |  |  |
| `LifecycleCanvas` | `summaryStatus` | `StatPillStatus` |  | `'ok'` |
| `LifecycleCanvas` | `actions` | `React.ReactNode` |  |  |

## Types — not components

These are TypeScript types, not exports you can render. `<LifecycleMilestoneItem />` is not a component; the type describes the shape of a value you pass to one of the exports above. The name usually reads like a component, which is why this heading says so.

| type | shape |
| --- | --- |
| `LifecycleMilestoneItem` | `name`, `description`, `bindings` |
| `LifecycleStageItem` | `key`, `name`, `description`, `milestones` |

## Also for

Every claim cites the prop, default or export that makes it true. A claim that cannot cite one is rejected by a test.

| claim | via |
| --- | --- |
| Any two-level map — phases and their steps, epics and their stories, a journey and its moments. | `type LifecycleStageItem.milestones` |
| A single-level row of stages, by omitting milestones entirely. | `type LifecycleStageItem.description` |
| Binding names as chips, since `bindings` is a list of nodes. | `type LifecycleMilestoneItem.bindings` |

## Not for — reach for this instead

| instead | why |
| --- | --- |
| `JourneyStep` | A single line of steps with a state-carrying connector. That is JourneyTrack. |
| `FlowNode` | Nodes joined by arrows with per-edge values. That is FlowDiagram. |

## Watch for

| note | via |
| --- | --- |
| It scrolls horizontally with a 190px column floor rather than wrapping, so a long lifecycle stays one canvas. | `prop LifecycleCanvas.stages` |

## Composes

`same-layer/artifact-shell`, `same-layer/key-value-table`

## Rendered in

Gallery cases with a committed light and dark baseline: `section-lifecycle-canvas`.

## Page templates that carry it

| template | role |
| --- | --- |
| [use-case-page](../pages/use-case-page.md) | optional, in 2 of 3 |

## What binds this module

- **Polarity `inherits`.** It puts no theme class on its own root, so it takes the page. Place it on a light fill and the `light` class is yours to add, or its mode-aware tokens resolve to their DARK values against that fill — measured at 1.07:1 on rendered pixels.
- **Content is props.** This section hardcodes no copy; the page supplies it.

These are the rules this module can break. The full set, the surface roles and
the contrast floors are in [DESIGN.md](../../DESIGN.md).

---

System-wide tokens, scales and the full module index: [DESIGN.md](../../DESIGN.md).
