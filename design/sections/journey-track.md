# sections/journey-track

<!-- @design:generated -->

_Generated by `scripts/generate-design-md.mjs` from `machine/context.yaml`, `machine/components.yaml`, `machine/compositions.yaml`._
_Do not edit. Change the source and run `npm run design`; `npm run design:check` fails the build if this file is stale._
_@skene/design-system 0.15.0._

|  |  |
| --- | --- |
| import | `@skene/design-system/sections/journey-track` |
| polarity | `inherits` |
| boundary | server-renderable |
| intent | `explain-a-process`, `show-status` |

## Use for

The numbered steps of a funnel on one line, where the connector between two steps is a gradient running from one step's status colour to the next's — the seam carries the state, not just the nodes. That is the section's argument, not a flourish: breakage happens *in the link* (an event that stops firing between checkout and confirmation), and the eye should find it before reading a label. The module also exports `MiniFunnel`, the small authored label/value/bar readout that usually sits beside the track.

## Exports

`JourneyStep`, `JourneyTrack`, `MiniFunnel`

## Props

| export | prop | type / allowed | required | default |
| --- | --- | --- | --- | --- |
| `JourneyTrack` | `steps` | `JourneyStepItem[]` | **yes** |  |
| `JourneyTrack` | `title` | `React.ReactNode` |  |  |
| `JourneyTrack` | `subtitle` | `React.ReactNode` |  |  |
| `MiniFunnel` | `rows` | `MiniFunnelRowItem[]` | **yes** |  |

## Types — not components

These are TypeScript types, not exports you can render. `<JourneyStepItem />` is not a component; the type describes the shape of a value you pass to one of the exports above. The name usually reads like a component, which is why this heading says so.

| type | shape |
| --- | --- |
| `JourneyStepItem` | `label`, `note`, `state`, `glyph` |
| `MiniFunnelRowItem` | `label`, `value`, `fill` |

## Also for

Every claim cites the prop, default or export that makes it true. A claim that cannot cite one is rejected by a test.

| claim | via |
| --- | --- |
| Any three-state pipeline, not just a purchase funnel: onboarding checklists, deploy stages, data-source health. States are the fixed `good \| warn \| danger` triple bound to matcha/amber/red — the same vocabulary `Finding` uses — and `note` is free prose, so the domain lives entirely in the caller's copy. | `type JourneyStepItem.note` |
| Building your own layout from `JourneyStep`, which is exported separately. Its `index` prop is a `ReactNode` and `JourneyTrack` merely fills in the 1-based position, so rendering steps directly lets you pass a glyph (✓, ✕, an icon) or your own numbering into the ring. | `export JourneyStep` |
| Both orientations with no prop: the track is a stacked column below `md` and a row from `md` up, and the connector's gradient angle rides a `--journey-connector-angle` custom property specifically because an inline style cannot hold a media query. You get vertical and horizontal for free — but only that pairing; forcing one orientation means overriding classes. | `inline style` |
| Server rendering and animated composition. There is no `use client` and no internal state — state arrives as props — so a track that advances composes by re-rendering, and it drops into a server component without pulling a client boundary around its siblings. | `type JourneyStepItem.state` |
| `MiniFunnel` on its own, anywhere a small ranked readout helps: quota usage, share-of-total, step drop-off. Its bar width is routed through the `--mini-funnel-fill` custom property rather than set on `width` directly, which is the deliberate seam for a wrapper to transition or override it (grow-on-scroll, staged entrance) without this component owning any animation or state. | `export MiniFunnel` |
| A verified or annotated track without composing steps by hand: each step's `glyph` replaces the ring's 1-based number (a ✓ on a proven step, per step, so a track can mix glyphed and numbered rings) while `JourneyTrack` still owns the connectors — the seam gradients keep deriving from the states with no re-derivation at the call site. | `type JourneyStepItem.glyph` |

## Not for — reach for this instead

| instead | why |
| --- | --- |
| `MiniFunnel` | A measured funnel chart. `MiniFunnel` has no axis and no scale, the widths are authored rather than computed, and its fill is `brand.peach` on purpose so quantities never borrow the status triple. For a shape-at-a-glance trend use `Sparkline`; for real numbers use a chart. |
| `PipelineStepper` | A progress indicator for a running job, or an interactive wizard. `PipelineStepper` owns the `done \| active \| pending` vocabulary and emphasises the active step; `JourneyTrack` has no active state, no state at all, and no click handling. |

## Easy to confuse with

`sections/pipeline-stepper#PipelineStepper`

These edges are symmetric — the same pairing is recorded from the other side.

## Watch for

| note | via |
| --- | --- |
| The 34px ring size is hard-coded into the connector, not read from it: `ml-[17px]` stacked and `md:mt-[17px]` in a row are both half the ring. Resizing the ring via `JourneyStep`'s `className` leaves the connector aligned to the old centre. | `export JourneyStep` |
| This section owns no ground and carries no `light` / `dark` class of its own by design — every colour is a mode-aware role or derived from one, and nothing uses `chrome.text.*`. A caller putting it on a cream fill inside a dark page must put `light` on that band (rule 2), or `semantic.matcha` resolves to its dark value at 1.16:1 on cream. | `type MiniFunnelRowItem.value` |
| The ring is `aria-hidden` and the label stays `text.primary` in *every* state — that is the rule the section exists to keep, not an oversight. So `note` is the only place the state is both announced and readable; a caller who omits it ships a broken step that only sighted readers can identify. | `type JourneyStepItem.note` |
| Two intentional non-obviousnesses that look like bugs: a `warn` end fades its connector to `surface.border` grey rather than contributing amber (an unmeasured step has no claim to make about the link), and `MiniFunnel`'s bars are `aria-hidden`, authored, and clamped 0-100 independently of `value` — the bar and the figure can disagree if the caller lets them. | `export MiniFunnel` |

## Reachable from outside

`style`, `className`

`style` means this module writes an inline style that beats any class you pass.

## Custom properties

`--journey-connector-angle`, `--mini-funnel-fill`

## Composes

`lib/status`

## Rendered in

Gallery cases with a committed light and dark baseline: `section-journey-track`.

## Page templates that carry it

| template | role |
| --- | --- |
| [product-page](../pages/product-page.md) | optional, in 1 of 4 |
| [use-case-page](../pages/use-case-page.md) | optional, in 1 of 3 |

## Rules that are not negotiable

1. **`chrome.*` is invariant and cannot invert.** `color.chrome.surface.*` and
   `color.chrome.text.*` are always dark. Use them only on a surface that never
   flips — a terminal, a log panel, a code frame. Anything on a surface that
   flips uses the theme-aware `color.text.*` / `color.surface.*`. The two share
   their dark values and diverge only in light, so the wrong pick looks correct
   until someone opens light mode.
2. **A light surface on a dark page needs the `light` class on its own root.**
   Without it the mode-aware tokens resolve to their dark values against a light
   fill. That has shipped text at 1.08:1. Check the module's `polarity` first —
   `applies-light` means the module already does this for you.
3. **Content is props.** No section hardcodes copy.

Theming is class-based and the package ships base mode `dark`. Contrast floors: body text 4.5:1, large text 3:1 (WCAG 2.2).

---

System-wide tokens, scales and the full module index: [DESIGN.md](../../DESIGN.md).
