# sections/plan-card

<!-- @design:generated -->

_Generated by `scripts/generate-design-md.mjs` from `machine/context.yaml`, `machine/components.yaml`, `machine/compositions.yaml`._
_Do not edit. Change the source and run `npm run design`; `npm run design:check` fails the build if this file is stale._
_@skene/design-system 0.22.0._

|  |  |
| --- | --- |
| import | `@skene/design-system/sections/plan-card` |
| polarity | `applies-both` |
| boundary | server-renderable |
| intent | `compare-options`, `group-related-items` |

## Use for

A pricing tier card plus the grid that holds a row of them (`PlanGrid` + `PlanCard`). It is pure markup — no state, no client directive — because the only interactive part of a pricing section, the billing switch, was deliberately split into `billing-toggle.tsx` so this file stays server-renderable. The shape's real argument is comparison-by-inversion: `featured` flips one card to cream-on-dark, lifts it `md:-translate-y-3`, and drops a `--shadow-modal` on it, so a row of peers stops reading as peers and one of them becomes the recommendation.

## Exports

`PlanGrid`, `PlanCard`

## Props

| export | prop | type / allowed | required | default |
| --- | --- | --- | --- | --- |
| `PlanGrid` | `children` | `React.ReactNode` | **yes** |  |
| `PlanCard` | `tier` | `React.ReactNode` | **yes** |  |
| `PlanCard` | `tierAs` | `'h2' \| 'h3'` |  |  |
| `PlanCard` | `flag` | `React.ReactNode` |  |  |
| `PlanCard` | `price` | `React.ReactNode` | **yes** |  |
| `PlanCard` | `unit` | `React.ReactNode` |  |  |
| `PlanCard` | `summary` | `React.ReactNode` |  |  |
| `PlanCard` | `features` | `React.ReactNode` |  |  |
| `PlanCard` | `bestFor` | `{ label: React.ReactNode; value: React.ReactNode }` |  |  |
| `PlanCard` | `action` | `React.ReactNode` |  |  |
| `PlanCard` | `footnote` | `React.ReactNode` |  |  |
| `PlanCard` | `featured` | `boolean` |  | `false` |
| `PlanCard` | `featuredTone` | `'light' \| 'dark'` |  | `'light'` |

## Also for

Every claim cites the prop, default or export that makes it true. A claim that cannot cite one is rejected by a test.

| claim | via |
| --- | --- |
| A billing/subscription panel in a product surface: `flag="Current plan"`, `bestFor={{label: 'Renews', value: 'Sep 12'}}` as a generic labelled metadata block, `action` as a Manage button, `footnote` for the fine print. | `prop PlanCard.flag` |
| Non-currency tiers. `price` is a `ReactNode` rendered in a `<strong>`, so "Free", "Custom", or "Let's talk" work as-is, and `unit` takes "/seat", "/yr", "per repo" — nothing in the component knows it is money. | `prop PlanCard.price` |
| Non-pricing package or offer cards — service tiers, plan-shaped feature bundles. `features` is an untyped `ReactNode` slot, so a `<dl>`, a `<Table>`, or plain paragraphs go in where the demo puts a `<CheckList>`. | `prop PlanCard.features` |
| Grids other than three-across. `PlanGrid` merges `className` through `cn` (clsx + tailwind-merge), so `md:grid-cols-2` or `lg:grid-cols-4` genuinely replaces the base `md:grid-cols-3` rather than fighting it. | `export PlanGrid` |
| Highlighting one item in any set of sibling cards — `featured` carries no pricing semantics, only the cream inversion and the lift. | `prop PlanCard.featured` |
| The featured promotion on a CREAM ground: `featuredTone="dark"` inverts the card to the invariant near-black fill with the `dark` class pinning every mode-aware token in the subtree, same lift and `--shadow-modal` — because the default cream inversion is cream-on-cream inside a light panel. A nested `CheckList` then wants its default rendering, not `onLight`. | `prop PlanCard.featuredTone` |

## Not for — reach for this instead

| instead | why |
| --- | --- |
| `ComparisonTable` | A feature-by-feature comparison matrix across tiers — the card has no cell/row model. Use `sections/comparison-table` (`ComparisonTable`, `ComparisonRow`, `TableCheck`, `TableDash`); it is a deep import, not in the root barrel. |
| `LightSectionCard` | A generic content card or dashboard tile. The tier chip, oversized price and 420px floor are all pricing furniture — use `ui/card` for product surfaces, or `LightSectionCard` for a cream editorial panel. |

## Easy to confuse with

`sections/bridge#BridgeNode`

These edges are symmetric — the same pairing is recorded from the other side.

## Watch for

| note | via |
| --- | --- |
| `featured` applies the `light` class, and that is load-bearing, not decoration (sections.md rule 2). Anything you pass into `features`/`action`/`bestFor` must use the theme-aware `text.*` role; `chrome.text.*` is invariant and renders cream on the cream card, i.e. invisible. When `featured`, also pass `onLight` to a nested `CheckList`. | `prop PlanCard.featured` |
| The tier marker is hardcoded `<Chip tone="neutral">` — no prop reaches it. A healthy/live-toned tier marker needs one small prop addition (e.g. `tierTone?: ChipTone` forwarded to `Chip`). | `prop PlanCard.tier` |
| The featured shadow is an inline `style={{ boxShadow: 'var(--shadow-modal)' }}`, so a `shadow-*` utility in `className` loses to it. Same for `bestFor`'s border colour, set inline from `--color-chrome-line-on-light`. | `prop PlanCard.bestFor` |
| `PlanGrid` uses `items-start` on purpose: a stretched grid would re-equalise row height and cancel the featured card's `-translate-y-3`. Overriding to `items-stretch` silently kills the lift. Cards are held level by `min-h-[420px]` instead. | `export PlanGrid` |
| The price renders at `clamp(2.55rem, 4vw, 4rem)`. Long strings ("Contact sales", "$1,299/mo") wrap or crowd the `unit` at that size. | `prop PlanCard.unit` |

## Reachable from outside

`style`, `className`

`style` means this module writes an inline style that beats any class you pass.

## Custom properties

`--shadow-modal`

## Composes

`same-layer/chip`

## Rendered in

Gallery cases with a committed light and dark baseline: `section-plan-card`.

## Page templates that carry it

| template | role |
| --- | --- |
| [home-page](../pages/home-page.md) | observed on 1 route |
| [pricing-page](../pages/pricing-page.md) | observed on 1 route |

## What binds this module

- **Polarity `applies-both`.** It has both forms and applies one of them; which is a call-site decision. Read Constraints above before placing it on a ground that flips.
- **`chrome.*` is invariant.** This module is in the fixed-dark world, so the distinction bites here: `chrome.*` never inverts and belongs only on a surface that never flips. Anything on a surface that follows the theme uses `text.*` / `surface.*`. The two share their dark values, so the wrong pick looks correct until someone opens light mode.
- **Content is props.** This section hardcodes no copy; the page supplies it.

These are the rules this module can break. The full set, the surface roles and
the contrast floors are in [DESIGN.md](../../DESIGN.md).

---

System-wide tokens, scales and the full module index: [DESIGN.md](../../DESIGN.md).
