# sections/code

<!-- @design:generated -->

_Generated by `scripts/generate-design-md.mjs` from `machine/context.yaml`, `machine/components.yaml`, `machine/compositions.yaml`._
_Do not edit. Change the source and run `npm run design`; `npm run design:check` fails the build if this file is stale._
_@skene/design-system 0.22.0._

|  |  |
| --- | --- |
| import | `@skene/design-system/sections/code` |
| polarity | `applies-both` |
| boundary | server-renderable |
| intent | `display-code`, `mark-a-label` |

## Use for

The inline identifier chip inside a sentence: an event name, a column, a flag, a path. Extracted from SEVEN copies — six byte-identical component declarations across skene-site route files, verified by comparing the emitted class string, plus a seventh spelling as `PROSE_CODE`, the same recipe as a descendant selector for prose the author cannot reach element by element. The oldest open entry on that repository's gap list, on a site where an event name appears in nearly every paragraph of body copy.

## Exports

`Code`, `PROSE_CODE`

## Props

| export | prop | type / allowed | required | default |
| --- | --- | --- | --- | --- |
| `Code` | `children` | `React.ReactNode` | **yes** |  |
| `Code` | `onLight` | `boolean` |  |  |

## Also for

Every claim cites the prop, default or export that makes it true. A claim that cannot cite one is rejected by a test.

| claim | via |
| --- | --- |
| Prose the caller does not author element by element — MDX, a table cell, a body rendered from a string. Applied to the CONTAINING block rather than each mark, because there is no `<code>` to wrap. Kept as a second mechanism deliberately: merging it into the component would leave that case with no answer. | `PROSE_CODE` |

## Not for — reach for this instead

| instead | why |
| --- | --- |
| `TerminalBlock` | Anything block-level. `TerminalBlock` renders a command as its own surface; this mark sits INSIDE running text at body size, and takes the body contrast floor rather than the large-text one because of it. |
| `Chip` | A status or identity marker beside text. `Chip` is padded and sized to sit NEXT TO a sentence at 10px mono; this sits in one at body size. Reaching for `Chip` here produces pressure to make its geometry variable, which is the one thing it exists to prevent. |
| `Badge` | A theme-neutral status marker. `Badge` is shadcn's and neutral by construction; this carries brand ink on a brand surface, which is why it is a section rather than a primitive. |

## Reachable from outside

`className`

## Custom properties

`--font-size-body`

## Rendered in

Gallery cases with a committed light and dark baseline: `section-code`.

## Page templates that carry it

| template | role |
| --- | --- |
| [product-page](../pages/product-page.md) | optional, in 1 of 4 |
| [use-case-page](../pages/use-case-page.md) | optional, in 2 of 3 |
| [developer-page](../pages/developer-page.md) | load-bearing |
| [community-page](../pages/community-page.md) | optional, in 1 of 2 |
| [capability-deep-dive](../pages/capability-deep-dive.md) | observed on 1 route |

## What binds this module

- **Polarity `applies-both`.** It has both forms and applies one of them; which is a call-site decision. Read Constraints above before placing it on a ground that flips.
- **Content is props.** This section hardcodes no copy; the page supplies it.

These are the rules this module can break. The full set, the surface roles and
the contrast floors are in [DESIGN.md](../../DESIGN.md).

---

System-wide tokens, scales and the full module index: [DESIGN.md](../../DESIGN.md).
