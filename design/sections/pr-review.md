# sections/pr-review

<!-- @design:generated -->

_Generated by `scripts/generate-design-md.mjs` from `machine/context.yaml`, `machine/components.yaml`, `machine/compositions.yaml`._
_Do not edit. Change the source and run `npm run design`; `npm run design:check` fails the build if this file is stale._
_@skene/design-system 0.20.0._

|  |  |
| --- | --- |
| import | `@skene/design-system/sections/pr-review` |
| polarity | `inherits` |
| boundary | server-renderable |
| intent | `show-status`, `display-code` |

## Use for

The pull-request artifact: Skene's review on a PR, with findings by severity, a suggested fix and the file it lands in.

## Exports

`PrReview`

## Props

| export | prop | type / allowed | required | default |
| --- | --- | --- | --- | --- |
| `PrReview` | `status` | `PrReviewStatus` | **yes** |  |
| `PrReview` | `repo` | `React.ReactNode` |  |  |
| `PrReview` | `statusLabel` | `React.ReactNode` |  |  |
| `PrReview` | `avatar` | `React.ReactNode` |  |  |
| `PrReview` | `author` | `React.ReactNode` |  |  |
| `PrReview` | `badge` | `React.ReactNode` |  |  |
| `PrReview` | `action` | `React.ReactNode` |  |  |
| `PrReview` | `title` | `React.ReactNode` |  |  |
| `PrReview` | `summary` | `React.ReactNode` |  |  |
| `PrReview` | `issuesLabel` | `React.ReactNode` |  |  |
| `PrReview` | `issues` | `PrReviewIssue[]` |  |  |
| `PrReview` | `fixLabel` | `React.ReactNode` |  |  |
| `PrReview` | `fix` | `React.ReactNode` |  |  |
| `PrReview` | `file` | `React.ReactNode` |  |  |
| `PrReview` | `fileNote` | `React.ReactNode` |  |  |

## Types — not components

These are TypeScript types, not exports you can render. `<PrReviewIssue />` is not a component; the type describes the shape of a value you pass to one of the exports above. The name usually reads like a component, which is why this heading says so.

| type | shape |
| --- | --- |
| `PrReviewIssue` | `severity`, `text` |

## Also for

Every claim cites the prop, default or export that makes it true. A claim that cannot cite one is rejected by a test.

| claim | via |
| --- | --- |
| Any review or audit output with ranked findings — a linter run, a security scan, a code review. | `prop PrReview.issues` |
| A passing review with no findings at all, via `status` and an empty issues list. | `prop PrReview.status` |

## Not for — reach for this instead

| instead | why |
| --- | --- |
| `SideBySideDiff` | The diff itself rather than the review around it. That is SideBySideDiff. |
| `Finding` | A single measured finding on a marketing band. That is Finding. |

## Watch for

| note | via |
| --- | --- |
| Severity maps high/medium/low onto the same three semantic colours as every other status vocabulary in the package, under a fourth set of names. | `type PrReviewIssue.severity` |
| Its author badge is StatPill minus the dot, hand-rolled in a file that already imports StatPill eight lines earlier. | `prop PrReview.badge` |

## Reachable from outside

`style`, `className`

`style` means this module writes an inline style that beats any class you pass.

## Composes

`lib/status`, `same-layer/artifact-shell`, `same-layer/traffic-lights`

## Rendered in

Gallery cases with a committed light and dark baseline: `section-pr-review`.

## Page templates that carry it

| template | role |
| --- | --- |
| [product-page](../pages/product-page.md) | optional, in 1 of 4 |
| [use-case-page](../pages/use-case-page.md) | optional, in 1 of 3 |
| [developer-page](../pages/developer-page.md) | optional, in 1 of 2 |
| [home-page](../pages/home-page.md) | observed on 1 route |

## What binds this module

- **Polarity `inherits`.** It puts no theme class on its own root, so it takes the page. Place it on a light fill and the `light` class is yours to add, or its mode-aware tokens resolve to their DARK values against that fill — measured at 1.07:1 on rendered pixels.
- **Content is props.** This section hardcodes no copy; the page supplies it.

These are the rules this module can break. The full set, the surface roles and
the contrast floors are in [DESIGN.md](../../DESIGN.md).

---

System-wide tokens, scales and the full module index: [DESIGN.md](../../DESIGN.md).
