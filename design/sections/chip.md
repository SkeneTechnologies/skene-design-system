# sections/chip

<!-- @design:generated -->

_Generated by `scripts/generate-design-md.mjs` from `machine/context.yaml`, `machine/components.yaml`, `machine/compositions.yaml`._
_Do not edit. Change the source and run `npm run design`; `npm run design:check` fails the build if this file is stale._
_@skene/design-system 0.22.0._

|  |  |
| --- | --- |
| import | `@skene/design-system/sections/chip` |
| polarity | `inherits` |
| boundary | server-renderable |
| intent | `mark-a-label`, `show-status` |

## Use for

The 10px monospace uppercase rectangle, extracted from two copies that were written independently — `PlanCard`'s inline tier span and `ProductWindow`'s `WindowStatus` — which had already drifted apart on tracking before anyone noticed. Its job is to hold the geometry still: `tone` carries colour and nothing else, with tracking as the single declared exception. It exists as much to stop a third inline copy being written as to render the two that exist.

## Exports

`Chip`

## Props

| export | prop | type / allowed | required | default |
| --- | --- | --- | --- | --- |
| `Chip` | `tone` | `ChipTone` |  | `'neutral'` |
| `Chip` | `children` | `React.ReactNode` | **yes** |  |

## Also for

Every claim cites the prop, default or export that makes it true. A claim that cannot cite one is rejected by a test.

| claim | via |
| --- | --- |
| An identity marker on either ground with `tone="neutral"`. This is the one tone allowed to use invariant `chrome.surface-darker` / `brand.light`, because its fill is near-black on both the dark page AND the featured cream `PlanCard` — there is no inversion for an invariant token to get wrong. | `prop Chip.tone` |
| A state marker inside a light surface with `tone="healthy"` (matcha) or `tone="live"` (violet). Both derive their fill through `color-mix` from mode-aware tokens, so they follow a `light` ancestor down to the light value rather than laying a dark-mode tint on cream — safe inside a light `ProductWindow` body or a cream card. | `prop Chip.tone` |
| Wrapping in a narrower-vocabulary named component. `WindowStatus` (product-window.tsx:97) is the sanctioned template: it restricts `tone` to `'healthy' \| 'live'`, pins its tracking override, and keeps the name callers actually look for. Do that again rather than adding an inline chip. | `prop Chip.tone` |
| Matching an already-shipped rendering. `className` merges last via `cn`, so a `tracking-*` utility beats the base 0.08em — the documented, and only documented, override. | `className` |
| Non-string content: `children` is a `ReactNode`, so a glyph plus a word, or a count, works. `shrink-0` is in the base, so the chip is safe as the fixed item in a flex row opposite a title that wraps. | `prop Chip.children` |
| A breakage or defect marker with `tone="danger"`. Its recipe deviates from `healthy`/`live` on purpose: the ink is the on-tint token (`--color-semantic-error-red-on-tint`), the split `StatPill` and `Finding` both shipped without and measured below the floor for, and the tint is 12% — inside the band those tokens were measured for. Both halves are mode-aware, so it holds inside a light `ProductWindow` as well as on the page. | `prop Chip.tone` |
| A caution marker with `tone="warn"` (amber): present but degraded, deprecated, or waiting on a decision. Same corrected recipe as `danger` — amber on-tint ink over a 12% tint — added because two marketing pages were retinting `tone="neutral"` through a shared amber `className`, base ink on a 15% fill, which is the inline fork plus the measured-band miss in one constant. | `prop Chip.tone` |

## Not for — reach for this instead

| instead | why |
| --- | --- |
| `Badge` | General status on a product surface. `Badge` (ui) is the shadcn primitive for that; `Chip` is section furniture locked at 10px mono, and stretching it to product use will produce pressure to make the geometry variable — the one thing it is here to prevent. |
| `StatChip` | A pill beside a heading. `StatChip` (a fact) or `MetaChip` (a promise) own that slot at `rounded-full` 12px; `WindowStatus` owns the window title bar's right-hand slot. |

## Easy to confuse with

`sections/stat-chip#StatChip`

These edges are symmetric — the same pairing is recorded from the other side.

## Watch for

| note | via |
| --- | --- |
| `className` merges last and therefore beats the base for *any* utility, but `tracking-*` is the only override this package sanctions. Changing `px-[7px]`, `py-1`, `rounded-[5px]` or `text-[10px]` at a call site re-forks the component the extraction exists to prevent. | `className` |
| Tracking is unreconciled on purpose: `Chip` holds the tier chip's 0.08em and `WindowStatus` overrides back to 0.05em, because both values shipped and were signed off in the browser. "Fixing" either one silently restyles a verified surface — about 5px across "DASHBOARD: HEALTHY", per the note at the override. | `export Chip` |
| `healthy` and `live` set their colour in an inline `style` from the `TONES` table, so a `text-*` utility in `className` will not override them. Only `neutral` colours via classes. | `className` |
| Adding a tone is a row in `TONES`, never a branch at the call site — and the file's stated rule is to add one only the day something actually renders it. If you add a light-filled tone, do not copy `neutral`'s invariant `chrome.*` licence; that exemption depends on the fill being near-black on both grounds. | `prop Chip.tone` |

## Reachable from outside

`className`

## Constraints

From `machine/components.yaml` (`sections.Chip`).

| prop | allowed |
| --- | --- |
| `tone` | `neutral`, `healthy`, `live` |

- The 10px mono rectangle, extracted 2026-08-12 from two copies that were written independently: PlanCard's inline tier span and WindowStatus. Do not write a third inline one — add a tone here instead, and only when something actually renders it.
- tone carries colour only. The geometry is fixed, with exactly one declared exception: tracking. The two originals differed (WindowStatus 0.05em, PlanCard tier 0.08em), Chip holds 0.08em, and WindowStatus overrides back to 0.05em so its verified rendering did not change. That divergence is unreconciled on purpose — picking a winner is a visual decision, not a refactor.
- neutral is the only tone allowed invariant chrome.* / brand.light: its fill is near-black on the dark page AND on the featured cream PlanCard, so there is nothing for an invariant token to get wrong. healthy and live mix their fill from mode-aware tokens through color-mix so they follow a `light` ancestor.

## Rendered in

Gallery cases with a committed light and dark baseline: `section-chip`.

## What binds this module

- **Polarity `inherits`.** It puts no theme class on its own root, so it takes the page. Place it on a light fill and the `light` class is yours to add, or its mode-aware tokens resolve to their DARK values against that fill — measured at 1.07:1 on rendered pixels.
- **`chrome.*` is invariant.** This module is in the fixed-dark world, so the distinction bites here: `chrome.*` never inverts and belongs only on a surface that never flips. Anything on a surface that follows the theme uses `text.*` / `surface.*`. The two share their dark values, so the wrong pick looks correct until someone opens light mode.
- **Content is props.** This section hardcodes no copy; the page supplies it.

These are the rules this module can break. The full set, the surface roles and
the contrast floors are in [DESIGN.md](../../DESIGN.md).

---

System-wide tokens, scales and the full module index: [DESIGN.md](../../DESIGN.md).
