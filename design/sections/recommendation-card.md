# sections/recommendation-card

<!-- @design:generated -->

_Generated by `scripts/generate-design-md.mjs` from `machine/context.yaml`, `machine/components.yaml`, `machine/compositions.yaml`._
_Do not edit. Change the source and run `npm run design`; `npm run design:check` fails the build if this file is stale._
_@skene/design-system 0.26.0._

|  |  |
| --- | --- |
| import | `@skene/design-system/sections/recommendation-card` |
| polarity | `inherits` |
| boundary | server-renderable |
| intent | `drive-an-action`, `prove-a-claim` |

## Use for

The proposal: what to do next, and what makes it worth doing. Deliberately without a status colour.

## Exports

`RecommendationCard`

## Props

| export | prop | type / allowed | required | default |
| --- | --- | --- | --- | --- |
| `RecommendationCard` | `eyebrow` | `React.ReactNode` |  |  |
| `RecommendationCard` | `title` | `React.ReactNode` | **yes** |  |
| `RecommendationCard` | `children` | `React.ReactNode` |  |  |
| `RecommendationCard` | `meta` | `React.ReactNode` |  |  |

## Also for

Every claim cites the prop, default or export that makes it true. A claim that cannot cite one is rejected by a test.

| claim | via |
| --- | --- |
| Any suggestion block — a next step, a recommended setting, an upsell that should not look like an alert. | `prop RecommendationCard.title` |
| Qualifier chips under the body, via `meta`, which takes nodes so TagChip fits. | `prop RecommendationCard.meta` |

## Not for — reach for this instead

| instead | why |
| --- | --- |
| `Finding` | A measured state. That is Finding, which owns the status vocabulary. |
| `AgentCallout` | A verdict spoken by the product. That is AgentCallout — the same padding and eyebrow spec in a different voice. |

## Watch for

| note | via |
| --- | --- |
| It carries no status colour on purpose. An amber pill on a suggestion teaches a reader who has learned that amber means drift to read the suggestion as a fault. | `prop RecommendationCard.eyebrow` |
| Its fill and hairline mix from currentColor, so it works on the light window it shipped in and on the dark page with no prop. | `inline style` |

## Reachable from outside

`style`, `className`

`style` means this module writes an inline style that beats any class you pass.

## Constraints

From `machine/components.yaml` (`sections.RecommendationCard`).

| prop | allowed |
| --- | --- |
| `eyebrow` | mono kicker — RECOMMENDED NEXT STEP |
| `meta` | qualifier chips under the body; TagChip fits |

- It carries NO status colour, and that is the difference from Finding. Finding reports a measured state; this proposes a next step, and an amber pill on a suggestion teaches the reader that amber means two different things.
- Fill and hairline mix from currentColor, so the card works on the light AppWindow it shipped in and on the dark page without a prop.
- meta is a slot. MetaChip is the wrong part for it — that one marks something not shipped yet — so callers pass TagChip.

## Rendered in

Gallery cases with a committed light and dark baseline: `section-recommendation-card`.

## Page templates that carry it

| template | role |
| --- | --- |
| [use-case-page](../pages/use-case-page.md) | optional, in 1 of 3 |
| [company-page](../pages/company-page.md) | load-bearing |

## What binds this module

- **Polarity `inherits`.** It puts no theme class on its own root, so it takes the page. Place it on a light fill and the `light` class is yours to add, or its mode-aware tokens resolve to their DARK values against that fill — measured at 1.07:1 on rendered pixels.
- **Content is props.** This section hardcodes no copy; the page supplies it.

These are the rules this module can break. The full set, the surface roles and
the contrast floors are in [DESIGN.md](../../DESIGN.md).

---

System-wide tokens, scales and the full module index: [DESIGN.md](../../DESIGN.md).
