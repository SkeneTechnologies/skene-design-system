# sections/discovery-table

<!-- @design:generated -->

_Generated by `scripts/generate-design-md.mjs` from `machine/context.yaml`, `machine/components.yaml`, `machine/compositions.yaml`._
_Do not edit. Change the source and run `npm run design`; `npm run design:check` fails the build if this file is stale._
_@skene/design-system 0.26.0._

|  |  |
| --- | --- |
| import | `@skene/design-system/sections/discovery-table` |
| polarity | `inherits` |
| boundary | server-renderable |
| intent | `tabulate-data`, `show-status` |

## Use for

The discovery artifact: the events Skene found, where it found them, and whether each one verified.

## Exports

`DiscoveryTable`

## Props

| export | prop | type / allowed | required | default |
| --- | --- | --- | --- | --- |
| `DiscoveryTable` | `columns` | `DiscoveryColumns` | **yes** |  |
| `DiscoveryTable` | `rows` | `DiscoveryEvent[]` | **yes** |  |
| `DiscoveryTable` | `title` | `React.ReactNode` |  |  |
| `DiscoveryTable` | `source` | `React.ReactNode` |  |  |
| `DiscoveryTable` | `separator` | `React.ReactNode` |  | `'/'` |
| `DiscoveryTable` | `summary` | `React.ReactNode` |  |  |
| `DiscoveryTable` | `summaryStatus` | `StatPillStatus` |  | `'bad'` |
| `DiscoveryTable` | `actions` | `React.ReactNode` |  |  |

## Types — not components

These are TypeScript types, not exports you can render. `<DiscoveryColumns />` is not a component; the type describes the shape of a value you pass to one of the exports above. The name usually reads like a component, which is why this heading says so.

| type | shape |
| --- | --- |
| `DiscoveryColumns` | `event`, `type`, `foundAt`, `status` |
| `DiscoveryEvent` | `event`, `type`, `foundAt`, `status`, `statusLabel` |

## Also for

Every claim cites the prop, default or export that makes it true. A claim that cannot cite one is rejected by a test.

| claim | via |
| --- | --- |
| Any found-things table with a per-row state — files scanned, endpoints discovered, records matched. Every column header is a prop and every cell is a node. | `prop DiscoveryTable.columns` |
| A table with no header bar at all: title, source, summary and actions are all optional and the window drops the bar when they are absent. | `prop DiscoveryTable.summary` |

## Not for — reach for this instead

| instead | why |
| --- | --- |
| `EvaluatorList` | A list of evaluations rather than of found events. That is EvaluatorList, which is the same window with a different column contract. |
| `KeyValueTable` | A settings or reference table, or any table of three columns or fewer. That is KeyValueTable's job — it owns per-column formatting at two densities and a headerless `<dl>` rendering — while this module is the fixed four-column discovery artifact: event, type, where it was found, whether it verified. Narrowing it re-derives KeyValueTable with the discovery chrome attached. |

## Watch for

| note | via |
| --- | --- |
| `foundAt` is the column allowed to be empty, and an empty cell is what breaks a table's alignment. The gallery case carries a row without one for that reason. | `type DiscoveryEvent.foundAt` |

## Composes

`same-layer/artifact-shell`

## Rendered in

Gallery cases with a committed light and dark baseline: `section-discovery-table`.

## Page templates that carry it

| template | role |
| --- | --- |
| [product-page](../pages/product-page.md) | optional, in 1 of 4 |
| [home-page](../pages/home-page.md) | observed on 1 route |

## What binds this module

- **Polarity `inherits`.** It puts no theme class on its own root, so it takes the page. Place it on a light fill and the `light` class is yours to add, or its mode-aware tokens resolve to their DARK values against that fill — measured at 1.07:1 on rendered pixels.
- **Content is props.** This section hardcodes no copy; the page supplies it.

These are the rules this module can break. The full set, the surface roles and
the contrast floors are in [DESIGN.md](../../DESIGN.md).

---

System-wide tokens, scales and the full module index: [DESIGN.md](../../DESIGN.md).
