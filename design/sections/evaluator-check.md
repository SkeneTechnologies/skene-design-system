# sections/evaluator-check

<!-- @design:generated -->

_Generated by `scripts/generate-design-md.mjs` from `machine/context.yaml`, `machine/components.yaml`, `machine/compositions.yaml`._
_Do not edit. Change the source and run `npm run design`; `npm run design:check` fails the build if this file is stale._
_@skene/design-system 0.20.0._

|  |  |
| --- | --- |
| import | `@skene/design-system/sections/evaluator-check` |
| polarity | `inherits` |
| boundary | server-renderable |
| intent | `tabulate-data`, `show-a-metric` |

## Use for

The evaluator's Check tab: a metric, the formula under it, one row per operand, and the results it produced.

## Exports

`CheckChip`, `CheckFigure`, `CheckOperand`, `CheckResult`, `EvaluatorCheck`

## Props

| export | prop | type / allowed | required | default |
| --- | --- | --- | --- | --- |
| `CheckChip` | `role` | `boolean` |  | `false` |
| `CheckChip` | `children` | `React.ReactNode` | **yes** |  |
| `CheckFigure` | `label` | `React.ReactNode` | **yes** |  |
| `CheckFigure` | `value` | `React.ReactNode` | **yes** |  |
| `CheckOperand` | `name` | `React.ReactNode` | **yes** |  |
| `CheckOperand` | `chips` | `React.ReactNode` |  |  |
| `CheckOperand` | `tone` | `StatPillStatus` |  | `'warn'` |
| `CheckOperand` | `figures` | `React.ReactNode` |  |  |
| `CheckResult` | `label` | `React.ReactNode` | **yes** |  |
| `CheckResult` | `value` | `React.ReactNode` | **yes** |  |
| `EvaluatorCheck` | `crumb` | `React.ReactNode` |  |  |
| `EvaluatorCheck` | `actions` | `React.ReactNode` |  |  |
| `EvaluatorCheck` | `heading` | `React.ReactNode` | **yes** |  |
| `EvaluatorCheck` | `headingNote` | `React.ReactNode` |  |  |
| `EvaluatorCheck` | `metric` | `React.ReactNode` | **yes** |  |
| `EvaluatorCheck` | `formula` | `React.ReactNode` |  |  |
| `EvaluatorCheck` | `qualifiers` | `React.ReactNode` |  |  |
| `EvaluatorCheck` | `children` | `React.ReactNode` |  |  |
| `EvaluatorCheck` | `results` | `React.ReactNode` |  |  |
| `EvaluatorCheck` | `note` | `React.ReactNode` |  |  |

## Also for

Every claim cites the prop, default or export that makes it true. A claim that cannot cite one is rejected by a test.

| claim | via |
| --- | --- |
| Any computed-value explanation — a score, a rate, a derived KPI. The formula is a slot, so the operands can be anything. | `prop EvaluatorCheck.formula` |
| A metric with no formula at all: pass `metric` and `results` and leave the middle out. | `prop EvaluatorCheck.metric` |

## Not for — reach for this instead

| instead | why |
| --- | --- |
| `EvaluatorVerify` | The requirements a signal must meet, rather than the arithmetic. That is EvaluatorVerify. |
| `ScoreRing` | A single score with no working shown. That is ScoreRing. |

## Easy to confuse with

`sections/overview-tiles#OverviewTile`

These edges are symmetric — the same pairing is recorded from the other side.

## Watch for

| note | via |
| --- | --- |
| CheckChip and TagChip are the same 11px mono tag under two names — recorded as a known duplicate in the source rather than laundered. | `export CheckChip` |

## Reachable from outside

`style`, `className`

`style` means this module writes an inline style that beats any class you pass.

## Custom properties

`--border`

## Composes

`ui/badge`, `same-layer/artifact-shell`

## Rendered in

Gallery cases with a committed light and dark baseline: `section-evaluator-check`.

## Page templates that carry it

| template | role |
| --- | --- |
| [use-case-page](../pages/use-case-page.md) | optional, in 1 of 3 |

## What binds this module

- **Polarity `inherits`.** It puts no theme class on its own root, so it takes the page. Place it on a light fill and the `light` class is yours to add, or its mode-aware tokens resolve to their DARK values against that fill — measured at 1.07:1 on rendered pixels.
- **Content is props.** This section hardcodes no copy; the page supplies it.

These are the rules this module can break. The full set, the surface roles and
the contrast floors are in [DESIGN.md](../../DESIGN.md).

---

System-wide tokens, scales and the full module index: [DESIGN.md](../../DESIGN.md).
