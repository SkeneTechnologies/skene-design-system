# sections/traffic-lights

<!-- @design:generated -->

_Generated by `scripts/generate-design-md.mjs` from `machine/context.yaml`, `machine/components.yaml`, `machine/compositions.yaml`._
_Do not edit. Change the source and run `npm run design`; `npm run design:check` fails the build if this file is stale._
_@skene/design-system 0.15.0._

|  |  |
| --- | --- |
| import | `@skene/design-system/sections/traffic-lights` |
| polarity | `inherits` |
| boundary | server-renderable |
| intent | `frame-a-mock` |

## Use for

The three window-chrome dots — red, yellow, green, 10px each — as one `aria-hidden` group at an 8px gap. It exists so a shell bar, an editor header or a GitHub file header can carry window furniture without hand-rolling three spans, which is what five files were doing: `TerminalBlock` and `PrReview` in this package, and two skene-site routes that each declared a local copy plus a comment saying the fix belonged upstream.

## Exports

`TrafficLights`

## Also for

Every claim cites the prop, default or export that makes it true. A claim that cannot cite one is rejected by a test.

| claim | via |
| --- | --- |
| Any drawn window header, not only a shell — an editor chrome bar, a browser mock, a GitHub file header. It carries no terminal semantics at all: it is three coloured circles in a flex row, and it needs no parent and no context. | `export TrafficLights` |
| A roomier bar, by passing `gap-*` — `cn` treats it as a `gap` conflict so the caller's value wins. That is how `PrReview` gets GitHub's 12px against the terminal bar's 8px, from the same component. | `className` |

## Not for — reach for this instead

| instead | why |
| --- | --- |
| `StatPill` | Saying something. These are furniture and carry no state; a dot that means a state, with the word beside it, is `StatPill`, which binds bad \| warn \| ok to the semantic colours. |
| `Terminal` | A whole terminal frame. `patterns/terminal` already paints its own lights in CSS — reach for this only when you are building the bar yourself. |

## Easy to confuse with

`patterns/terminal#Terminal`, `sections/terminal-block#TerminalBlock`

These edges are symmetric — the same pairing is recorded from the other side.

## Watch for

| note | via |
| --- | --- |
| `className` lands on the WRAPPER. The three dots take invariant `terminal-chrome.traffic.*` tokens and are not reachable from outside — a recolourable red light is not a variant. | `className` |
| `patterns/terminal` draws the same three dots and does NOT use this: they are a `::before` with two box-shadows on `.skene-terminal` in `styles/effects.css`. A `Terminal` therefore has its lights whether or not you render this, and rendering both gives six. Which of the two terminals wins is the open `terminals` decision, and this component does not settle it. | `export TrafficLights` |
| There is no `size` prop. 10px is what both call sites shipped and what the CSS half draws; a fourth diameter would push the two terminals further apart rather than closer. | `export TrafficLights` |

## Reachable from outside

`className`

## Rendered in

Gallery cases with a committed light and dark baseline: `section-traffic-lights`.

## Page templates that carry it

| template | role |
| --- | --- |
| [developer-page](../pages/developer-page.md) | optional, in 1 of 2 |
| [community-page](../pages/community-page.md) | optional, in 1 of 2 |

## Rules that are not negotiable

1. **`chrome.*` is invariant and cannot invert.** `color.chrome.surface.*` and
   `color.chrome.text.*` are always dark. Use them only on a surface that never
   flips — a terminal, a log panel, a code frame. Anything on a surface that
   flips uses the theme-aware `color.text.*` / `color.surface.*`. The two share
   their dark values and diverge only in light, so the wrong pick looks correct
   until someone opens light mode.
2. **A light surface on a dark page needs the `light` class on its own root.**
   Without it the mode-aware tokens resolve to their dark values against a light
   fill. That has shipped text at 1.08:1. Check the module's `polarity` first —
   `applies-light` means the module already does this for you.
3. **Content is props.** No section hardcodes copy.

Theming is class-based and the package ships base mode `dark`. Contrast floors: body text 4.5:1, large text 3:1 (WCAG 2.2).

---

System-wide tokens, scales and the full module index: [DESIGN.md](../../DESIGN.md).
