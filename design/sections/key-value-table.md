# sections/key-value-table

<!-- @design:generated -->

_Generated by `scripts/generate-design-md.mjs` from `machine/context.yaml`, `machine/components.yaml`, `machine/compositions.yaml`._
_Do not edit. Change the source and run `npm run design`; `npm run design:check` fails the build if this file is stale._
_@skene/design-system 0.15.0._

|  |  |
| --- | --- |
| import | `@skene/design-system/sections/key-value-table` |
| polarity | `inherits` |
| boundary | server-renderable |
| intent | `tabulate-data` |

## Use for

The reference table: keys and values at two densities, with a masked value, a note and the 11px tag chip.

## Exports

`KeyValueTable`, `MaskedValue`, `TableNote`, `TagChip`

## Props

| export | prop | type | required | default |
| --- | --- | --- | --- | --- |
| `KeyValueTable` | `columns` | `KeyValueColumn[]` | yes |  |
| `KeyValueTable` | `rows` | `KeyValueRow[]` | yes |  |
| `KeyValueTable` | `density` | `KeyValueDensity` |  | `'reference'` |
| `KeyValueTable` | `headerless` | `boolean` |  | `false` |
| `MaskedValue` | `prefix` | `React.ReactNode` |  |  |
| `MaskedValue` | `length` | `number` |  | `16` |
| `TableNote` | `children` | `React.ReactNode` | yes |  |
| `TagChip` | `variant` | `TagChipVariant` |  | `'outline'` |
| `TagChip` | `children` | `React.ReactNode` | yes |  |

## Types

| type | shape |
| --- | --- |
| `KeyValueColumn` | `header`, `mono`, `muted`, `strong`, `nowrap`, `className` |
| `KeyValueRow` | `id`, `cells`, `className` |

## Also for

Every claim cites the prop, default or export that makes it true. A claim that cannot cite one is rejected by a test.

| claim | via |
| --- | --- |
| Any settings, environment or schema reference — the `reference` density drops the row fill and hover so it stops reading as a data grid. | `prop KeyValueTable.density` |
| A secret or token display via MaskedValue, which shows a prefix and a fixed number of dots. | `prop MaskedValue.length` |
| Column-level formatting without touching cells: mono, muted, strong and nowrap are per-column flags. | `type KeyValueColumn.mono` |
| The headerless key/value list — a settings readout whose column headings would only repeat what every row already says. `headerless` renders the same rows and column flags as a semantic `<dl>` (the term, then one `<dd>` per value column) rather than a table with visually hidden headers; the file header records why `sr-only` `<th>`s were rejected. | `prop KeyValueTable.headerless` |

## Not for — reach for this instead

| instead | why |
| --- | --- |
| `DiscoveryTable` | A found-things table with per-row status. That is DiscoveryTable. |
| `TableRow` | An interactive product grid. That is ui/table. |

## Watch for

| note | via |
| --- | --- |
| TagChip is one of five names in this package for the same 11px mono tag. Prefer it, and do not add a sixth. | `prop TagChip.variant` |

## Reachable from outside

`style`, `className`

`style` means this module writes an inline style that beats any class you pass.

## Custom properties

`--border`

## Composes

`same-layer/artifact-shell`

## Constraints

From `machine/components.yaml` (`sections.KeyValueTable`).

| prop | allowed |
| --- | --- |
| `density` | `reference`, `data` |
| `headerless` | boolean — renders a semantic `<dl>` instead of a `<table>` |
| `TagChip.variant` | `outline`, `solid` |

- `rows` is DATA here and `children` in DataTable, and the split is the whole point of having two components. Formatting that is a property of the COLUMN belongs on the column; it is what stops the tenth subprocessor row being the one that forgot `mono`. Anything genuinely per-cell still goes in the cell as a node — that is what TagChip and MaskedValue are for.
- `density` exists because the four call sites had already drifted into two: the prototype's .ptab and .dtab differ by 4px of vertical padding, a vertical-align, a hover fill and tabular-nums. That is one design at two densities. Do not add a third by overriding padding from a call site.
- `reference` density defaults column 0 to mono and nowrap, and that is sourced, not stylistic: the page notes enumerate where mono may appear and name the permission column, because that column holds things a reader matches against their own config. It is a default, not a rule — `mono: false` on the column turns it off where the first column is prose.
- `headerless` is a real `<dl>`, not visually-hidden `<th>`s, and it drops the header row for the readout whose headings would only repeat what every row says. Keep authoring the column `header`s anyway: they document the columns and every other column flag still applies.
- Nothing here sets a width or a min-w. The table scrolls inside ui/table's overflow-auto container, cells wrap, and only column 0 refuses to. Adding a width at a call site is what turns a table that scrolls into a page that scrolls.

## Rendered in

Gallery cases with a committed light and dark baseline: `section-key-value-table`.

## Page templates that carry it

| template | role |
| --- | --- |
| [product-page](../pages/product-page.md) | optional, in 2 of 4 |
| [use-case-page](../pages/use-case-page.md) | load-bearing |
| [developer-page](../pages/developer-page.md) | optional, in 1 of 2 |
| [comparison-page](../pages/comparison-page.md) | optional, in 1 of 2 |
| [community-page](../pages/community-page.md) | optional, in 1 of 2 |
| [company-page](../pages/company-page.md) | optional, in 1 of 2 |
| [pricing-page](../pages/pricing-page.md) | observed on 1 route |

## Rules that are not negotiable

1. **`chrome.*` is invariant and cannot invert.** `color.chrome.surface.*` and
   `color.chrome.text.*` are always dark. Use them only on a surface that never
   flips — a terminal, a log panel, a code frame. Anything on a surface that
   flips uses the theme-aware `color.text.*` / `color.surface.*`. The two share
   their dark values and diverge only in light, so the wrong pick looks correct
   until someone opens light mode.
2. **A light surface on a dark page needs the `light` class on its own root.**
   Without it the mode-aware tokens resolve to their dark values against a light
   fill. That has shipped text at 1.08:1. Check the module's `polarity` first —
   `applies-light` means the module already does this for you.
3. **Content is props.** No section hardcodes copy.

Theming is class-based and the package ships base mode `dark`. Contrast floors: body text 4.5:1, large text 3:1 (WCAG 2.2).

---

System-wide tokens, scales and the full module index: [DESIGN.md](../../DESIGN.md).
