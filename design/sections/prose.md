# sections/prose

<!-- @design:generated -->

_Generated by `scripts/generate-design-md.mjs` from `machine/context.yaml`, `machine/components.yaml`, `machine/compositions.yaml`._
_Do not edit. Change the source and run `npm run design`; `npm run design:check` fails the build if this file is stale._
_@skene/design-system 0.26.0._

|  |  |
| --- | --- |
| import | `@skene/design-system/sections/prose` |
| polarity | `inherits` |
| boundary | server-renderable |
| intent | `explain-a-process` |

## Use for

Long-form body copy whose HTML this package did not author: MDX, a CMS body, an array of sections rendered as plain tags. It styles its DESCENDANTS rather than rendering its own markup, which is the one place that is correct.

## Exports

`Prose`

## Props

| export | prop | type / allowed | required | default |
| --- | --- | --- | --- | --- |
| `Prose` | `density` | `'comfortable' \| 'compact'` |  | `'comfortable'` |

## Accepts

| export | props passed through |
| --- | --- |
| `Prose` | `React.ComponentProps<'div'>` |

## Also for

Every claim cites the prop, default or export that makes it true. A claim that cannot cite one is rejected by a test.

| claim | via |
| --- | --- |
| A dense column, a sidebar note or a definition list, where the comfortable rhythm is too loose. The vertical scale halves and the type does not move. | `prop Prose.density` |

## Not for — reach for this instead

| instead | why |
| --- | --- |
| `LightSectionCard` | A section with a heading, a lede and a visual, authored as props. This is for a document whose markup the caller does not control. |

## Watch for

| note | via |
| --- | --- |
| It applies NO panel: no border, no radius, no padding, no ground. Where the column sits and what frames it are page decisions. The implementation this came from welded its prose rules to a bordered card with a sidebar-docking variant, and the two pages using it framed it differently. | `prop Prose.className` |
| Sizes are token values rather than Tailwind text-* utilities on purpose. The package's marketing scale and Tailwind's default ladder are different, and mixing them is how a document ends up with two h2 sizes. | `export Prose` |

## Reachable from outside

`className`

## Custom properties

`--font-size-h1`, `--font-size-h2`, `--font-size-stage-header`, `--font-size-label`, `--font-line-height-relaxed`, `--font-size-body-sm`

## Rendered in

Gallery cases with a committed light and dark baseline: `section-prose`.

## What binds this module

- **Polarity `inherits`.** It puts no theme class on its own root, so it takes the page. Place it on a light fill and the `light` class is yours to add, or its mode-aware tokens resolve to their DARK values against that fill — measured at 1.07:1 on rendered pixels.
- **Content is props.** This section hardcodes no copy; the page supplies it.

These are the rules this module can break. The full set, the surface roles and
the contrast floors are in [DESIGN.md](../../DESIGN.md).

---

System-wide tokens, scales and the full module index: [DESIGN.md](../../DESIGN.md).
