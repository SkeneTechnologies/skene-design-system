# sections/billing-toggle

<!-- @design:generated -->

_Generated by `scripts/generate-design-md.mjs` from `machine/context.yaml`, `machine/components.yaml`, `machine/compositions.yaml`._
_Do not edit. Change the source and run `npm run design`; `npm run design:check` fails the build if this file is stale._
_@skene/design-system 0.15.0._

|  |  |
| --- | --- |
| import | `@skene/design-system/sections/billing-toggle` |
| polarity | `inherits` |
| boundary | `"use client"` — client component |
| intent | `collect-input`, `compare-options` |

## Use for

The monthly/yearly switch, and the only `'use client'` file in the pricing section. It exists as its own module for a structural reason rather than a visual one: putting the directive in `plan-card.tsx` would drag the whole pure-markup card across the client boundary, so the interactivity was quarantined here (sections.md rule 4). It is fully controlled — `yearly` in, `onChange` out — and makes both word labels clickable, not just the 40px track, because the words are the obvious target.

## Exports

`BillingToggle`

## Props

| export | prop | type | required | default |
| --- | --- | --- | --- | --- |
| `BillingToggle` | `yearly` | `boolean` | yes |  |
| `BillingToggle` | `onChange` | `(yearly: boolean) => void` | yes |  |
| `BillingToggle` | `monthlyLabel` | `React.ReactNode` |  | `'Billed monthly'` |
| `BillingToggle` | `yearlyLabel` | `React.ReactNode` |  | `'Billed yearly'` |

## Also for

Every claim cites the prop, default or export that makes it true. A claim that cannot cite one is rejected by a test.

| claim | via |
| --- | --- |
| Any two-word binary switch — the pricing wording lives entirely in props. `monthlyLabel`/`yearlyLabel` are `ReactNode`, so "USD / EUR", "Self-serve / Enterprise", "Cloud / Self-hosted" all work with no code change. | `prop BillingToggle.monthlyLabel` |
| Labels that carry a badge: since they are `ReactNode` and the row is `flex items-center gap-3`, `yearlyLabel={<>Billed yearly <Chip>-20%</Chip></>}` renders inline. | `prop BillingToggle.yearlyLabel` |
| Driving more than one section from one switch. It holds no internal state, so a single `useState` in a page island can feed the `PlanGrid` prices and a comparison table from the same value — the demo (`docs-app/app/pricing-demo.tsx`) does exactly this pattern with one plan grid. | `prop BillingToggle.onValueChange` |
| Placement and scale via `className`, which merges into the outer flex row through tailwind-merge: `mx-auto`, `justify-center`, `text-[12px]` all take. | `className` |

## Not for — reach for this instead

| instead | why |
| --- | --- |
| `Switch` | A settings or form boolean — use `ui/switch` or `SettingsField`. This one has marketing-sized side labels, a pricing-specific hardcoded aria-label, and no form/name/id integration. |
| `PillNav` | Three or more options (a third billing term, a segmented control). It is a two-state switch with `aria-checked`; use `ui/tabs` or the `PillNav` pattern instead. |

## Watch for

| note | via |
| --- | --- |
| `aria-label="Bill yearly"` on the switch is hardcoded and not derived from `yearlyLabel`. Repurpose the labels and the accessible name still says "Bill yearly" — fixing that needs one small prop addition (`switchLabel` / `aria-label` passthrough). | `prop BillingToggle.yearlyLabel` |
| Labels use invariant `chrome.text.*` (`text-chrome-text-primary` when active). Correct only on an always-dark surface; on a cream panel the active label is cream on cream. It is not safe inside a featured `PlanCard` or a `LightSectionCard`. | `prop BillingToggle.value` |
| The knob positions `left-1` and `left-[26px]` are hand-fitted to the track's `h-6 w-11` and the knob's `size-4`, and only `left` is transitioned. Resizing the track via `className` leaves the knob travelling to the wrong stop. | `className` |
| Controlled-only: there is no `defaultYearly` and no internal state. Omit `onChange` wiring and you ship a switch that visibly does nothing. | `prop BillingToggle.onChange` |
| It is the client boundary. Deep-import `@skene/design-system/sections/billing-toggle` rather than pulling it from the root barrel next to server-rendered siblings. | `use client` |

## Reachable from outside

`className`, `use client`

## Rendered in

Gallery cases with a committed light and dark baseline: `section-billing-toggle`.

## Rules that are not negotiable

1. **`chrome.*` is invariant and cannot invert.** `color.chrome.surface.*` and
   `color.chrome.text.*` are always dark. Use them only on a surface that never
   flips — a terminal, a log panel, a code frame. Anything on a surface that
   flips uses the theme-aware `color.text.*` / `color.surface.*`. The two share
   their dark values and diverge only in light, so the wrong pick looks correct
   until someone opens light mode.
2. **A light surface on a dark page needs the `light` class on its own root.**
   Without it the mode-aware tokens resolve to their dark values against a light
   fill. That has shipped text at 1.08:1. Check the module's `polarity` first —
   `applies-light` means the module already does this for you.
3. **Content is props.** No section hardcodes copy.

Theming is class-based and the package ships base mode `dark`. Contrast floors: body text 4.5:1, large text 3:1 (WCAG 2.2).

---

System-wide tokens, scales and the full module index: [DESIGN.md](../../DESIGN.md).
